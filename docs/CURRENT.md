# CURRENT

task: real second guitar part -- pedal/chug doubler (scope §17.1 extension)
phase: cross-cutting (post-Phase-9 scope reconciliation)
status: DONE
last_pytest: engine/ 637 passed, 1 skipped; editor/backend/ 34 passed
note: Direct continuation of the same-day listening-feedback loop (atmosphere/pad export fix -> synth-doubles-the-riff wiring -> this). After the synth-double fix, user feedback ("still sounds like garbage... not something Born of Osiris would write") kept being about composition, not mix. Iterated through several real, data-backed preset/vocab adjustments (metalcore's 52%-root vocab diluting melodic character; a progressive x melodic blend). User then made a sharp, correct call: "Its probably because you can't hear real born of Osiris you're just guessing." True -- so instead of continuing to guess from genre description, found a real reference MIDI the user had already supplied earlier in the session (`C:\Users\RIGGUSPIG\Downloads\born of osiris style midi.mid`) and actually parsed it: Krumhansl-Schmuckler key correlation (reusing `audio_vocab.estimate_key`'s own real technique, fed a MIDI-derived pitch-class histogram instead of audio chroma) plus a real per-track interval-vocabulary histogram.

Measured result: the reference has TWO distinct rhythm-guitar tracks, not one double-tracked riff -- "Guitar 1" (49.8% root, real P5/m3/m6 color, spans 44 semitones) is the wide melodic riff; "Guitar 2" (95.9% root, tight low register) is a genuinely separate, near-monophonic pedal/chug part. This project's engine only ever double-tracked ONE riff (`guitar_take_a`/`guitar_take_b`, same motif, independently humanized for stereo width). User confirmed (via AskUserQuestion, over saving the current preset or pivoting to audio quality): build this real second-guitar architecture.

Entered plan mode given the scope (touches generation, cross-section blending, regen wiring, export, tests). Plan: new `section["guitar_pedal"]`/`pedal_pitches_per_cell`, generated in `_generate_one_section` right after the existing `double_track` call, skipped for chill/interlude. Pitch drawn via a new `_PEDAL_DOUBLE_WEIGHTS` vocab (the real measured percentages) through the exact same `generate_pitch_deltas` mechanism the main guitar already uses; rhythm reuses the main guitar's own cells (locked timing, a documented simplification matching real "low string doubler" production practice); single-take humanized (not double-tracked). Blended across section boundaries via the same real `_pickup_cells`/`_pickup_values` pair already blending `guitar_take_a`/`pitches_per_cell`, in both `_generate_attempt`'s main pass and `regenerate_section`'s neighbor re-blend. Bass/kick locking, judge scoring, and the editor's tab/stat display deliberately left keyed to `guitar_take_a` only -- additive, not a replacement for any existing relationship.

New "Guitar (Pedal)" MIDI/`.rpp` track. Caught and fixed a real, pre-existing test fragility the new rng draws exposed (not a bug in the new code): a kick-lock test had no exclusion for X.34's blast-fill overriding a "breakdown" section's kick/snare regardless of kick_style -- fixed using the same "blast fired" heuristic already established elsewhere in that test file.

Verified against real generated output (every section shows real, root-heavy pedal-guitar content, matching the measured ~96% root ballpark) and live in the running editor (all 9 real tracks arrive over `/api/export-midi`, zero console errors). Re-rendered the demo song (now also using a preset calibrated from the same real reference data: minor scale, measured interval weights, heavier pedal bias) via the already-downloaded fluidsynth + GeneralUser-GS pipeline and sent the user an updated MP3.
updated: 2026-09-10

Remaining, per the earlier scope re-read (unchanged by this fix): P10.2 (Song JSON/whole-arrangement persistence), small unwired devices (`interplay.call_and_response`, `structure.apply_half_time`, `slam.chromatic_creep`), §17.2's dense-passage tab flagging, and all of Phase 8's real audio-render pipeline (P8.2-P8.8) beyond `.rpp` generation -- the GM soundfont ceiling is still a real, acknowledged limit on how "produced" any of these demo renders can sound regardless of further composition work. See TASKS.md's tracker for full detail.
