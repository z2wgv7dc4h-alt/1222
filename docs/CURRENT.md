# CURRENT

task: X.20
phase: cross-cutting
status: DONE
last_pytest: 528 passed, 1 skipped in 16.45s
note: Fixed a real, severe density bug. song.py was passing preset.open_chance as hit_chance (rhythm.generate_rhythm's rest-vs-hit probability). Checked the real reference source (reference/ww-forge-prior-attempt/engine/riff_engine.py): open_chance there is wants_open = rng.random() < open_chance -- open-string-vs-muted PITCH articulation per hit, a completely different axis from rest-vs-hit density. Verified real damage: tech.json ("extreme technical... kick-locked triplet chug") had a REAL 5.56% hit rate -- 94% rests -- because its correctly-low open_chance (0.10) was misapplied as "almost never even a hit." Almost certainly the biggest real root cause behind the session's recurring "not enough intensity" complaints. Found the real fix in the same reference file: base_density=0.72, density=clamp(base*(0.55+0.9*energy), 0.12, 0.98) -- ported exactly as song._resolve_hit_chance, driven by theory.arc()'s already-computed per-role "energy" field (never consumed before this). open_chance itself stays validated but unwired -- its real purpose (open-string articulation) needs a pitch-model axis this project doesn't have yet, tracked as a real deferred gap in the Scope gaps tracker. Verified: tech 5.56%->86.11%, deathcore 25%->68%, every preset now 68-88% (genre-plausible). Also fixes backwards build<breakdown density ordering from before. 528 passed (up from 525; one pre-existing test's margin narrowed 0.05->0.03, a real expected consequence of density now varying by role). Next: get real listening feedback on this + X.19 together before assuming they close "not composed/intense enough" -- then open_chance's real purpose, sec.14.2 item 5 (key modulation), P10.2 Song JSON, Phase 9 editor.
updated: 2026-09-08
