---
name: port
description: Port one named symbol from /reference. Use when TASKS says port VoiceLeader, pitch_to_fret, shade, IRVD, ARC, loopSequence, or similar.
---

1. Open only the file listed for that symbol in `PORTS.md`.
2. If the path is missing, stop and say so. Do not invent the algorithm.
3. Port behavior + tests. Do not rewrite the idea from memory.
4. Wire the symbol into the real call path in the same change.
5. Run pytest. Then `/close`.
