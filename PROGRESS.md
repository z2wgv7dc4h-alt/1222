# PROGRESS

## 2026-09-08 — knowledge-chat close
Decisions:
- Discard `123/` as foundation. Keep only as cautionary reference + `RHYTHM_ENGINE_FIXES.md` pattern.
- metalerator superseded. djent-master: port algorithms only. Anvil/react-chords: ignore except optional WAV preview / VexFlow not react-chords.
- Ww/Forge: port named symbols in PORTS.md; do not run Ww.
- Engine is writer. No Suno/ACE-Step as composer.
- New FCC loop: short CLAUDE.md + atomic TASKS + hooks + one task per /clear.
- Completed P0.1: Created folder structure (engine/, engine/tests/, editor/, docs/, reference/, assets/drums/, assets/nam/)
- Completed P0.2: Confirmed assets/drums/ has .sfz and assets/nam/ has .nam files
- Completed P0.3: Set up pytest with test_smoke.py asserting True (1 passed in 0.01s)
- Completed P0.4: Verified .gitignore from kit is present (no assets/, no reference/)
- Completed P0.5: Confirmed docs/STATUS.md template exists with no Working claims

Phase 0 complete. Ready for P1.1: Fretboard + MIDI↔(string,fret)

## 2026-09-08 — P1.1-P1.5
- P1.1: `engine/fretboard.py` `Fretboard` class, MIDI<->(string,fret).
- P1.2: ported `pitch_to_fret` from `reference/ww-forge-prior-attempt/engine/tab_score.py` onto `Fretboard` (lowest-string, then cost `abs(fret_diff)+abs(string_diff)*2`).
- P1.3: `pitch_to_fret` raises `ValueError` on an unplayable pitch instead of the source's fake-position fallback; tested.
- P1.4/P1.5: `engine/scales.py` — `SCALES` table incl. `cluster`/`power`, `ALIASES` for shared-interval names (aeolian==minor); `test_no_duplicate_non_alias_scales` checks uniqueness.
- Fix: had created a nested `engine/engine/` package by mistake; flattened back to the single `engine/` folder (the one with `pyproject.toml`). Added `.claude/rules/anti-patterns.md` rule + `test_no_nested_engine_folder` so this fails loudly if repeated.
- Scope-index cleanup after a full read of `god-tier-metal-scope.md`: `PORTS.md` tunings row was missing `drop_e_8` (8-string); `SCOPE-INDEX.md`/`TASKS.md` P5 pointed at `## 6` (no bass content there) instead of `### 14.2` item 2; P8/P10 were missing `### 14.1`/`### 14.2` respectively. All corrected.
- 15 passed in 0.03s. Next: P1.6 tuning JSON from source MIDI.

## 2026-09-08 — Phase 1 + Phase 2 complete (parallel build)
- User directed a faster pace for this session: multiple worktree-isolated agents, still wired+tested per box, no shortcuts on rigor.
- Phase 1 (P1.6-P1.11) and Phase 2 (P2.1-P2.8) built concurrently by two agents in separate git worktrees (genuinely independent -- rhythm is pitch-agnostic), merged cleanly, no file conflicts.
- P1.6-P1.9: `engine/presets.py` + `engine/presets/*.json` -- 7 tunings, 6 mood presets, schema validator wired into the loader, glob-discovered.
- P1.10: `engine/theory.py` -- VoiceLeader (pick/walk/move/stab), shade(), ARC (density = 0.28+0.62*energy, derived not settable).
- P1.11: `engine/chords.py` -- ground-up chord-shape solver, no lookup tables.
- P2.1-P2.8: `engine/rhythm.py` -- generate_rhythm, tile_cell, metric_polyrhythm, tuplet_grid, pick_blast_type, RhythmRegistry, phrase_plan (IRVD), seeded-RNG reproducibility test.
- Enriched the 6 presets post-merge with the real `PACKS`/`VOCAB` data from `style_packs.py` (bpm, bars, feel, open_chance, octave_stab, kick style, per-style weighted interval vocab + motion, plus an ALIASES table for old band ids) -- the first pass used an invented minimal schema; the real, already-tuned data serves the Born-of-Osiris/Infant-Annihilator/Veil-of-Maya quality bar directly.
- Found and fixed a real correctness bug: P2.7's IRVD was implemented from PORTS.md's prose description because style_packs.py (the file PORTS.md named) doesn't contain IRVD logic. Located the real source (theory.py's `phrase_plan`, via a `riff_engine.py` comment) and re-ported verbatim -- the real rounding rule differs from the guess. Corrected PORTS.md's file attributions for IRVD, flatten+pickup (song_writer.py), and judge/retry (riff_engine.py's real `judge()` algorithm, now documented exactly).
- Expanded TASKS.md against a full re-read of god-tier-metal-scope.md: added P3.9-P3.12 (call-and-response, slam devices, chord-solver wiring, optional 2nd guitar), P6.6-P6.7 (atmospheric interlude section type, tempo curve), P9.7 (section-level presets) -- all real scope items that had no task box.
- Deleted stray empty "New Text Document.txt".
- 159 passed in 0.24s. Next: Phase 3 (Motif/riff), Phase 4 (Drums), Phase 5 (Bass) -- dispatching as parallel agents next.

## 2026-09-08 — Phase 4 (Drums) + Phase 5 (Bass) complete; Phase 3 (Motif) in progress
- Both merged cleanly from separate worktrees, no conflicts (distinct new files: drums.py, bass.py).
- P4.1-P4.3: `engine/drums.py` -- role->MIDI map for the real SFZ kit (god-tier-metal-scope.md sec.11.7), wired CHINA->CRASH_2 fallback (this kit has no china sample), kick_follows_guitar (exact hit-position match), generate_blast_fill using the shared RhythmRegistry + pick_blast_type, three blast renderers (traditional/gravity/hammer).
- P5.1-P5.2: `engine/bass.py` -- derive_bass_tuning anchors the bass's TOP string an octave below the guitar's lowest open string then descends in perfect 4ths (guarantees the bass stays below the guitar even for wide extended-range tunings, unlike naively copying the guitar's lowest 4 strings down an octave); follow_guitar_rhythm locks to the guitar's hit/rest pattern but resolves every hit to the bass's own playable position, searching every octave of the target pitch class and falling back to the nearest reachable one.
- User supplied a much larger real MIDI corpus (`C:\Forge\user\midi\MIDIS.zip`, 1967 files) -- extracted into `reference/midi-corpus/` (gitignored) for P4.4, superseding the sparse ~63-file sample that shipped in the original reference bundle.
- 189 passed in 0.30s. Next: Phase 3 (Motif) landing shortly, then P4.4 (MIDI vocab mining), then Phase 6 (Structure).

## 2026-09-08 — Phase 3 (Motif/riff) complete -- Phases 1-5 all done
- `engine/motif.py`+6 supporting modules (groove/lead/performance/interplay/slam/riff), 47 new tests, 236 passed total.
- All 12 items done including the optional P3.12 (harmonized second guitar).
- P3.11 is the standout: wired `chords.solve_chord` (built in Phase 1, P1.11) into a real riff-generation call path via `riff.voice_chord_section` -- it had no caller anywhere until this.
- P3.8's double-tracking has an explicit regression test against the exact historical mistake documented in god-tier-metal-scope.md §18.3 (an earlier attempt's "double-tracking" was one performance time-shifted +8 ticks, not two independent takes).
- Caught a bookkeeping gap while ticking boxes: TASKS.md's `## Next` section had been recording progress, but the underlying per-phase sections (Phase 2, Phase 3) were never ticked when `## Next` moved on to the next wave -- fixed now; will keep both in sync going forward.
- 236 passed in 0.50s. Next: P4.4 (MIDI vocabulary mining, unblocked now that drums.py exists), then Phase 6 (Structure).

## 2026-09-08 — P4.4 complete: MIDI corpus density vocabulary
- `engine/midi_vocab.py`: parses the 1967-file real corpus with `mido` (new dependency, already installed), extracts hits-per-beat/fill-length/distinct-note stats per file, aggregates into 20-BPM-wide buckets + per-pack summaries, caches to `engine/data/midi_vocab.json`. `vocabulary_informed_hit_chance(bpm)` maps a target BPM to a density-informed `hit_chance`, wired into `drums.py` via `generate_vocabulary_informed_blast_fill` (a real call path, not a dead function).
- Real finding worth remembering for preset tuning later: density clearly falls as BPM rises across the whole corpus (80-100 BPM: 6.4 hits/beat -> 220-240 BPM: 2.8 hits/beat) -- faster sections are rhythmically sparser in real reference material, not busier.
- Honest deviations the agent flagged: this corpus has no tempo meta events (BPM comes from folder/file names via regex) and uses channel 0 for drums, not channel 9/10 as initially assumed.
- 256 passed in 4.48s (main's full corpus is present, unlike the isolated worktree which correctly skipped the corpus-dependent test). Next: Phase 6 (Structure) landing shortly.

## 2026-09-08 — Phase 6 (Structure) complete -- Phases 1-6 + P4.4 all done
- `engine/structure.py`: weighted section graph, flatten+bridge+pickup (ported from song_writer.py -- pickup was unused dead code in the source, this port wires it in for real, an improvement over the original), half-time-in-a-section, arc()-driven modulation (P6.4 finally calls theory.arc(), which existed since Phase 1 but had no caller), interlude section type, judge/retry (ported from riff_engine.judge's real thresholds), and internal tempo curves.
- 287 passed in 2.33s. The full pure-Python, pytest-verifiable generation engine (Phases 1-6) is now complete. Remaining work (Phase 7 Atmosphere is still pure-Python; Phases 8 Reaper, 9 Editor, 10 Export need real external tools/human verification and can't be honestly marked WIRED+TESTED without the user present to run/listen/click).
- Next: Phase 7 (Atmosphere), then a status report before attempting Phase 8+.

## 2026-09-08 — Phase 7 (Atmosphere) complete + a full review pass found and closed the biggest gap
- `engine/atmosphere.py`: GM 90 pad / GM 56 orch-hit voicings, synth doubling (reuses motif.render_motif directly), accent-synced stabs. 302 passed.
- User asked for a full review + gap analysis. Biggest finding: Phases 1-7 each built and pytest-verified their OWN mechanism in isolation (rhythm, motif, drums, bass, structure, atmosphere), but nothing had ever taken a real `Preset` and run it through one real end-to-end generation pass. Confirmed via grep: no generation code anywhere read `preset.bpm`/`.bars`/`.open_chance`/`.kick`/`.group`/`.pedal`/`.octave_stab`, and `lead.py` (VoiceLeader-driven lead lines, built in Phase 3) had no caller anywhere.
- Closed this with `engine/song.py::compose_song(preset_id, seed)` -- a real four-piece band per section (two independently-humanized rhythm-guitar takes, a VoiceLeader lead line, bass locked to the guitar's rhythm) plus drums (kick + vocabulary-informed fills) and atmosphere (pad + accent stabs), judged/retried via the real Phase 6 `judge()`. `engine/tests/test_song.py` runs this against every real preset and passed on the FIRST attempt -- strong evidence the independently-built phases genuinely compose, not just individually correct.
- Also fixed: `pyproject.toml` still declared `dependencies = []` even though `drums.py` (imported almost everywhere downstream) now hard-imports `midi_vocab` -> `mido` at module level. Added `mido>=1.3.0` to match `requirements.txt`.
- Remaining honest gap (tracked as X.2 in TASKS.md): `preset.kick`/`.group`/`.pedal`/`.octave_stab` are validated in Phase 1 but no generation code branches on them yet -- `kick_follows_guitar` is one fixed algorithm regardless of a preset's declared kick style, and djent's signature 3-against-4 displacement/pedal behavior isn't implemented.
- 313 passed in 2.95s.

## 2026-09-08 — Wired ThemeRegistry, fixed 2 real bugs, added realistic lead behavior
- While discussing song.py with the user, walked through the code together and found `motif.ThemeRegistry` (built in Phase 3) had no caller anywhere -- every section was getting an independently-rolled motif instead of a developed recurring theme, which the scope doc calls "the single highest-leverage change for making songs sound composed rather than generated." Wired it into `compose_song`, keyed per section role, with `invert()` applied on alternate reuses.
- That surfaced a real bug: `invert()` can push a rendered pitch below the guitar's lowest open string (confirmed via the integration test suite, which failed loudly and correctly rather than silently). Fixed with `_snap_to_playable_octave`, mirroring the discipline `bass.py` already applies to the bass voice.
- User asked for real solos -- added role-aware lead generation: `solo` sections get a genuinely denser, higher, more active featured line; `chill`/`interlude` get a harmonized doubling of the rhythm's own theme (`riff.harmonize_line`); everything else is silent, since a busy independent lead would clash with a dense chug section (per the user's own sharp observation that the two guitars should be doing different things at different points, not the same generic thing everywhere).
- While testing the solo feature, found a second real bug: `compose_song`'s `seed` parameter was completely dead -- it delegated to `structure.judge_and_retry`, which always tries its own `range(max_seeds)` (0, 1, 2...) internally regardless of the caller's seed, so every call for a given preset silently collapsed onto whichever of seeds 0-5 happened to judge `ok` first. Replaced with a local retry loop seeded `seed, seed+1, ...` -- confirmed different seeds now produce genuinely different song structures (verified: seeds 0-4 on the djent preset now produce 5 distinct section sequences, and "solo" sections actually appear, which they never did before the fix).
- User named Periphery as a specific, standing musicianship goal (odd-grouping displacement, legato/technical lead lines, extended-chord ambient sections, metric modulation) -- added to CLAUDE.md's Bar line and tracked as TASKS.md X.6a/b/c (legato-run generator, extended chord vocabulary, metric modulation) since these are genuinely new capabilities, not just unwired existing data.
- 314 passed in 2.39s.

## 2026-09-08 — Real-audio analysis capability (audio_vocab.py)
- User asked whether the engine could actually "listen" to reference music, not just read tabs. Declined tab-reading specifically (reproducing a full copyrighted transcription with near-perfect recall is a different risk than a human's fuzzy auditory memory of a song they learned by ear, plus Songsterr-style sites prohibit scraping in their own ToS) but agreed real audio analysis was a genuinely better, buildable path: extracting structural/tonal STATISTICS from audio, never transcribing notes/chords -- the same "reference data, not composer" boundary the MIDI-vocabulary work (P4.4) already established.
- User supplied a real track (an original song a friend made, not a commercial release) to work from.
- Built `engine/audio_vocab.py`: tempo detection, a harmonic/percussive onset-density split (proxy for drum-like vs guitar-like rhythmic activity), a spectral-centroid brightness descriptor ("dark/scooped" / "balanced" / "bright/aggressive"), and a per-section relative density curve. Installed `librosa`+`soundfile` as real dependencies.
- User asked to run an "online demucs" -- declined uploading the friend's track to an unknown third-party site (neither the user's nor their friend's call to make), installed and ran the REAL `demucs` model locally instead (audio never left the machine), giving real drums-vs-rest stems.
- Ran the analysis on both real separated stems. Genuine finding: the guitar/bass stem has a noticeably wider section-to-section density range (0.73x-1.24x of the track's average) than the drums stem (0.85x-1.1x) -- in this track, the guitar arrangement does more of the dynamic shaping than the drums do, which is a real data point that validates `theory.arc()`'s existing design (a per-section energy/density curve primarily expressed through the tonal/rhythmic instruments, not just the kit).
- `demucs` itself is intentionally NOT a hard project dependency (pulls in PyTorch) -- documented in `audio_vocab.py` as an optional local pre-processing step, installed ad hoc when real stem separation is wanted.
- 319 passed in 8.79s (5 new tests, using synthetic click-track WAVs so CI/other machines never need a real audio file present).

## 2026-09-08 — Preset lineup change (no slam, add metalcore+deathcore) + deeper audio analysis
- User: project targets djent/deathcore/metalcore/technical-deathcore, not slam -- this directly contradicted CLAUDE.md's own stated identity line ("writes djent / tech-death / slam"), so updated it. Removed `engine/presets/slam.json` and its id (NOT `engine/slam.py`, an unrelated technique module for pinch-harmonic accents/chromatic creep that's genre-agnostic and stays). Added `metalcore.json` (drop_b_7, 2-step breakdown feel, octave stabs) and `deathcore.json` (drop_c_6, brutal/anchored, blast-heavy) as real NEW presets with their own designed tuning/bpm/vocab data -- documented in PORTS.md as newly designed, not ported, since neither existed in the original style_packs.py source. Lineup is now 7 ids: djent, tech, metalcore, deathcore, melodic, chill, groovy (user said "more diverse," so added rather than swapped 1:1; kept groovy/chill despite them not mapping to a named genre, since chill is a structural necessity and groovy is harmless variety).
- User pushed the audio-analysis work further ("figure out how you can fully understand it" / "can't you fully comprehend the song"). Extended `audio_vocab.py` with: `estimate_key` (Krumhansl-Schmuckler profile correlation), `structural_segments` (real detected section boundaries via MFCC self-similarity, not naive equal slices), `energy_curve` (RMS loudness per section), `tempo_stability` (per-window local tempo -- directly relevant to this project's own P6.7 tempo-curve feature), `classify_drum_onsets` (KICK/SNARE/HIHAT_OR_CYMBAL by onset frequency-band energy), and `guitar_rhythm_pattern` (straight/gallop/syncopated from onset TIMING alone, deliberately never pitch, since distorted-guitar pitch detection is unreliable).
- Found and fixed a real bug during this: the first `guitar_rhythm_pattern` draft bucketed each inter-onset-interval against the track's own global median, which silently fails for a genuine short-short-long gallop cycle (the short interval, being the majority value, IS the median, so nothing reads as "short" relative to itself). Redesigned around consecutive-IOI ratios instead (same/double/half), verified against synthetic straight and gallop click tracks built specifically to catch this.
- Ran the full extended pipeline against the real user-supplied track (via the demucs-separated stems from the earlier session). Real findings: (a) the track sits in A major -- a concrete, non-theoretical data point that the scale library's lack of major-family modes (tracked as new X.6d) is a real limitation, not a hypothetical one; (b) the drum-role classifier's own output on this track was implausible (only 20 detected snare hits across a 4-minute song) -- disclosed this honestly to the user as a real ceiling of the hand-built frequency-band heuristic on a dense, cymbal-heavy mix, rather than presenting the numbers as reliable ground truth.
- Also confirmed with the user: MIT-licensed `demucs` is fine to use and lean on going forward; documented as a real, promoted (if optional/heavy) part of the audio-analysis workflow rather than something used once ad hoc.
- 333 passed in 13.45s.

## 2026-09-08 — Real polyphonic transcription (basic-pitch), rhythm/lead register split
- User pushed further: "convert it to midi and analyse it more." Installed `basic-pitch` (Spotify's MIT-licensed polyphonic transcription model) -- its packaging turned out to be genuinely fragile on this Python version (pinned to `tensorflow<2.15.1`, which has no wheel for Python 3.12). Worked around it: installed with `--no-deps`, pulled in a modern `tensorflow` first (which broke basic_pitch's own TF-detection logic), then installed `onnxruntime` instead and let basic_pitch fall back to its ONNX model path, resolving several missing transitive dependencies (`six`, `importlib_resources`) one at a time until it actually ran.
- Ran it on the real isolated guitar+bass stem (from the earlier demucs separation): 1283 transcribed notes. In the dense riff section (50-132s), split notes by register (<MIDI 52 = rhythm, >=52 = lead) per the user's own observation ("there's lead and rhythm guitars") -- and the two layers cleanly separated on their own statistics: rhythm layer denser (3.43 notes/s vs 1.96), shorter notes (0.21s vs 0.33s avg), and a narrow 3-pitch-class cluster (A/F/C) consistent with pedal-tone chugging; lead layer sparser, longer, and spread across more pitch classes, consistent with an actual melodic line.
- Real cross-validation: the rhythm layer's dominant transcribed pitch class was A -- independently matching `estimate_key`'s separate, unrelated chroma-correlation key estimate (also A) computed earlier in this same session. Two different methods agreeing on the same answer is genuine evidence the analysis is tracking something real, not coincidence -- reported honestly as "evidence, not proof" rather than overclaiming certainty.
- Added `audio_vocab.transcribe_and_split_registers` as a real function (optional -- requires `basic_pitch`, intentionally NOT added to requirements.txt given how fragile its own dependency pins are; documented with the exact working install recipe in the docstring). Added `test_audio_vocab_transcription.py`: an ImportError-clarity test that always runs (simulates the missing-dependency path via import mocking) and a shape test that's skipped when `basic_pitch` isn't installed.
- 335 passed in 12.93s.

## 2026-09-08 — X.2 (kick/group/pedal/octave_stab) + X.6a (legato generator), merged from parallel agents
- Added major-family scales first (major/ionian, lydian, mixolydian, harmonic_major) -- closes X.6d, directly validated by the real A-major finding from the audio analysis work.
- Dispatched two parallel background agents: one to wire the unconsumed preset fields (X.2), one to build a legato-run generator (X.6a). Both were flagged in advance as likely to touch `song.py` -- expected, not a surprise.
- Both agents hit a session-wide rate limit mid-task and failed before committing anything; re-dispatched both fresh with the same briefs once the limit reset -- no work was lost since neither had made a commit yet.
- Both completed clean on the second run. Merged the legato branch first (clean, additive diff, no conflicts). Merged the kick/group/pedal branch second via git's real 3-way merge -- its own diff (taken against the pre-legato base) looked like it would delete the legato code, but a proper merge (not a manual patch) correctly combined both sets of changes since they touched different regions of `song.py`. Verified this directly by reading the merged file rather than trusting the auto-merge blindly: both `"legato"` and `"octave_stabs"` keys, and all the new imports, survived correctly together.
- X.2: `drums.kick_pattern_for_style` -- bounce/lock (exact guitar-lock), sparse (every-other-hit), euclid (genuine even-distribution rhythm, a documented Bjorklund-equivalent construction, verified against the canonical E(3,8) tresillo pattern), two_step (a documented metalcore breakdown interpretation -- kick at beat-offsets 0.0 and 1.5 per 2-beat cycle), blast (every cell). `motif.apply_pedal_bias` biases delta selection toward the root proportional to `preset.pedal`, preserving relative proportions of the other intervals (the same "rebalance, never zero out" principle `shade()` already uses). `preset.octave_stab` now adds real `VoiceLeader.stab()` leaps at accent positions when true.
- X.6a: `engine/legato.py` -- `generate_legato_run` walks CONTIGUOUS scale degrees via `scale.step()` (deterministic by default, optional single seeded direction-reversal), structurally distinct from `VoiceLeader`'s probabilistic pick-and-place model. `fret_positions_for_run` chains `Fretboard.pitch_to_fret`'s own `prev`-based cost minimization to bias a run onto one string -- the real reason hammer-on/pull-off technique works. Wired into every solo section in `song.py`, spliced onto the featured lead line, failing closed (skip, don't fabricate) if genuinely unplayable on a preset's tuning.
- Both closed with real A/B tests against the actual loaded `djent` preset (`dataclasses.replace` to flip one field, identical seeds on both sides) proving each field/capability measurably changes `compose_song`'s real output, not just passing in isolation.
- 382 passed in 10.24s.

## 2026-09-08 — X.6b (extended chord vocabulary) + X.6c (metric modulation), merged from parallel agents
- Dispatched two more parallel background agents to close the last two X.6 gaps: one to build a named extended-chord vocabulary for ambient sections (X.6b), one to build real metric modulation as a compositional device (X.6c). Both were flagged in advance as likely to touch `song.py` again, same as the X.2/X.6a pair.
- Both completed clean. Reviewed each agent's actual diff before merging (not just its self-report): `chord_vocab.py` matches `scales.py`'s existing dict-of-tuples convention and fails closed (raises on unknown quality/unreachable chord, same contract as `get_scale`/`solve_chord`); `metric_modulation.py`'s ratio math was checked directly against the required textbook example (dotted-quarter-becomes-new-quarter = ratio 1.5) before merging, not just trusted from the agent's summary.
- Merged X.6b first (fast-forward, no conflict). Merged X.6c second via git's real 3-way merge -- both branches modified `song.py`, but in disjoint regions (X.6b: inside the per-section loop's `chill`/`interlude` branch; X.6c: new module-level constants/helpers plus the final `comp` dict at the end of `_generate_attempt`), so the auto-merge combined them correctly. Verified directly by reading the merged `song.py`: both `chord_quality`/`chord_voicing` and `tempo_map`-producing code are present and intact together.
- X.6b: `engine/chord_vocab.py` -- `CHORD_QUALITIES` (maj7/min7/dom7/sus2/sus4/add9/maj9/min9/six9), `get_chord_quality`, `voice_named_chord` (delegates fingering to the existing `riff.voice_chord_section`/`chords.solve_chord` stack, no reimplementation), `quality_for_dissonance` (buckets a section's own already-computed `arc_row["dissonance"]` across a documented least-to-most-dissonant ordering). Wired into `song.py`'s `chill`/`interlude` sections as `section["chord_quality"]`/`section["chord_voicing"]`, alongside the existing `harmonize_line` doubling rather than replacing it; fails closed to `None` on a genuinely unreachable voicing.
- X.6c: `engine/metric_modulation.py` -- `modulation_ratio`/`apply_metric_modulation`, real subdivision-fraction math, fails closed on non-positive/malformed inputs. Wired as a new `"metric_modulation"` branch into `structure.tempo_at`'s existing curve dispatch (same pattern as the pre-existing `"drop"`/`"ramp"` types). `song.py` fires a real trigger on the first `build`->`breakdown` transition in the generated section sequence (straight eighth of the old pulse becomes the new quarter, ratio 0.5 -- the genre's standard half-time breakdown treatment), producing a new `song["tempo_map"]` output (one real BPM per section, computed through `tempo_at` for every composed song, not just the ones with a modulation).
- This closes X.6 in full: the whole user-named Periphery-musicianship goal (odd-grouping displacement via X.2's `group_beats`, legato/technical leads via X.6a, extended-chord ambient sections via X.6b, metric modulation via X.6c) is now wired and tested, not just tracked as a goal.
- 428 passed in 12.19s (up from 382).

## 2026-09-08 — P10.1: real MIDI export (Reaper-importable)
- Closed X.6 in full (previous entry), then moved to Phase 10 (Export) since it's genuinely buildable/testable in pure Python without needing Reaper open -- unlike Phase 8/9 which need real external tools or human ears to verify, MIDI-file correctness is fully checkable by parsing the file back and comparing against the song's own already-generated data.
- Built `engine/midi_export.py`: `song_to_midi(song, path, ppq=480)` writes a real Standard MIDI File (type 1) -- a dedicated tempo track using X.6c's real per-section `tempo_map` (not a flat single tempo), both double-tracked guitar takes, bass on its own fretboard's real positions, drums via the real GM kick note (`drums.note_for_role`), and the lead voice.
- The lead voice needed real design thought, not a guess: `harmony`-mode sections (chill/interlude) get their timing read directly from the rhythm motif's own cells, since `riff.harmonize_line` renders one note per guitar hit in the same order -- exact, not inferred. `solo`-mode sections have no stored per-note duration in `song.py`'s data model (just a pitch count), so those are placed at the exact even 8th-note spacing that count's own derivation (`round(total_beats * 2)`) implies -- documented as exact-given-the-generator's-own-math, not an approximation invented in the exporter. The X.6a legato tail spliced onto a solo DOES carry real per-note durations (`legato_run_rhythm`), used directly.
- Deliberately did NOT export `section["fill"]` (vocabulary-informed blast fills): grep confirmed `song.py`'s own `_generate_attempt` never actually blends it into the assembled `drum_track` it hands to `judge()` -- inventing a fill/kick blend in the exporter would be new arrangement logic this project's generation code doesn't itself define, which would violate "Grid is the writer, no new note-choice logic outside the generation modules."
- Tests verify real correctness, not just "a file exists": guitar/bass pitches and hit-counts cross-checked against the actual `pitches_per_cell`/bass-cell data for a real composed song; tempo-track values checked against real `tempo_map` (and asserts a real tempo change is actually present for the test seed, not just a flat file); drum notes checked against the real GM kick value (36) on the real percussion channel; solo lead note-count checked against real `lead` data. Every real preset round-trips through `compose_song` -> `song_to_midi` -> `mido.MidiFile` parse-back. Plus 2 bad-input rejections (non-positive ppq, mismatched tempo_map/sections length).
- User confirmed Reaper and Tone3000 are both already installed on this machine -- this changes Phase 8's status from "needs external tools I can't verify blind" to "realistically buildable and testable right now" (real `reapy-boost` scripting against an actually-installed Reaper, not a guess).
- Sent the user a real demo `.mid` file generated by this new exporter so they can drag it straight into their installed Reaper.
- 443 passed in 11.19s (up from 428).

## 2026-09-08 — Forward direction: editor UI stack, OSS/MIT leverage, preset calibration
- User asked for the Phase 9 editor to be "gorgeous," cutting-edge, and highly interactive/polished from the start, not a bare-bones afterthought -- saved as a standing preference (see auto-memory) since it will matter whenever Phase 9 work actually begins, likely a separate future session.
- Recommended stack for when Phase 9 starts: React + TypeScript + Vite (matches CLAUDE.md's existing "React later" plan), Tailwind + shadcn/ui (Radix primitives, MIT) for fast, accessible, polished components, Framer Motion (MIT) for interaction polish, VexFlow or alphaTab (MIT, already scoped at P9.6) for tab rendering, and Tone.js (MIT) or the raw Web Audio API for in-browser preview playback of exported MIDI -- keeping the actual final render on Reaper's side per CLAUDE.md's "Reaper renders" law, browser audio is preview-only.
- User asked what other real MIT/open-source leverage exists: `reapy-boost` (MIT, already scoped P8.1, real Reaper scripting -- now confirmed buildable since Reaper is actually installed), `sfizz` (BSD-2, sample playback engine for P8.4's sfizz+role-map item), `mido` (already a dependency), `librosa`/`soundfile` (already dependencies, BSD), `basic-pitch` (MIT, already integrated optionally). Noted Surge XT/Supermassive (already scoped at P8.5) are GPL-3, not MIT -- still usable as standalone plugins Reaper loads (no code linked into this project), just not code this project could vendor/embed directly the way an MIT library could be.
- User asked to put the real analyzed reference track's findings (X.7's audio_vocab.py work: real A-major key estimate, tempo/structure/drum-role data) to use ensuring generation quality, not just as a one-off analysis exercise -- this is exactly scope sec.17.6's "preset calibration" idea and item 12's "reference-corpus analysis," tracked as the still-open P10.3. Queued as a real next task: compare a preset's actual generated tempo/structure/density against the reference track's real measured values, and consider adopting the real A-major finding into a preset's scale field (the major-family scales existed since X.6d but were deliberately not yet adopted into any preset, per that task's own note that it "needs careful vocab-weight redesign, not a drop-in swap").

## 2026-09-08 — X.8: preset.feel wired via real Metalerator port; reapy-boost installed; djent reference-track calibration data gathered
- User asked directly: "are the presets actually going to generate that particular genre properly, look at how metalerator did it for metalcore." Investigated `reference/metalerator/` (a DIFFERENT, previously-unread folder from the `reference/ww-forge-prior-attempt/` and `123/`/`claude-code-first-attempt/` folders already covered by CLAUDE.md's read restrictions -- this one has no restriction and the user pointed straight at it). Its own README confirms it's a real, dedicated procedural metalcore generator, not a genre-agnostic engine metalcore was bolted onto.
- Found a real, concrete, previously-uncaught gap while investigating: `preset.feel` (bounce/breakdown/triplet/chug/wall/two_step) is validated at load time but was NEVER consumed by any generation code -- grepped the whole engine to confirm zero real usage. Same class of "declared but dead" gap as X.2's kick/group/pedal/octave_stab fields, just missed until now.
- Read Metalerator's real breakdown-guitar generator (`rhythm_guitar/breakdown/default_melodic.py`) and its kick module (`drums/kick/kick.py`). Found two genuinely portable techniques (not genre-locked, not plugin-specific): (1) a real weighted duration table for breakdown riffing, clearly biased toward 8th/quarter-note chugging over rapid 16ths or held long notes; (2) a "no isolated single 16th note" rule -- a lone 16th is always forced to pair with a second one. Also found techniques that are NOT portable: MIDI notes 12/14 are Ample Metal Hellrazer keyswitch triggers embedded directly in the note stream (a plugin-specific hack our cleaner role/cell-based articulation model already avoids), and its breakdown kick is literally guitar-accent-following (`default_kick`, explicitly commented "follows the pattern of the guitar") rather than a fixed grid -- closer to our existing `bounce`/`lock` kick styles than to `two_step`, worth noting but not changed (two_step is still a real, legitimate genre convention, just not this specific reference's literal approach).
- Ported the two portable techniques for real: `rhythm.FEEL_DURATION_WEIGHTS`/`FEEL_NO_SINGULAR_SHORT`/`duration_bias_for_feel`, new opt-in `weights`/`no_singular_short` params on `generate_rhythm` (default `None` preserves the exact old uniform behavior -- verified byte-identical via test, zero regression risk to the other 443 already-passing tests), threaded through `motif.generate_motif`/`ThemeRegistry.get_or_create`, wired at `song.py`'s existing `themes.get_or_create` call via `feel=preset.feel`. Only `"breakdown"` (metalcore's real feel) has ported reference data; every other feel name intentionally stays uniform rather than inventing a table with no real source. A real A/B test against actual `compose_song` output (metalcore vs. the same preset with an unmapped feel, same seeds) proves the 16th-note share measurably drops in real generated song data, not just in rhythm.py's own isolated tests. 450 passed in 10.76s.
- Installed `reapy-boost` (confirmed MIT-licensed via pip metadata) for Phase 8 and located the user's actual Reaper install (`C:\Program Files\REAPER (x64)\reaper.exe`) -- Phase 8 status upgraded from "needs external tools, can't verify blind" to a real, in-progress SCAFFOLD. User clarified they want Reaper driven entirely programmatically/by the eventual editor UI, never operated by hand -- the one-time `reapy` bridge configuration (which normally needs a human to click a "restart REAPER" dialog) will need to be automated via launching/restarting the Reaper process from script rather than asking the user to do it. Bridge/connection code itself not yet written -- next session's concrete next step.
- User pointed back at the same real friend's track already analyzed this session (confirmed via MD5, byte-identical to the earlier upload) as the explicit target/floor for djent quality: "we want our djent etc sounding something like this or better." Re-ran real analysis for precise current numbers: tempo (global beat-tracking) ~103 BPM (likely a half-time read of a genuinely faster felt pulse -- common librosa behavior on syncopated modern metal, not necessarily "the" tempo), key A major (confidence 0.48, same as the earlier finding), mean spectral centroid ~3805 Hz (bright/articulate, not dark/scooped). Compared against the real, current `djent.json`: phrygian scale (not major), motion 0.12 (very low -- root/5th-heavy, deliberately "far less scalar than tech" per its own description). This is a real, named mismatch worth a real follow-up: this specific reference track reads as "melodic technical djent" (the user's own words), which the current pure-djent preset deliberately isn't tuned for -- open question for next session is whether to adjust djent itself, lean on the existing `melodic`/`tech` presets instead, or design a new preset for this specific character, using the major-family scales added back in X.6d but never yet adopted into any preset.
- User also flagged drum-pattern richness as underdeveloped for deathcore/tech ("we want blast beats and double kicks too... everything heavily use it"). Checked real current state: `deathcore.json` already uses `kick: "blast"` (every-cell blast pattern) for real; `tech.json` uses `kick: "lock"` (kick-locked triplet chug) with no blast usage despite being the most extreme/technical preset; there is no dedicated continuous "double kick" (Metalerator's `double_bass`: a fixed continuous 16th-note pulse independent of the guitar's own rhythm, mechanically distinct from our existing `_kick_blast`, which is guitar-cell-driven) kick style in this engine yet. Real, scoped gap, not yet implemented -- next session's other concrete candidate.
