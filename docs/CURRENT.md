# CURRENT

task: atmosphere/pad export gap fix
phase: cross-cutting (post-Phase-9 scope reconciliation)
status: DONE
last_pytest: engine/ 620 passed, 1 skipped; editor/backend/ 34 passed
note: User asked for a full re-read of `god-tier-metal-scope.md` ("review the full scope file... there's a lot of gaps") now that Phase 9 is complete -- this IS the sanctioned reconciliation moment per `SCOPE-INDEX.md`'s own exception (after a batch of X.*/P9.* lands). Full 1130-line file read, cross-checked against TASKS.md's tracker + a series of targeted greps against the real code (not assumptions). Findings written into TASKS.md's `## Scope gaps tracker` (corrected a stale `structure.pickup`/`bridge`/`flatten` entry, flagged P10.2 as now more urgent given P9's editor has zero whole-song persistence, flagged §17.2's "flag dense passages in tab view" as still unimplemented despite P9.6 landing) and a stale, self-contradictory "Reaper | MISSING" row in `docs/STATUS.md` was removed.

The single highest-value finding: `song._generate_one_section` computes real `pad` (synth pad chord) and `octave_stabs` (accent-synced wide-interval leaps) data every section, every song -- real and tested since P7.1-P7.3 -- but a whole-engine grep confirmed NEITHER `midi_export.py` NOR `reaper_project.py` ever consumed either key. Scope §6/§16's synth/atmosphere layer (explicitly "close to mandatory for Born of Osiris atmosphere") and §16.3's "accent-synced orchestral hits" were silently inert in every real exported file all session -- plausibly a real contributor to earlier "still sounds thin" feedback. User picked this as the priority fix (via AskUserQuestion) over three other real candidates (P10.2 Song JSON, small unwired named devices, or bookkeeping-only).

Fixed: new `midi_export.py::_pad_events_for_section`/`_accent_events_for_section`, two new real MIDI tracks ("Pad" GM 90, "Accents" GM 56, merging `octave_stabs` into the same accent event since it's already 1:1-zipped with `accents`); `reaper_project.py` reuses the exact same two functions for two new `.rpp` tracks. `editor/frontend/src/components/MidiPlayer.tsx` gained dedicated undistorted `pad`/`accent` Tone.js synths + track-name routing (the two new tracks would otherwise have silently played through the distorted guitar rig). Verified live: real exported files for a real djent song (octave_stab=true) show real pad/accent note counts matching the math exactly (24 pad notes = 8 sections x 3-tone chord; 40 accent notes = 8 accents x 4-tone stab + 8 octave-stab leaps); confirmed in the running editor (after a manual backend restart -- `uvicorn --reload` only watches `editor/backend/`, not the sibling `engine/` dir, a real gotcha worth remembering) that the tracks arrive and play with zero console errors.
updated: 2026-09-10

Remaining, per the fresh scope re-read: P10.2 (Song JSON/whole-arrangement persistence), small unwired devices (`interplay.call_and_response`, `structure.apply_half_time`, `slam.chromatic_creep`), §17.2's dense-passage tab flagging, and all of Phase 8's real audio-render pipeline (P8.2-P8.8) beyond `.rpp` generation. See TASKS.md's tracker for full detail.
