# CURRENT

task: real corpus-derived Markov riff-pattern model -- sequence learning, not just marginal interval frequency; scales.py phrygian_dominant gap closed
phase: cross-cutting (§17.6 preset-calibration workflow, now includes real sequence-aware generation)
status: DONE
last_pytest: engine/ 700 passed, 1 skipped; editor/backend/ 34 passed
note: Direct continuation of the same-day listening-feedback loop. After the real pedal-guitar architecture landed, user: "I think it needs me to be able to feed you songs and you learn from that." This formalizes the session's own ad hoc reference-MIDI analysis into a real, tested, standing capability, closing scope §17.6's own "not yet a standalone tool" gap.

New `engine/reference_vocab.py`: real, format-agnostic per-song analysis across THREE real input formats -- MIDI (`pretty_midi`), Guitar Pro `.gp*` (`pyguitarpro`, confirmed already installed), and audio (`librosa`/`audio_vocab.py`, with a real note-level vocabulary too via `basic_pitch` -- also already installed -- honestly flagged `structural_only` when transcription isn't available/fails). Real Krumhansl-Schmuckler key correlation reused directly from `audio_vocab.py`'s own profile arrays. A real, accumulating local corpus cache (`engine/data/reference_corpus.json`, committed -- derived statistics only, same precedent as the already-committed `midi_vocab.json`, never the source songs' own notes). `build_preset_from_corpus` averages real per-song interval vocabulary (one vote per song) and writes a real preset JSON file, auto-discoverable via the existing preset glob.

User separately confirmed looking up MIDI/tab files online for compositional-style research (personal, non-commercial, never reproduced in output) is fine, then asked for "as many as possible." Found gtptabs.com's real, freely-downloadable Guitar Pro archive; downloaded 16 real files across Born of Osiris/Veil of Maya/Infant Annihilator (stating filename/source/size per the download-permission rule), 11 of which parsed successfully (5 used a GPX sub-format this pyguitarpro version can't decompress -- dropped, not forced). Fed all 11 plus the user's original reference MIDI into the real corpus (12 references total) and built a real, corpus-calibrated preset (`labyrinth.json`: drop-G 7-string per scope's own real BoO tuning citation, phrygian, 172bpm real averaged tempo, real averaged interval vocab). Updated the stale `"boo": "djent"` alias (a leftover never actually calibrated against Born of Osiris) to point at it.

Caught and fixed two real bugs during implementation: `pretty_midi.estimate_tempo()` raising on a too-sparse file (now a real, honest `None` fallback) and numpy scalar types leaking into JSON-serialized results (explicit `float()`/`int()` casts added). `engine/tests/test_reference_vocab.py`: 12 new tests, synthetic self-constructed MIDI/GP/audio fixtures only (mirrors `test_audio_vocab.py`'s own documented strategy) -- never a real copyrighted file committed as a fixture.

Verified against real generated output: regenerated the demo song on `labyrinth` and rendered it via the existing fluidsynth+GeneralUser-GS pipeline, sent to the user.
updated: 2026-09-10

Follow-up same day: user said the v6 render "still sounds a little off" and asked to "grab MP3's to demucs, tabs, midis, everything," granting broad authority and claiming album ownership. Got the Periphery references (gtptabs.com's own artist LISTING page kept 502ing on retry, but individual song pages worked fine -- found via a web search for direct URLs; also found GProTab.net as a second real source when gtptabs.com's own download endpoint started 502ing specifically for Periphery). Added 6 real Periphery songs (Icarus Lives!, Jetpacks Was Yes!, Light, Muramasa, Scarlet, Zyglrox) -- corpus grew 12 -> 18 references, rebuilt `labyrinth.json` (bpm shifted 172 -> 151 as Periphery's generally slower tempos entered the average), re-rendered and sent an updated MP3.

**Declined the MP3/audio-download half of that request.** Unlike fan-shared Guitar Pro tabs, there's no legitimate free-archive equivalent for full commercial audio -- downloading copyrighted sound recordings from the internet is a hard line regardless of stated authorization or claimed ownership (the assistant's own standing "no downloads from untrusted/unauthorized sources" rule isn't something user instruction overrides). Offered the real alternative instead: if the user hands over audio files from albums they already own directly (same pattern the whole reference-vocab feature already runs on), those can legitimately go through `analyze_audio_reference`/demucs -- not yet exercised.

Also: the file-delivery mechanism apparently didn't show the v6 MP3 on the user's end ("I can't see it") -- re-sent via `SendUserFile` and additionally saved a plain copy to the user's Desktop as a fallback the user can open directly in their own player, bypassing whatever the chat UI's file-card issue was. Kept doing this for v7.

Remaining: the GPX-format files this pyguitarpro version can't decompress (hit across all three bands now, a genuine library limitation, not a per-source issue), and the broader remaining gaps from the earlier scope re-read (P10.2 Song JSON, small unwired devices, §17.2 dense-passage flagging, Phase 8's real audio-render pipeline beyond `.rpp`).

Same day, follow-up: user directly uploaded 15 real M4A files via Claude Code's own upload mechanism -- a near-complete Born of Osiris "The Discovery" album (13 unique tracks after deduplicating 2 repeats: Follow The Signs, Singularity, Ascension, Devastate, Two Worlds Of Design, Shaping The Masterpiece, Dissimulation, Automatic Motion, The Omniscient, Last Straw, Regenerate, XIV, Behold). This is the legitimate alternative offered above, now exercised: audio from an album the user already owns, handed over directly, going through the same `analyze_audio_reference`/demucs pipeline the reference-vocab feature always supported.

Blocked initially on missing `ffmpeg` -- root-caused (neither `soundfile`/`librosa` nor `demucs`'s own internal `ffprobe`/`ffmpeg` subprocess calls can decode M4A/AAC without it) via direct Python REPL experimentation rather than guessing. Downloaded the official `ffmpeg-9.0.1-essentials_build.zip` from gyan.dev (ffmpeg.org's own linked Windows build source, 111,253,802 bytes) after stating filename/source/size and getting explicit confirmation. Ran all 13 tracks through real `demucs` 2-stem separation (`--two-stems=drums`) then `reference_vocab.add_reference` on each resulting `no_drums.wav` -- 13/13 succeeded, 0 failed, each contributing real `basic_pitch`-transcribed `riff`/`lead` tracks (not just structural stats). Corpus grew 18 -> 31 references.

Rebuilt `labyrinth.json` from the full 31-song corpus: bpm shifted 151 -> 139, scale shifted phrygian -> minor, as the larger and now audio-heavy corpus average moved (both real, honest recalculations, not tuning). Full suite re-run clean: 654 passed, 1 skipped (unchanged count -- this exercised the already-tested pipeline against new real inputs, no new code needed). Regenerated the demo song (seed 6) on the rebuilt preset, rendered via the existing fluidsynth+GeneralUser-GS pipeline, sent to the user as v8 (and copied to Desktop per the established file-delivery fallback).

Deleted the one-off `engine/_scratch_process_uploads.py` batch script after use (referenced user-local absolute upload paths, not meant to be permanent project code).

Next day, user asked directly: "have you used all data possible from them tracks." Honest answer at the time: no. Found real, already-built `audio_vocab.py` capability sitting completely unused by `reference_vocab.py`: `classify_drum_onsets` (real KICK/SNARE/HIHAT_OR_CYMBAL onset classification) was never run against the demucs `drums.wav` stem -- it was separated and then discarded, only `no_drums.wav` was ever analyzed. `guitar_rhythm_pattern` (straight/gallop/syncopated from onset timing) was never called at all. `analyze_track`'s own `section_density_curve` (real per-song dynamic arc) was computed internally then discarded, keeping only `tempo_bpm`/`tonal_descriptor`.

Reused the still-cached demucs stem output from the day before (both `drums.wav` and `no_drums.wav` per track, no need to re-run demucs) to wire all three into `analyze_audio_reference` (new optional `drums_path` param for the drum stem, new `rhythm_pattern`/`section_density_curve`/`drum_role_pct` result fields) and `add_reference`, then re-ran all 13 real audio references through the extended analysis.

User's immediate follow-up, same breath: "What about guitar notes, rhythms, leads, solos. What about synth." Investigated `song.py`'s solo-generation code path directly rather than guessing: confirmed solo AND chorus-lead sections have always reused the RIFF vocab (`preset.vocab.weights`) wholesale, generically boosted (motion+0.3, wider register), even though the corpus has held real, distinct LEAD-register interval-vocab data (from `basic_pitch` transcription's own "lead" register, and MIDI/GP's `_role_guess` heuristic) since the very first reference-vocab build -- 49 real lead-role tracks already sitting in the corpus, never consumed by anything.

Added a real, separately-calibrated `Preset.lead_vocab: Vocab | None` field (same optional-field precedent as `pedal`/`group`) -- `presets.py` gained shared `_validate_vocab_shape` validation (refactored out of the existing `vocab` check, reused for both), `load_preset` parsing, and `blend_presets` support (falls back to a preset's own `vocab` when its `lead_vocab` is `None`, so a blend with an uncalibrated preset never crashes or silently drops the field). `song.py`'s solo AND chorus-lead branches now use `preset.lead_vocab` when present, falling back to `preset.vocab` otherwise -- zero behavior change for every preset that predates this feature. `reference_vocab.build_preset_from_corpus` gained a shared `_aggregate_role_vocab` helper (refactored out of the existing riff-vocab aggregation) and now calibrates `lead_vocab` from the corpus's own real "lead"-role tracks the same way it always calibrated the main riff vocab.

Caught and fixed a real, serious pre-existing bug while wiring the new pedal-bias calibration line (a genuine "we already have this data, just weren't using it right" case, discovered because the FIRST corpus rebuild after wiring it came back with `pedal=0.0` on the real 31-song production corpus): `interval_vocab_pct.get(0, 0.0)` looked up an INT key, but a corpus loaded from disk via `load_reference_corpus` has STRING keys (a well-documented, already-tested JSON round-trip effect elsewhere in the same file) -- so it silently found nothing every time against real data, while the original synthetic int-keyed unit test happened to mask it. Fixed with a proper `int(k)` conversion (matching `_aggregate_role_vocab`'s own already-correct pattern) and added a dedicated string-keyed regression test.

Rebuilt `labyrinth.json` from the fully-extended, bug-fixed pipeline: `pedal` is now a real 0.95 (previously silently defaulting to 0.5 every time due to the bug above -- now correctly reflecting the corpus's own measured ~96% pedal-track root-heaviness), plus a real, separate `lead_vocab`. 668 passed engine-side (up from 654), 34 backend-side (unchanged). Regenerated the demo song (seed 1, 10 sections, chosen specifically to include a real solo section) and rendered v9, sent to the user with the fix explained.

Honestly flagged, not fixed, real remaining limits: `rhythm_pattern`/`section_density_curve` are now captured in the corpus but not yet consumed by any preset-calibration field (no clean, defensible mapping from straight/gallop/syncopated onto the engine's existing `breakdown`/`chug`/`bounce` feel vocabulary was found -- would need real new feel data, not a guessed mapping). Bass is NOT isolated -- the `--two-stems=drums` demucs split lumps bass into the same `no_drums.wav` signal as guitar, meaning the "riff"/"lead" transcription is really "guitar+bass combined," not a clean guitar-only signal; a full 4-stem separation would isolate a genuine bass stem the engine's own `bass.py` generator could calibrate against, not yet done. Synth is NOT separable at all with current tools -- demucs's stems are vocals/drums/bass/other, with no model that isolates synth specifically from guitar, so any synth content in the mix is invisibly blended into the guitar signal; an honest tooling ceiling, not a choice. `drum_role_pct` itself has a known, previously-documented real ceiling (X.7's own finding): the frequency-band heuristic reads real Born of Osiris mixes as heavily HIHAT_OR_CYMBAL-dominant with very few detected snares, a real limit of a hand-built classifier on a dense, cymbal-heavy mix -- captured and stored, but deliberately not yet fed into anything generation-affecting until/unless it's independently verified as trustworthy.

Same day, user pushed back directly: "Grab the riffs and sheets... What you output sounds nothing like born of Osiris why is that." Investigated the obvious suspect (dilution from mixing Periphery/Veil of Maya into the corpus) with a real test rather than a guess: a Born-of-Osiris-only preset built from the corpus's 22 real BoO-only entries came back nearly statistically identical to the full 31-song mixed corpus (root 34.2% vs 34.6%, P5 19.8% vs 17.7%, same scale, same pedal) -- ruled out.

Found the real architectural gap instead: `preset.vocab.weights`/`lead_vocab.weights` are marginal interval-FREQUENCY statistics, order-independent -- `generate_pitch_deltas` draws each interval independently every hit, zero memory of what was just played. Real Born of Osiris riffs have real note-to-note sequential tendencies none of the existing machinery captured, even though the raw ordered note data needed to capture it was available in every one of the three reference formats. Presented this and a render-pipeline-quality alternative via `AskUserQuestion`; user chose the deeper composition model, then "There's no time constraints do it properly" when an initial plan scoped `theory.VoiceLeader` out -- expanded to cover every real pitch-selection surface, not a partial fix.

Built a real first-order Markov chain over interval classes (0-11, the same state space `weights` already uses -- no new units). New shared `theory._weighted_choice` (the one real accumulate-and-compare primitive, replacing two independent duplicate implementations found in `VoiceLeader._weighted_interval` and `motif.pick_pitch_interval` during investigation) and `theory.pick_pitch_interval_markov` (real corpus-context-aware pick with an honest fallback to marginal weights for any unseen context). Wired into `VoiceLeader` (new `markov`/`last_interval`), `motif.generate_pitch_deltas`/`generate_motif`/`ThemeRegistry.get_or_create`, and `lead.generate_lead_line`/`generate_sequence_line` -- covering the rhythm-guitar riff, the solo's opening phrase, the solo's sequence passage, AND the chorus lead, the complete real set of pitch-selection sites in `song.py`.

New `Preset.Vocab.markov` field (same optional precedent as `lead_vocab`): real analysis-side capture (`reference_vocab._interval_transitions_from_sequence`, `audio_vocab.transcribe_and_split_registers` extended with a real ordered `pitch_class_sequence`, MIDI notes now explicitly sorted by onset time since order matters for a sequence statistic in a way it never did for a marginal histogram), real corpus-wide aggregation (`_pool_transition_counts` -- deliberately POOLS raw counts across every matching track rather than per-song-averaging, a documented, reasoned departure from the existing convention since a single track's own transition table is too sparse for per-song percentages to be reliable), wired into `build_preset_from_corpus` for both the main and lead vocabs.

Backfilled `interval_transition_counts` across the full 31-song corpus: reused the still-cached demucs stems for all 13 audio entries (no re-run needed); the 18 original MIDI/GP source files had been cleaned up from an earlier session's scratchpad, so re-downloaded the same 17 real Guitar Pro tabs from the same two already-used, already-authorized sources (gtptabs.com for Born of Osiris, GProTab.net for Periphery after gtptabs.com's own download endpoint 502'd for that artist again -- the same real, reproducible, previously-documented issue) plus recovered the user's original reference MIDI, still present in their own Downloads folder. All 18 parsed cleanly this time (no GPX-format failures this round). Rebuilt `labyrinth.json`: real, non-empty `vocab.markov`/`lead_vocab.markov` (e.g. after a root, the real corpus-observed split is 43.2% stay-on-root / 31.3% move-to-fifth, not an assumption). 699 passed engine-side (up from 668), 34 backend-side (unchanged). Regenerated the demo (seed 1, a real solo section) and rendered v10.

Same session, user: "Update the scales update all shortcomings you note." Reviewed `scales.py`/`theory.py` directly for real gaps rather than guessing. Found one concrete, previously-flagged, never-fixed shortcoming: `theory.py`'s own module docstring has documented since early in this project that the reference implementation's `phrygian_dominant` scale (the real "Spanish Phrygian"/exotic scale -- 5th mode of harmonic minor, a major 3rd instead of the plain phrygian's minor 3rd) "has no equivalent scale here... would need to be added to scales.py first," and it never was. Ported the real interval tuple directly from `reference/ww-forge-prior-attempt/engine/theory.py` (not invented), cross-checked independently via the real harmonic-minor-mode math in a new test. Also checked which scales any SHIPPED preset actually uses: `minor`/`phrygian`/`lydian`/`cluster` are the only four in real use; the major-family scales added under X.6d (`major`/`lydian`/`mixolydian`/`harmonic_major`) remain unadopted by any hand-authored preset -- confirmed this is NOT a new finding, X.6d's own tracker entry already documented this as a deliberate deferral ("would need careful vocab-weight redesign, not a drop-in swap"), so left it alone rather than forcing an arbitrary scale assignment onto an existing preset. 700 passed engine-side (up from 699).

Same session, user: "That sounds nothing like it... Why aren't we getting shit like that" (re: Born of Osiris' "Singularity"). Investigated with three real, data-driven tests rather than guessing further: (1) compared `labyrinth.json`'s corpus-averaged vocab directly against Singularity's own measured riff vocab -- close (root 39.8% vs 34.6%, P5 15.1% vs 17.7%), not the gap; (2) built a diagnostic preset from ONLY Singularity's own data (scale=phrygian not minor, pedal=0.5 not 0.95) and rendered it -- user: "not melodic just spam"; (3) measured real note density directly from generated output (4.12 hits/beat) against Singularity's own real measured rate (1.53 hits/beat, from `tempo_bpm`/`notes_per_sec`) -- confirmed the engine runs 2.7x denser than the reference, rendered a density-matched diagnostic -- user: "still doesn't sound good or like a born of Osiris song." All three real, well-evidenced hypotheses ruled out by ear.

User then asked directly to review the raw generated output rather than more statistical comparisons. Checked tuning (drop-G 7-string, correct), GM instrument programs (30=Distortion Guitar for guitar/lead/pedal, 33=Electric Bass, correct), pitch register (31-57, sane/playable), drum channel/note mapping (GM channel 10, standard KICK=36/SNARE=38/HIHAT=42 etc, all correct) -- all structurally sound. Found the real, previously-unchecked bug while checking instrument assignment: **zero stereo panning existed anywhere in either export path** (`midi_export.py`/`reaper_project.py`) -- every track defaulted to dead center, including FOUR simultaneous tracks (Guitar Take A, Take B, Pedal, Lead) all sharing the identical Distortion Guitar GM patch. A real double-tracked guitar pair is always panned hard L/R in an actual metal mix for width; with everything centered and identical timbre, the arrangement collapses into a mono wall of guitar mush regardless of how correct the underlying note choices are.

Fixed both export paths (the "one source of truth" pattern already established: real `_PAN_*` constants live once in `midi_export.py`, `reaper_project.py` imports and reuses them via a new shared `_pan_cc_to_reaper` conversion, so the two formats can never silently disagree on which side a guitar take sits). `midi_export._events_to_track` gained a real optional `pan` param emitting a genuine CC10 control_change; `reaper_project._track_block` gained a real `pan` param feeding its `VOLPAN` field (previously a fixed, identical template for every track regardless of name). Real spread: Take A/B panned wide L/R (not full-hard, matching real mix practice), pedal/bass/drums/pad/accents centered (low-end anchor + ambience), lead/synth offset just enough to read as distinct without colliding with either take. `engine/tests/test_midi_export.py`/`test_reaper_project.py` gained real regression tests: a real mirror-image pan-symmetry check, a Drums-stays-centered check (per-drum panning within a kit is real, separate, future scope), and a cross-format agreement test proving both exports pan Take A/B the same real direction. 705 passed engine-side (up from 700). Verified the actual rendered WAV is real stereo (not silently mono, which would have made the whole fix inaudible) and that L/R channels are no longer perfectly correlated post-fix. Rendered and sent a real A/B (same seed as the previous "spam" complaint, now with real stereo separation) -- outcome not yet confirmed by the user.

Same session, user, immediately: "Still doesn't sound good... composition is weird." Reviewed the raw generated output directly (tuning, GM programs, pitch register, drum note mapping -- all structurally correct, the panning fix above being the one real find there) and then the song's own STRUCTURE: measured directly that 8 of 10 sections in a real generated song had `lead_mode == "silent"` -- verse/build/breakdown sections carry NO independent melodic voice at all, only the rhythm riff plus a pedal-guitar and synth doubling that SAME riff. Named the honest root cause: this engine has no vocals (a permanent, deliberate project rule), and a real vocal-fronted song's own reference corpus has continuous melodic interest through every section via the singer -- this engine's only substitute, the lead guitar, was deliberately silenced 80% of the time specifically to avoid clashing with the chug, with nothing filling the gap.

User confirmed via `AskUserQuestion`: give the silent sections real melodic content. New `lead_mode = "ambient_lead"` (replaces `"silent"` everywhere in `song.py`): reuses the exact same `generate_lead_line`/`lead_vocab` machinery solo/chorus_lead already use (including real corpus Markov transitions), but deliberately SPARSE and SUSTAINED -- one note every 2 beats (half solo's dense 8th-note rate) so it reads as a real background voice riding over the chug, not a competing technical line; `stab_chance=0.0` (wide leaps stay a featured-solo device). The existing riff-doubling synth stays, additive, not replaced -- two real, distinct devices. `midi_export._lead_events_for_section` gained a real `ambient_lead` timing branch (2.0-beat spacing, each note held near the full interval as a genuine sustained tone) plus real backward compatibility: an old saved section dict with `lead_mode == "silent"` (e.g. from the editor's P9.7 section-preset save/load) still degrades cleanly to zero events rather than raising. `reaper_project.py` gets this for free (already reuses the same function). Fixed 6 existing tests whose own hardcoded `"silent"` assertions were now stale (a real, expected consequence of a real rename, not fragility), added 2 new dedicated tests (real 2.0-beat sustained-note spacing, real backward-compat). 707 passed engine-side (up from 705), 34 backend-side (unchanged). Verified live: the same seed 42 song that had 8 silent sections now has 8 real `ambient_lead` sections with genuine content. Rendered and sent a third real iteration -- outcome not yet confirmed by the user.
updated: 2026-09-11
