# TASKS

Only ## Now is active. Checked history lives in git `ac047fd` and `PROGRESS.md`.

## Now

- [ ] Labyrinth bank: pick **one** `source_song`, take a **2-4 bar** contiguous riff on the rhythm-guitar track, **tile** to the section. Same rng → same pick. Provenance on the motif (`source_song`, measure_start, track). Empty role → `None` (no Markov hide) for labyrinth. Files: `engine/riff_bank.py`, `song._try_riff_bank_motif`, `engine/tests/test_riff_bank.py`. No push unless asked.

## Later (do not start)

- Import `tools/boo-lab` `riffs.jsonl` keepers (`match=yes` + human pins).
- Drop chill/outro from the labyrinth graph **or** add fragments — no silent fallback.
- One winner for pickup / pinch twins.
- P8.2-P8.8 NAM bounce of seed 1 **after** tab view is a riff.
- Era packs after catalogue exists.
