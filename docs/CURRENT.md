# CURRENT — 2026-09-12

Generator: z2wgv7dc4h-alt/1222
Lab: boo-lab (FLAC pins + GP extract). Not this repo.

Listen: phase2_riffbank_demo.mp3 — user: sounds like shit.
Cause: role-bag bars, not one riff tiled.

Frozen: no new X devices, no /next, no train, no riff_model import.

Next (one): labyrinth bank selects one source_song, 2–4 bar riff, tiles it.
Keepers come from boo-lab `match=yes` + human pins.

Pytest: cd engine && python -m pytest -q
P8/NAM after a riff survives tab view.
