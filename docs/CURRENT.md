# CURRENT

task: synth-doubles-the-riff wiring (real user listening feedback)
phase: cross-cutting (post-Phase-9 scope reconciliation)
status: DONE
last_pytest: engine/ 627 passed, 1 skipped; editor/backend/ 34 passed
note: Direct continuation of the atmosphere/pad export fix (same day). User asked for a real generated Born-of-Osiris-style song, then gave direct listening feedback on the render: "no melody... no awesome notes... no solo... no synth... nothing." Investigated rather than assuming a mix issue -- measured the real generated song directly and found 7 of its 10 sections had ZERO melodic voice by design: `song.py`'s dense-chug sections (intro/build/breakdown/verse) were deliberately left `lead_mode="silent"` (correct instinct: an independently-composed busy lead would clash with a chugging riff) but this ALSO left them with no synth doubling, contradicting scope sec.16.1's own real Born-of-Osiris research finding ("synth leads that double or harmonize with the guitar riff... a genuine compositional voice under riffs, not just atmospheric texture").

The exact right tool already existed and was unused: `atmosphere.synth_double` (P7.2, real and tested since that session, never once called from production generation). Wired it into every dense-chug section as a real octave-up doubling of that section's own riff. Real bug caught during implementation, not by luck: the first version stored `synth_double` per-HIT and crashed with `StopIteration` at cross-section-blended boundaries (X.24's own blending pass changes a section's tail hit-pattern) -- the exact same class of bug X.28 already fixed once for guitar/bass/kick. Fixed by storing it PER-CELL (`None` on rest, identical shape to `pitches_per_cell`) and blending it via the same real `_pickup_values` call already used for `pitches_per_cell`, in both `_generate_attempt`'s main blending pass and `regenerate_section`'s own neighbor re-blend.

New "Synth" MIDI/`.rpp` track (GM 81 "Lead 2 (sawtooth)"), reusing the same `_pad_events_for_section`/`_accent_events_for_section` pattern from the earlier fix. `editor/frontend/src/components/MidiPlayer.tsx` gained a dedicated undistorted synth voice + routing for it. Verified against real generated output: the same demo song (progressive x metalcore blend, seed 7) went from melodic content in 3/10 sections to melodic content (lead OR synth-double) in all 10/10; re-rendered and sent the user an updated MP3 for direct comparison. Verified live in the running editor (zero console errors).
updated: 2026-09-10

Remaining, per the earlier scope re-read (unchanged by this fix): P10.2 (Song JSON/whole-arrangement persistence), small unwired devices (`interplay.call_and_response`, `structure.apply_half_time`, `slam.chromatic_creep`), §17.2's dense-passage tab flagging, and all of Phase 8's real audio-render pipeline (P8.2-P8.8) beyond `.rpp` generation. See TASKS.md's tracker for full detail.
