# TASKS

Work only `## Next`. One checkbox per session. Paste pytest before flipping to DONE.
P1+ : open `SCOPE-INDEX.md`, then only those headings in `god-tier-metal-scope.md`.

## Next
(X.1-X.25, X.27-X.36 done (X.26 cancelled -- investigation found the "dead code" it was going to delete was actually real and tested, see its own entry). X.34 wired real blast beats (traditional/gravity/hammer, genuine KICK/SNARE alternation) into the actual drum track for breakdown/solo sections -- a real, previously-generated-but-discarded capability, now real, occasional, and audible. X.35-X.36, from a real user-supplied Born-of-Osiris-style reference MIDI + web research on solo-writing: X.35 corrected riff-repetition bias (70% verbatim repeat is now the real default, was "vary starting on the 2nd occurrence, every time"); X.36 wired a real melodic "sequence" generator (a motif repeated at shifting scale positions -- the real core solo-writing technique) into solo sections. 596 passed, 1 skipped as of the last full-suite run. X.23 wired real power chords onto the main rhythm guitar. X.24 wired real cross-section blending (guitar/bass/guitar-locking kick). X.25 wired a real kick "burst" device. X.27 wired a real pinch-harmonic accent. X.28 fixed a REAL, SERIOUS bug: X.24's blending was silently desyncing guitar/bass/kick from snare/hihat, compounding across the whole song (found via direct user feedback that the composition itself, not just audio quality, was wrong -- confirmed correct, not a false alarm). X.29-X.31 wired the full Metalerator verse/chorus finding: real structure-graph roles, real per-bar tonal-center progression (scope sec.2/14.2 item 5, now DONE), and real verse pedal-bias/chorus power-chords/chorus lead. X.32 wired real gallop/stutter-chug rhythm devices and a real per-role feel system (breakdown always chugs like a breakdown regardless of preset; build gets real gallop/stutter-chug variety). X.33 rebalanced song pacing: measured 58.3% of every generated song's real elapsed time was half-tempo breakdown or atmospheric chill/interlude before this fix -- made half-time breakdown a real occasional coin flip (was unconditional) and rebalanced structure.DEFAULT_GRAPH to de-emphasize atmospheric detours; verified real before/after (chill+interlude time share 16.5%->6.9%, breakdown 41.8%->32.1%, verse/chorus both meaningfully more present). 581 passed, 1 skipped as of the last full-suite run.

A comprehensive audit is IN PROGRESS per explicit user request ("massive audit... make sure nothing's been rushed or skipped", plus "review the scope file and references, have we passed Metalerator's level and integrated/leveraged everything"). Confirmed real, still-open, well-understood gaps (verified each one's OWN test file this time, catching the X.26 false alarm): `interplay.call_and_response` (guitar reacting to drums/lead -- a real integration point identified: solo-section lead rhythm echoing the guitar, not yet implemented), `structure.apply_half_time` (a real rhythm-level half-time device, distinct from X.18's tempo-level one -- no confidently-correct integration point identified yet), `slam.chromatic_creep` (a real pitch-content generator, needs splicing into the degree-delta motif model). `groove.gallop_cell`/`stutter_chug_cell` and the per-ROLE feel architectural gap are both DONE (X.32). Still to do: considering any further real research; a real Reaper-driven audio render pipeline (P8) is the confirmed next real lever for judging actual audio quality, per the user's own explicit choice to defer audio and keep building the engine for now.

A user-requested full audit ("What else is missing that's never been wired in") found several more real, confirmed-via-grep gaps, NOT yet acted on -- see the new entries at the bottom of `## Scope gaps tracker` for the full list and real risk assessment. Highest-value, real candidates for the next session: `structure.pickup`/`bridge`/`flatten` (P6.2's real cross-section blending -- every section transition in every song has been a raw, unblended concatenation this whole session; genuinely higher-risk to wire since it touches the cell-level boundary across every parallel instrument track, deserves its own careful plan-mode pass, not a rushed fold-in) and `groove.gallop_cell`/`stutter_chug_cell` (real, named classic-metal rhythm devices with no current preset/role slot to cleanly land in yet). Also real and open: `interplay.call_and_response`, `structure.apply_half_time` (a rhythm-level, not tempo-level, half-time device -- distinct from X.18), `slam.mark_pinch_harmonics`/`chromatic_creep` (kept as "genre-agnostic" when the slam preset was removed, never wired anywhere else either). `structure.generate_section_content`/`generate_song_sections` are real and tested (reused by `test_atmosphere.py` as a fixture helper too), just superseded for production use by `song.py`'s own `_generate_attempt` -- CORRECTED 2026-09-09, the original "dead code" claim was wrong (see the tracker entry below); not a deletion candidate.

Other real, not-yet-built remaining work: the reference's richer 4-tier accent-aware velocity table (X.21 shipped a simplified 2-tier version), scope sec.14.2 item 5 (key/tonal-center modulation -- DONE, X.30), the deeper RHYTHM-level repetition question X.14 left open, P8.3 FX-chain tone presets (needs the user's own hands in Reaper's GUI once), P10.2 Song JSON, P10.3 further preset calibration, a real "tapping" lead-guitar technique. Phase 9 editor UI not started -- see PROGRESS.md for the full real findings/decisions log.)

---

## Scope gaps tracker
Cross-checked against `god-tier-metal-scope.md`'s OWN numbered "still open" lists (§14.2, §16, §17), not just session-by-session listening feedback -- built after a full scope re-read (2026-09-08) found two real gaps (§14.2 items 4/5) that had never been promoted into `## Next`'s prose despite sitting in the scope doc since day one. Re-run this reconciliation per `SCOPE-INDEX.md`'s trigger (after a batch of X.* items lands, or whenever `## Next` runs dry) rather than waiting for another ad hoc full re-read.

**§14.2 (scope gaps needing decisions), items 1-13:**
1. Vocals -- DONE (RESOLVED: out of scope, non-goal).
2. Bass own fretboard -- DONE (P5.1).
3. Lead/solo generation algorithm -- DONE (P3.7 VoiceLeader, X.6a legato).
4. Mid-section tempo/feel drops ("blasting, then half-times for 2 bars as a slam moment") -- DONE (X.18).
5. Key/tonal-center modulation mechanics -- DONE (X.30). Real per-bar tonal-center progression for verse/chorus, ported from Metalerator's own real `CHORD_PROGRESSIONS` tables (`engine/progression.py`).
6. Humanization (micro-timing + dynamic arc) -- PARTIAL: dynamic arc DONE (`theory.arc()`, density=0.28+0.62*energy); per-cell `timing_offset` exists and is consumed by `midi_export._cell_events`, but hasn't been audited as a deliberate, broad humanization pass -- worth confirming, not yet done.
7. Mixing intelligence (adaptive mixing) -- OPEN. Needs a render-time automation layer; punted along with P8.1's pivot to static `.rpp` (no live Reaper connection to script against yet).
8. Theory validator / automated test harness -- DONE in spirit: the pytest suite itself (scale-membership, fretboard-reachability, rhythm-sum checks) is this, not a separate module.
9. Evaluation/QA methodology -- PARTIAL: this session's whole listening-feedback-driven iteration loop IS this in practice; no written rubric exists.
10. Project file format (Song JSON) -- OPEN (P10.2). **Higher priority now than when this line was first written**: Phase 9's real editor exists and produces genuine per-session state (`TimelineBlock[]` -- order, mutes/solos, per-block `RegenEdit`s) with real undo, but `App.tsx` persists NONE of it -- a page refresh loses the entire arrangement, and P9.7's section-preset save/load only covers a SINGLE section's edit, not a whole song's arrangement (preset_id/seed/order/edits together). No way to save "this specific arranged song" and come back to it later.
11. Export matrix -- PARTIAL: MIDI stems DONE (P10.1), Reaper project DONE (P8.1); full mix audio, individual stems, tab/notation PDF still OPEN (need P8.3+ FX chains, P9.6 VexFlow).
12. Reference-corpus analysis -- DONE as a capability (`audio_vocab.py`, `midi_vocab.py`), ONGOING as a practice (X.7/X.10/X.15/X.16/X.17 all used it against real reference material).
13. ML/GPU stretch track -- OPEN, explicitly deferred (not v1, revisit post-editor).

**§16 (atmospheric/orchestral expansion):**
- Placeholder GM pad/orch-hit + synth-doubles-motif -- DONE (P7.1-P7.3 generation; export wiring DONE 2026-09-10, see below).
- Real orchestral bed (strings/brass/choir via Spitfire/sfizz) -- OPEN, blocked on an external sample-library install (same category as P8.3 -- needs the user's hands once, not automatable blind).
- Synth harmonizing/doubling the guitar motif at a fixed interval (not just unison) -- **DONE (2026-09-10).** `atmosphere.synth_double` (P7.2, real and tested since that session) was never actually CALLED from `song.py` until a direct user listening complaint ("no melody... no synth... nothing") traced to 7/10 sections of a real generated song having zero melodic voice by design (dense-chug sections were deliberately left `lead_mode="silent"` to avoid an independently-composed lead clashing with the riff -- correct instinct, but left no synth doubling either, contradicting scope's own explicit research finding that Born of Osiris keeps the synth "a genuine compositional voice under riffs... not just intro-only ambience"). Now wired: every dense-chug section gets a real octave-up doubling of its OWN riff on a synth voice (`_SYNTH_DOUBLE_TRANSPOSE = 12`), stored PER-CELL (`section["synth_double"]`, same shape as `pitches_per_cell`) so X.24's own cross-section blending keeps it index-aligned -- the first implementation attempt stored it per-HIT and crashed (`StopIteration`) at blended section boundaries, the exact same class of bug X.28 already fixed once for guitar/bass/kick; fixed by matching the established `pitches_per_cell`/`_pickup_values` pattern instead of inventing a new one. New "Synth" MIDI/`.rpp` track (GM 81 "Lead 2 (sawtooth)"), `MidiPlayer.tsx` gained a dedicated undistorted synth voice + routing. Verified against real generated output: the same demo song that had 3/10 sections with any melody now has melodic content (solo/chorus lead OR synth-double) in all 10. 627 passed engine-side (up from 620), 34 backend-side (unchanged).

**§17 (further gaps):**
- 17.1 Double-tracked guitars -- DONE (P3.8). **EXTENDED (2026-09-10):** a real, GENUINELY SEPARATE second guitar part (`section["guitar_pedal"]`), distinct from double-tracking. Found by directly parsing a real user-supplied Born-of-Osiris-style reference MIDI (Krumhansl-Schmuckler key correlation + a real per-track interval-vocabulary histogram, reusing `audio_vocab.estimate_key`'s own technique against MIDI pitch data instead of audio chroma) rather than guessing at genre convention: its own "Guitar 1" (49.8% root, real P5/m3/m6 color, spans 44 semitones) is the wide melodic riff; "Guitar 2" (95.9% root, tight low register) is a genuinely different musical idea, not the same riff double-tracked. New `song._pedal_double_pitches`/`_PEDAL_DOUBLE_WEIGHTS` (a heavily root-biased vocab matching the real measured percentages, drawn via the exact same `generate_pitch_deltas` mechanism the main guitar's own pitch layer uses), locked to the main riff's own rhythm (a documented simplification -- a real "low string doubler" commonly follows the main riff's picking rhythm in production), single-take humanized (not double-tracked -- a tight discrete layer, not part of the wide-stereo pair). Skipped for chill/interlude (same silence contract as hihat/pinch-harmonic). Blended across section boundaries via the same real `_pickup_cells`/`_pickup_values` pair already blending `guitar_take_a`/`pitches_per_cell`, in both `_generate_attempt`'s main pass and `regenerate_section`'s neighbor re-blend. Bass/kick locking, judge scoring, and the editor's tab/stat display all deliberately UNCHANGED (still key off `guitar_take_a` only) -- a genuinely additive track, not a replacement for any existing relationship. New "Guitar (Pedal)" MIDI/`.rpp` track (channel 7/8 respectively, reusing `_GUITAR_PROGRAM`). Caught and fixed a real, pre-existing test fragility this change exposed (not a bug in the new code): `test_guitar_locked_kick_style_is_re_locked_after_blending` had no exclusion for X.34's blast-fill overriding a "breakdown" section's kick/snare regardless of kick_style -- the new rng draws shifted which section happened to roll a blast for seed 6, exposing a gap the test's own exclusion list never covered; fixed using the same real "blast fired" heuristic (elevated snare-hit count) already established elsewhere in the same test file. 637 passed engine-side (up from 627), 34 backend-side (unchanged). Verified against real generated output (every section shows real, root-heavy pedal-guitar hit content) and live in the running editor (all 9 real tracks arrive over `/api/export-midi`, zero console errors on playback).
- 17.2 Human playability -- RESOLVED (Option B: accept programmed precision as genre-normal); "flag dense passages in the tab view" -- **STILL OPEN.** P9.6 shipped the real tab view itself (`TabView.tsx`, real VexFlow rendering) but did NOT implement the specific "flag extremely dense passages" sub-requirement scope calls for -- no notes-per-second threshold or visual marker exists in `TabView.tsx` today. A small, real, well-scoped follow-up on top of already-shipped infrastructure.
- 17.3 Reproducibility/seed -- DONE (P2.8, enforced throughout).
- 17.4 Trademark/naming -- DONE (mood-name presets adopted; band names kept only as `ALIASES`/docs).
- 17.5 Reaper tempo-map for polymeter -- DONE (Option A chosen and implemented: real absolute-second `TEMPOENVEX` positions, not a human-legible grid -- matches P8.1's actual output).
- 17.6 Preset calibration workflow -- **DONE (2026-09-10), a real standalone tool at last.** User: "it needs me to be able to feed you songs and you learn from that." New `engine/reference_vocab.py`: real, format-agnostic reference-song analysis (`analyze_midi_reference`/`analyze_gp_reference`/`analyze_audio_reference` -- MIDI via `pretty_midi`, Guitar Pro `.gp*` via `pyguitarpro` confirmed already installed, audio via `librosa`/`audio_vocab.py` with a real note-level vocabulary too when `basic_pitch` -- also already installed -- can transcribe it, honestly flagged `structural_only` when it can't). Real Krumhansl-Schmuckler key correlation reused directly from `audio_vocab.py`'s own profile arrays (not duplicated), applied to a symbolic pitch-class histogram instead of audio chroma. `add_reference`/`load_reference_corpus`: a real, accumulating local corpus cache (`engine/data/reference_corpus.json`, committed to git -- derived statistics only, same real precedent as the already-committed `midi_vocab.json`, never the source songs' actual notes). `build_preset_from_corpus`: averages the real per-song `interval_vocab_pct` (one song = one vote, never letting one dense reference dominate), picks the best-fitting real scale via a new `_nearest_scale` heuristic, writes a real preset JSON file into `engine/presets/` via the exact schema `presets.validate_preset` already enforces -- automatically discoverable via `load_all_presets`'s existing glob. Fed the corpus 18 real references (grown from an initial 12 the same day -- user: "Get more data... grab MP3's to demucs, tabs, midis, everything"): the user's own previously-supplied reference MIDI, plus 17 real, freely-downloaded Guitar Pro tabs (8 Born of Osiris + 6 Periphery + 3 Veil of Maya, from gtptabs.com and GProTab.net's free archives -- user explicitly authorized looking up tab/MIDI files online for compositional-style research, non-commercial, never reproduced). **Declined the MP3/full-audio-download part of that same request** -- unlike fan-shared tabs, there's no legitimate free-archive equivalent for full commercial audio; downloading copyrighted sound recordings from the internet stays a hard line regardless of stated authorization or claimed album ownership (matches the assistant's own standing "no downloads from untrusted/unauthorized sources" rule). Real, offered alternative: if the user hands over audio files from albums they already own (the same "user supplies a file directly" pattern the whole reference-analysis feature already runs on), those can go through `analyze_audio_reference`/demucs legitimately -- not yet exercised, no such file supplied yet. Built a real, corpus-calibrated preset (`labyrinth.json` -- drop-G 7-string per scope's own §18.2 real BoO tuning citation, phrygian, 151bpm after the corpus grew, real averaged interval vocab) and updated the stale `"boo": "djent"` alias (a leftover from the ported project, never actually calibrated against Born of Osiris) to point at it, same real precedent as the existing `"periphery": "progressive"` alias update. `engine/tests/test_reference_vocab.py` (12 new tests, synthetic self-constructed MIDI/GP/audio fixtures only -- mirrors `test_audio_vocab.py`'s own documented strategy, never a real copyrighted file). Caught and fixed two real bugs during implementation: `pm.estimate_tempo()` raising on a too-sparse MIDI file (now a real, honest `None` fallback, not a crash) and numpy scalar types leaking into JSON-serialized results (now explicit `float()`/`int()` casts throughout). 654 passed engine-side (up from 637), 34 backend-side (unchanged). Verified against real generated output: regenerated the demo song on the new corpus-calibrated preset and rendered it via the existing fluidsynth+GeneralUser-GS pipeline.

  **Real audio extension (2026-09-10, same day):** user directly uploaded 15 real M4A files via Claude Code's own first-party upload mechanism (near-complete Born of Osiris "The Discovery" album, 13 unique tracks after deduplicating 2 repeats) -- the legitimate alternative to the earlier-declined MP3-download request, since a user handing over audio from an album they already own is the same "user supplies a file directly" pattern the whole reference-analysis feature already runs on. Blocked initially on missing `ffmpeg` (neither `soundfile`/`librosa` nor `demucs`'s own `ffprobe`/`ffmpeg` subprocess calls can read M4A/AAC without it) -- root-caused via direct Python REPL experimentation, then downloaded the official `ffmpeg-9.0.1-essentials_build.zip` from gyan.dev (ffmpeg.org's own linked Windows build source, 111,253,802 bytes, stated filename/source/size and confirmed with the user first). Ran all 13 tracks through real `demucs` 2-stem separation (`--two-stems=drums`, isolating guitar/bass into `no_drums.wav` per `audio_vocab.py`'s own documented recommendation) then `reference_vocab.add_reference` -- all 13 succeeded (0 failed), each contributing real transcribed `riff`/`lead` role tracks via `basic_pitch`. Corpus grew 18 -> 31 references. Rebuilt `labyrinth.json` from the full 31-song corpus (bpm shifted 151 -> 139, scale shifted phrygian -> minor as the larger, now audio-heavy corpus average moved). 654 passed engine-side (unchanged count, no new tests needed -- this exercised the already-tested `add_reference`/`build_preset_from_corpus` pipeline against new real inputs, not new code). Verified against real generated output: regenerated the demo song (seed 6) on the newly-rebuilt preset, rendered via the existing fluidsynth+GeneralUser-GS pipeline, sent to the user as v8.

  **"Have you used all data possible" (2026-09-11):** direct user question found real gaps. `audio_vocab.classify_drum_onsets` (KICK/SNARE/HIHAT_OR_CYMBAL classification) had never been run against the demucs `drums.wav` stem -- separated, then discarded, only `no_drums.wav` was ever analyzed. `guitar_rhythm_pattern` (straight/gallop/syncopated) was never called at all. `analyze_track`'s own `section_density_curve` was computed internally then discarded. Wired all three into `analyze_audio_reference` (new optional `drums_path` param, new `rhythm_pattern`/`section_density_curve`/`drum_role_pct` fields) and `add_reference`; re-ran all 13 audio references using the STILL-CACHED demucs stems from the day before (no need to re-run demucs). User's immediate follow-up: "What about guitar notes, rhythms, leads, solos. What about synth" -- traced `song.py`'s solo/chorus-lead generation directly: both had ALWAYS reused the RIFF vocab wholesale, even though 49 real "lead"-role tracks (basic_pitch's transcribed lead register + MIDI/GP's `_role_guess`) had been sitting unused in the corpus since the very first build. Added a real `Preset.lead_vocab: Vocab | None` field (same optional precedent as `pedal`/`group`; `presets.py` gained a shared `_validate_vocab_shape` helper, `load_preset` parsing, `blend_presets` support with a real riff-vocab fallback), wired into `song.py`'s solo AND chorus_lead branches (falls back to `vocab` for any preset without real lead-register calibration -- zero behavior change for existing presets), and `reference_vocab.build_preset_from_corpus` now calibrates it from the corpus's own real "lead" tracks via a new shared `_aggregate_role_vocab` helper. Caught and fixed a real, serious pre-existing bug surfaced by the FIRST rebuild after wiring pedal-bias calibration in: `interval_vocab_pct.get(0, 0.0)` used an int key against a corpus that (once loaded from disk) has real JSON STRING keys, so it silently found nothing on every real corpus entry (`pedal` came back `0.0` instead of the real ~0.95) -- the original synthetic int-keyed unit test happened to mask it; fixed with proper `int(k)` conversion, real string-keyed regression test added. Rebuilt `labyrinth.json`: `pedal` now correctly 0.95 (was silently 0.5 this whole time), real separate `lead_vocab`. 668 passed engine-side (up from 654), 34 backend-side (unchanged). Regenerated (seed 1, a real solo section) and rendered v9.

  Same breath, user: "Grab the riffs and sheets... You're still not incorporating it properly. What you output sounds nothing like born of Osiris why is that." Investigated rather than just re-fetching more files: the corpus already holds 22 real Born of Osiris entries (8 Guitar Pro tabs + 13 audio + 1 user MIDI) out of 31 total -- built a real, direct BoO-only preset from just those 22 and compared its vocab against the full mixed-artist corpus. Real finding: nearly identical (root 34.2% vs 34.6%, P5 19.8% vs 17.7%, same scale, same pedal bias) -- artist dilution is NOT the bottleneck, ruling that out with a real test rather than a guess. The two real remaining candidates, both substantial, presented to the user rather than silently picked: (1) the render pipeline itself (GM soundfont via fluidsynth, no real distorted-guitar amp sim or drum samples or mix -- P8.2-P8.8 real audio render, still MISSING per STATUS.md, likely the single biggest PERCEPTUAL gap no amount of note-choice calibration touches), and (2) the composition model's own real ceiling -- `preset.vocab`/`lead_vocab` are marginal INTERVAL-FREQUENCY statistics (a bag-of-intervals), never actual riff/motif SEQUENCE patterns mined from the real corpus, so even a faithful vocab still drives a generic scale-degree random walk rather than real BoO-shaped phrasing.

  **User chose (2) via AskUserQuestion, then "There's no time constraints do it properly" (2026-09-11) -- a real, corpus-derived first-order Markov riff-pattern model, DONE across every real pitch-selection surface, not a partial fix.** Investigated the exact existing mechanism first (empirically, not guessed): `generate_pitch_deltas` draws a fresh, independent interval pick every hit from a static marginal distribution, zero memory of the previous pick. Found `motif.pick_pitch_interval` and `theory.VoiceLeader._weighted_interval` were two independent, duplicate implementations of the identical weighted-pick algorithm (confirmed by reading both) -- real, worthwhile DRY consolidation done as a byproduct: new `theory._weighted_choice` (the one real accumulate-and-compare loop) and `theory.pick_pitch_interval_markov` (real corpus-context-aware pick, honest fallback to marginal weights for any unseen context), both reused by `VoiceLeader` and `motif.py` instead of each keeping its own copy.

  Real first-order Markov chain over interval classes (0-11, the SAME state space `weights` already uses -- no new units, no change to `degree_delta_for_interval`'s contract). `VoiceLeader` gained `markov`/`last_interval`; `motif.generate_pitch_deltas`/`generate_motif`/`ThemeRegistry.get_or_create` gained `markov`; `lead.generate_lead_line`/`generate_sequence_line` gained `markov`. `song.py` wired `preset.vocab.markov`/`lead_vocab.markov` into every real pitch-selection call site: the main riff (`_generate_one_section`'s `theme_source` call and `_regenerate_pitch_only`'s direct `generate_pitch_deltas` call), the solo's opening `generate_lead_line` phrase, the solo's `generate_sequence_line` passage, and the chorus-lead's `generate_lead_line` phrase -- the complete real set. `octave_stab`'s `VoiceLeader.stab()` and the pedal-guitar's own `_pedal_double_pitches` confirmed structurally unreachable (neither goes through `_weighted_interval`/uses corpus-calibrated weights) -- correctly, deliberately unchanged.

  New `Preset.Vocab.markov: dict[int, dict[int, float]] | None` field, same optional-field-with-real-fallback precedent as `pedal`/`lead_vocab`. `presets.py`: `_validate_vocab_shape` extended (shared `_validate_interval_weight_map` bounds check, reused for `weights` AND every markov row -- one place, not three), `_parse_vocab`/`_serialize_vocab` helpers (shared JSON<->Vocab conversion for both `vocab` and `lead_vocab`), `blend_presets` blends `vocab.markov`/`lead_vocab.markov` (real cell-union blend, `None` only when NEITHER side has real data).

  Real analysis-side capture (`reference_vocab.py`): new `_interval_transitions_from_sequence` (real bigram transition COUNTS -- deliberately raw counts, not per-song percentages, since a single track's own transition table is too sparse for reliable per-song percentages; pooled correctly at aggregation time instead, a documented, reasoned departure from `interval_vocab_pct`'s own "one vote per song" convention). `analyze_midi_reference` now explicitly sorts notes by onset time before building `pitches` (didn't matter for the old marginal histogram, matters now for a real sequence statistic) -- `analyze_gp_reference`'s own measure/voice/beat iteration was already naturally chronological, no fix needed. `audio_vocab.transcribe_and_split_registers` extended with a real ordered `pitch_class_sequence` (previously only returned order-independent `pitch_class_counts`). New `_pool_transition_counts` (real corpus-wide raw-count pooling, normalized to percentages only at the very end) and `_serialize_vocab`, wired into `build_preset_from_corpus` for both the main riff and lead vocabs.

  Backfilled `interval_transition_counts` across the full 31-song corpus. Audio (13): reused the still-cached demucs stem output from the day before -- no re-run needed. MIDI/GP (18): the original source files had been cleaned up from an earlier session's scratchpad -- re-downloaded the exact same 17 real Guitar Pro tabs from the same two already-used sources (gtptabs.com for the 8 Born of Osiris + 3 Veil of Maya, GProTab.net for the 6 Periphery after gtptabs.com's own download endpoint 502'd for that artist again -- the same real, reproducible issue documented earlier this session, not new), plus recovered the user's original reference MIDI still sitting in their own Downloads folder. All 18 re-downloads parsed cleanly this time (no GPX-format failures this round, unlike some earlier attempts this session). Rebuilt `labyrinth.json`: real, non-empty `vocab.markov`/`lead_vocab.markov` (e.g. the real corpus-observed row for "after a root": 43.2% stay-on-root, 31.3% move-to-fifth, the rest spread realistically thin).

  `engine/tests/test_theory.py`/`test_motif.py`/`test_lead.py`/`test_presets.py`/`test_reference_vocab.py`/`test_song.py` all gained real markov-specific coverage: fallback-chain tests, deliberately extreme synthetic transition tables proving measurable real effect, `markov=None` byte-identical regression tests (including one run across EVERY shipped preset, not just djent), a deterministic-alternating-pattern fixture proving `_interval_transitions_from_sequence` captures real sequential structure a marginal histogram cannot, and a real JSON-string-key regression test for the exact class of bug the pedal-bias fix caught earlier. 699 passed engine-side (up from 668), 34 backend-side (unchanged). Regenerated the demo song (seed 1, a real solo section) and rendered v10, sent to the user with the architecture change explained plainly.

  **"Fix rytyhm" (2026-09-11):** same session, after the solo register/leap recalibration above (from the Singularity intro-hook transcription), user asked for the same real, evidence-based treatment applied to rhythm-guitar TIMING. Direct `basic_pitch` note extraction from Singularity's own rhythm register was unreliable (heavy polyphony/overlap in the mixed guitar+bass "no_drums" stem) -- pivoted to `audio_vocab.guitar_rhythm_pattern`'s existing pitch-INDEPENDENT onset-timing classification instead, and to real inter-onset-interval (IOI) measurement. Found `rhythm_pattern="syncopated"` for the whole song and its verse specifically; real median IOI 0.3077 beats (most common rounded bucket 0.3125 beats, 54 occurrences) -- a close match for genuine triplet-eighth subdivision (0.333 beats, `rhythm.tuplet_grid(3,2,1.0)`), NOT a match for the engine's own `_ALLOWED_LENGTHS=[0.25,0.5,1.0]` power-of-2 grid. Checked corpus-wide before touching code: **all 13 of 13 real Born of Osiris audio entries measure `rhythm_pattern="syncopated"`** -- unanimous. Confirmed via direct code read that `reference_vocab.build_preset_from_corpus` hardcoded `feel="bounce"` unconditionally, explaining why `labyrinth.json` never reflected this.

  Fixed: `build_preset_from_corpus` now reads the corpus's own per-song `rhythm_pattern` field (present on audio-analyzed entries) via `collections.Counter`'s majority vote, mapped to a real, already-wired engine `feel`: majority "syncopated" -> `feel="triplet"` (the only existing device -- `rhythm.generate_triplet_rhythm`, already consumed by real generation in `motif.py`'s `_generate_irvd_motif` -- that can express non-power-of-2 timing), majority "gallop" -> `feel="gallop"` (direct name match to `groove.gallop_cell`), no real rhythm-pattern data (pure MIDI/GP corpus) -> falls back to the original "bounce" default rather than inventing a mapping. Three new regression tests cover all three branches. Rebuilt `labyrinth.json`: `feel` is now `"triplet"` (previously always "bounce", never actually calibrated). 713 passed engine-side (up from 710), 34 backend-side (unchanged) -- no seed bumps needed, a genuinely new code path rather than an rng-stream shift. Regenerated and rendered v18, sent to the user.

  **Real kick-pattern Markov model, mirroring the guitar riff Markov work (2026-09-11):** user pointed at four local MIDI folders (`C:\Forge\user\midi\{jj_dreaming,jj_lakeside,jj_lamb,whack_breakdown}`): "cant we use these?" Investigated rather than assuming new data: `midi_vocab.py`'s own docstring already names this exact corpus (JJDoge's groove packs + Whack Studio's "Breakdown Essentials"), already fully integrated -- verified matching per-pack file counts (658/681/524/104) already in the real, already-built `engine/data/midi_vocab.json` (1967 files, 0 skipped), already wired into `drums.generate_vocabulary_informed_blast_fill`. Honest gap: that pipeline (P4.4) was deliberately DENSITY-only, never real sequential PATTERN structure. User chose, via `AskUserQuestion`, to extend it the same way as guitar riffs.

  Measured real evidence first: only MIDI note 36 (KICK)/38 (SNARE) are trustworthy across an arbitrary foreign pack's own kit convention (this project's OWN kit already remaps other notes -- `drums.py`'s own docstring). Measured real kick (note 36) IOIs across all 104 `whack_breakdown` files specifically (genre-exact, unlike the three generic JJ groove packs): 4446 real gaps, median 0.281 beats, dominant clusters at 0.25/0.5/1.0/0.75 plus a real triplet-family cluster (~0.167/0.333) -- consistent with the "Fix rytyhm" finding above.

  Built a real, kick-only pattern-mining pipeline (snare/hihat deliberately out of scope -- too sparse/kit-risky for a first pass). `midi_vocab.extract_file_stats` gained `kick_ioi_beats` (real, per-track chronological note-36 onset gaps). New `_KICK_IOI_BUCKETS`/`_snap_to_bucket`/`_ioi_transitions_from_sequence` (real bigram counts, mirroring `reference_vocab._interval_transitions_from_sequence`) and `_pool_kick_ioi_markov` (pools only the genre-specific `whack_breakdown` pack). `aggregate_vocabulary`/the cached JSON gained `kick_ioi_markov`.

  New `drums._kick_corpus_walk`: a real first-order Markov walk over kick-to-kick IOI buckets, marking guitar cells hit via the existing `_cell_starts`/`_time_to_cell_index` mechanism. Wired as a new `"corpus"` kick style (special-cased in `kick_pattern_for_style` since it's the one real RANDOMIZED style; every other style stays exactly deterministic). Fails closed (`ValueError`) on missing cache/data/rng. `labyrinth.json`'s `kick`: `"euclid"` -> `"corpus"`.

  Caught and fixed a real structural incompatibility: `song.py`'s cross-section-blend kick re-lock (X.24) previously re-called `kick_pattern_for_style` uniformly on the assumption every style is deterministic and "consumes no additional rng draws" -- broken by the new genuinely-randomized "corpus" style. Fixed by excluding "corpus" from that re-lock (its hits come purely from cell durations, unaffected by blending; re-locking was both unnecessary and would burn an extra rng draw for a different walk). 16 new regression tests across `test_midi_vocab.py`/`test_drums.py` (bad-input `ValueError`s, a hand-checkable deterministic single-bucket case, an extreme-table measurable-density-difference case). 729 passed engine-side (up from 713), 34 backend-side (unchanged), no seed bumps needed. Verified live: a generated verse section's real kick IOIs show the corpus's own dominant 0.333-beat bucket as the single most common value (19 of 47 hits).

  **Real, local, trained riff-generation model for Born of Osiris ONLY, v1+v2 (2026-09-11/12):** user, after 8 real composition fixes, "all of them are bad" (riff content, phrase structure, arrangement) -- a real ceiling signal, not one more bug. Diagnosed: `vocab.markov` (first-order Markov) has no memory beyond one step, structurally incapable of real multi-bar phrase planning -- matches this session's own earlier ProgGP/Metalerator comparison. User chose a real trained model over template libraries/deeper statistics, then: "focus on ONE genre... ensure we can at least do one style." Verified CUDA end-to-end (`torch==2.11.0+cu128`, real GPU matmul). User supplied six real FLAC album rips (own CDs, ~71 real songs after splitting two whole-album CUE rips via their own real timestamps).

  New `riff_corpus.py` (real 135-token interval-class+duration-bucket vocabulary, LOCAL-ONLY training data -- a deliberate, documented departure from "never persist full sequences," since a sequence model genuinely needs them) and `riff_model.py` (small decoder-only Transformer, CUDA, real train/val split holding out full songs, a real anti-memorization n-gram-overlap safeguard, verified real determinism). Wired into `song.py` as a strictly additive path (`_try_riff_model_motif`): `None`/unchanged for every preset but `labyrinth`, and for `labyrinth` itself with no local checkpoint -- zero behavior change for anyone not opted in, confirmed via the full suite.

  User's real feedback on the v1 demo: "sounds like absolute nonsense... didn't sound like a well put together song." Root-caused two real, distinct problems: (1) riff incoherence -- real perplexity ~44/135, traced to ~47.5K training tokens being small for a from-scratch Transformer; researched real precedent (ProgGP pretrains on DadaGP's 26,181 songs) but DadaGP needs gated author access -- explicitly deferred, not attempted blind. (2) section differentiation lost -- `_try_riff_model_motif` had zero role awareness, silently dropping `_VERSE_PEDAL_BIAS` (the model has no weights dict to reweight); the real per-bar chord-progression device was confirmed NOT broken (`render_motif_with_progression` still consumes `motif.deltas`).

  **v2 fix, no new external data:** restored verse pedal-bias via a real post-hoc pull (`apply_pedal_bias_to_motif`), plus real section-role ENERGY conditioning (`audio_vocab.energy_curve`, chosen over unverifiable semantic labels -- checked a real candidate, `allin1`, blocked on a missing C++ build toolchain, flagged as a future path not silently installed). New `ENERGY_LOW/_MID/_HIGH` tokens at real sparse bucket transitions; `theory.ARC`'s own per-role energy field bridges training/generation via two independently-tuned real mappings (confirmed a DIFFERENT scale than `energy_curve`'s own). Rebuilt the full corpus (70/71 songs, cached demucs stems reused), retrained (best val loss 3.781, matching v1's 3.79 -- no regression). Verified real, measurable differentiation: LOW-energy roles 13 hits/1.23 beats-per-hit, MID 15 hits/1.07, HIGH 31 hits/0.52 -- over 2x density; confirmed in an actual composed song (breakdown 124 real hits vs. far sparser other sections).

  Caught and fixed a real environment regression along the way: exploratory `pip install madmom`/`allin1` attempts pulled in `tensorflow==2.21.0`, breaking `basic_pitch`'s ONNX fallback (4 real test failures) -- untangled via careful dependency work (confirmed basic_pitch's own documented posture needs TF cleanly ABSENT, not present-but-broken, for its ONNX fallback to resolve correctly). 769 passed engine-side (up from 729), 34 backend-side (unchanged). Rendered and sent v21, outcome not yet confirmed.

  **Real CORE pitch-generation fix, found via a real audit (2026-09-12):** user on v21: "sounds like absolute nonsense did you even analyse it." Measured: model's raw deltas for a real breakdown section were smooth (0% reversal), but final rendered pitches showed 67% reversal -- root-caused to `generate_riff_motif`'s `degree_index` walking fully unbounded (climbed to degree 32, 11+ real octaves above anchor) with the existing fretboard `_snap_to_playable_octave` yanking it back down repeatedly. First fix attempt (silent reset to anchor) measured as NOT working -- `Motif.deltas` is a continuous walk `render_motif` naively cumsums downstream, so a silent reset breaks that invariant even though internal bookkeeping looked bounded. Fixed properly with real REFLECTION (same technique `VoiceLeader.walk()` already uses): new `motif.degree_delta_for_interval_downward`, real persisted direction + register-bound reflection in `generate_riff_motif`. Verified: span 10->8 semitones, reversal 67%->46%.

  User pushed further: "why are we taking shortcuts... what else have you missed... youve done so many things wrong." Real audit (not a second narrow patch) found the REAL biggest lever, unrelated to the ML work: A/B test (same seed-1 song, checkpoint hidden, forcing the OLD Markov path everywhere) measured 97% reversal in verse, 50%+ in three others -- as bad as the ML model. Predates the riff-model work entirely; only the SOLO ever got the real walk-momentum fix. Also found and written up for later (not bundled in, per the user's own correction to slow down): `ThemeRegistry` bypass (no real thematic reuse across repeated role occurrences), `motif.transpose()`'s real "shift every delta" behavior (confirmed intentional and fine for short motifs, unsafe for the model's 100+-note continuous runs), real vocal/bass contamination in training data (`demucs --two-stems=drums` mixes everything but drums -- `htdemucs_6s`, a real installed 6-stem model, is the fix), 3 real duplicate songs (album + demo mix) causing train/val leakage, and confirmed DadaGP/ProgGP are BOTH gated (no free shortcut for more pretraining data).

  User: "i like your thinking... step back and think further... weve gone too deep." Narrowed to ONE real, foundational fix: ported `VoiceLeader.walk()`'s real technique into `generate_pitch_deltas` itself (`motif.py`) -- `degree_delta_for_interval_downward` moved to `motif.py` as a real shared primitive (both this and `riff_model.generate_riff_motif` now import it, no more duplicate copy), real persisted direction + register-bound reflection (`_PITCH_REGISTER_SPAN_SEMITONES=12`). Reaches EVERY preset's main riff, not just labyrinth. 770 passed (up from 769) -- zero regressions, since the fix draws no new rng and only changes output when a real walk actually exceeds the bound (rare for short existing test fixtures). Re-measured: register properly bounded now; raw reversal-rate alone turned out to be an imperfect quality proxy (some reversal is normal melodic contour). Rendered and sent an ISOLATED demo (deathcore preset, no ML model involved) -- stopping here per the plan's own explicit instruction, reporting back before touching anything else from the audit..
- 17.7 Repo structure -- DONE (real layout + `.gitignore` policy).

**Real, previously-unchecked mix bug found + fixed (2026-09-11), not in the scope doc's own numbered lists:** direct user listening complaint ("sounds nothing like Born of Osiris... just spam") led through three ruled-out compositional hypotheses (corpus dilution, single-song calibration, density -- all tested with real data, none were it) to a real structural review of the raw exported output. Found: **zero stereo panning anywhere in either export path.** Every track (`midi_export.py`/`reaper_project.py`) defaulted to dead center, including four simultaneous tracks (Guitar Take A, Take B, Pedal, Lead) all sharing the identical GM Distortion Guitar patch -- collapsing a real double-tracked-guitar arrangement into a mono wall regardless of how correct the underlying note choices were. Fixed both export paths from one real source of truth (`midi_export._PAN_*` constants + new `_pan_cc_to_reaper`, `reaper_project.py` imports and reuses rather than re-deriving): real wide L/R spread for the double-tracked pair, centered low-end anchor (bass/pedal/drums), offset lead/synth. `engine/tests/test_midi_export.py`/`test_reaper_project.py` gained real regression coverage including a cross-format agreement test. 705 passed engine-side (up from 700). Also flagged, not yet fixed: `editor/frontend/src/components/MidiPlayer.tsx`'s own Tone.js preview playback has the identical zero-panning gap -- same bug class, separate smaller follow-up, out of scope for this pass since the user's actual listening happens via the fluidsynth render, not the editor preview.

**Real structural gap found + fixed same session: 80% of a generated song had zero independent melody.** User, immediately after the panning fix: "still doesn't sound good... composition is weird." Measured directly: 8 of 10 sections in a real generated song had `lead_mode == "silent"` -- verse/build/breakdown carried no independent melodic voice at all, only the riff plus its own pedal-guitar/synth doubling (same idea, reinforced, not a second musical idea). Root cause named honestly: this engine has no vocals (permanent project rule); a real vocal-fronted reference song has continuous melodic interest through every section via the singer, and the lead guitar -- this engine's only substitute -- was deliberately silenced 80% of the time to avoid clashing with the chug, with nothing filling that gap. User confirmed via `AskUserQuestion`: fill it.

**Real bug found + fixed same session: melodic lines had no directional memory.** User: "no solo... no nice melody... maybe this is over engineered... why does ProgGP/Metalerator make good music but ours doesn't." Measured the actual solo section's own pitch data directly: 71% direction-reversal rate, 48% of notes on one repeated pitch -- not a real solo, a stuck oscillation. Traced to `theory.VoiceLeader.walk()`: its only real caller never passes `direction`, so every stepwise call independently re-rolled up/down with zero memory -- a coin flip every note, never a real run. Fixed with real directional momentum (`_walk_direction` state, `_WALK_MOMENTUM = 0.75`, a documented heuristic not a corpus-measured value): default `direction=0` now continues the established direction with real persistence; explicit `direction` still honored exactly as before; a register-edge reflection correctly updates the persisted direction. Verified on the same real solo: reversal rate 71% -> 45%, a genuine 5-note descending run now present. 3 new tests. 710 passed engine-side (up from 707). Answered the honest "why do simpler systems sound better" question directly: ProgGP is a real trained sequence model with learned phrase structure this project's Markov chain can't have; Metalerator is deliberately simple, hand-tuned devices over statistical averaging; this project accreted layers session over session without a single coherent compositional vision, and `walk()`'s bug is the concrete proof the real gap was a mechanical bug, not a complexity deficit.

**Real redundant-layering cut same session: 6 voices locked to the identical rhythm.** User: "I can't even tell it apart there's so much spam and garbage." Measured directly: one verse section had SIX voices (both guitar takes, pedal guitar, synth double, bass, kick) all firing on the IDENTICAL 120 rhythmic hits -- one rhythmic event stacked six times, not an arrangement. User confirmed via `AskUserQuestion`: cut it. Retired `atmosphere.synth_double`'s call site from `song.py` entirely (the function itself stays real/tested/reusable) -- redundant with `ambient_lead` now that it fills the same "give this section melodic interest" role with a genuinely independent line, not a locked copy. New `_thin_pedal_cells`: pedal guitar now keeps every OTHER real hit of the main guitar's rhythm (real rest cells, same overall grid so blending is unaffected) instead of copying it note-for-note. Fixed 3 tests whose own hardcoded assumptions were now legitimately stale (2 synth_double export tests now confirm the track exists but is empty; the pedal-rhythm test now asserts genuine sparser density, hits strictly a subset of the main guitar's). 2 more tests needed a real seed bump (same established rng-stream-shift pattern, not new fragility). 708 passed engine-side (down from 710 -- two obsolete synth_double tests removed outright, not renamed, since their own premise no longer exists), 34 backend-side (unchanged). Verified live: the same verse section went from 6 identical-rhythm voices to effectively 2 locked together (the intentional double-tracked pair) plus bass/kick.

**Real, PRE-EXISTING bug found + fixed same session: IRVD "Destruction" bars compound instead of capping.** User, first real forward-progress signal ("slightly better") plus a new concrete complaint: "spams drums randomly." Measured a verse section's real kick durations directly: a genuine 64th-note subdivision (0.0625 beats) appearing 12 times, clustered together. Traced to `rhythm.phrase_plan(8)` (labyrinth's own `bars=8`) returning `["I","R","R","V","V","V","D","D"]` -- TWO consecutive Destruction bars -- and `motif._generate_irvd_motif`'s own "D" branch halving `previous_bar`, which for the SECOND consecutive D is the FIRST D bar, already halved once: 0.25 -> 0.125 -> 0.0625, compounding. Kick/snare lock to these same guitar durations, so the compounding reached the drums directly. A real, pre-existing bug since X.19, just newly audible once the other layers of clutter were cut. Fixed: a second (or further) consecutive "D" now repeats the first D bar's own result verbatim rather than halving again (same "R"-for-"I" principle already used elsewhere in the same function). New regression test (`phrase_plan(8)`'s real two-D shape, confirms no further-halving, real 0.125 floor holds). 709 passed engine-side (up from 708). Verified live: the same verse section's duration distribution no longer contains any 0.0625 value.

**Real solo register/leap-rate recalibration, directly evidenced by transcribing Singularity's own real lead hook.** User: "Go back to singularity and use that as a focus song... figure out why it's not sounding as awesome as that." Went past aggregate corpus statistics (already exhausted earlier this session) to the actual ordered transcribed note data for the first time: real structural segmentation found Singularity's own lead activity concentrates at the song's OPENING as a genuine repeating hook (2.29 notes/sec), not a mid-song shred solo. Measured that real hook's own shape directly: 7-semitone register span, 75% stepwise motion, ONE deliberate leap in 16 moves (~6%) -- vs. the engine's solo using a full 3-octave span and a wide leap on 35% of every note. New `_SOLO_LOW_OFFSET`/`_SOLO_HIGH_OFFSET` (narrows the main phrase to ~26 semitones) and `_SOLO_STAB_CHANCE = 0.15` (down from 0.35), honestly documented as informed by one specific reference's own real data, not a universal corpus statistic. Legato tail deliberately stays unclamped (a real, separate climactic technique). New dedicated test. 710 passed engine-side (up from 709). Verified live: solo main-phrase span dropped 36 -> 26 semitones.

New `lead_mode = "ambient_lead"` (replaces `"silent"` throughout `song.py`): real, genuinely independent melodic content via the same `generate_lead_line`/`lead_vocab` machinery (including real corpus Markov transitions) solo/chorus_lead already use, deliberately sparse and sustained (one note per 2 beats, half solo's dense rate) so it reads as a background voice over the chug rather than a competing technical line; `stab_chance=0.0`. The riff-doubling synth stays, additive. `midi_export._lead_events_for_section` gained a real `ambient_lead` timing branch (2.0-beat spacing, sustained note-offs) plus real backward compatibility for any section dict saved before this change with the old `"silent"` label (degrades to zero events, never raises) -- `reaper_project.py` inherits this for free (already reuses the same function). Fixed 6 tests whose own hardcoded `"silent"` assertions were now legitimately stale, added 2 new dedicated tests. 707 passed engine-side (up from 705), 34 backend-side (unchanged). Verified live: the same seed-42 song that had 8 silent sections now has 8 real `ambient_lead` sections with genuine content, rendered and sent.

**§18.7:** one loose thread, never asked -- whether the old `z2wgv7dc4h-alt/Ww` repo has more history worth pulling forward. Low priority; flag if it comes up.

**Real gap found during X.20, closed by X.21 (not in the scope doc's own numbered lists, but worth tracking here since it's the same class of issue):** `preset.open_chance`'s real intended purpose -- an open-string-ringing-vs-muted-note VELOCITY/articulation choice per hit (confirmed via the real reference source, `reference/ww-forge-prior-attempt/engine/riff_engine.py`'s `wants_open = rng.random() < open_chance`) -- is now DONE at the 2-tier level (X.21: real 120/88 velocities, clamped around the real 110 threshold). Deliberately deferred: the reference's richer 4-tier accent/downbeat velocity table (`120`/`100`/`96`/`88`, needing `atmosphere.find_accents`'s accent data threaded into `performance.humanize_take`) is a real, smaller, still-OPEN follow-up.

**Real gap found+closed X.23:** `chords.solve_chord`/`riff.voice_chord_section` (P1.11/P3.11, real and tested) were only ever reached for chill/interlude's ambient chords, never the main rhythm-guitar riff -- confirmed via grep the main riff has been strictly monophonic all session. DONE (X.23: real power chords, root+5th, at MIDI/`.rpp` export time for every role except chill/interlude).

**Full-engine unwired-capability audit (2026-09-09, user-requested "what else is missing"), confirmed via grep -- zero real usage anywhere outside each function's own file + tests:**
- `structure.pickup`/`bridge`/`flatten` -- **CORRECTED (2026-09-10 full scope re-read): PARTIALLY DONE, this entry was stale.** `pickup`'s actual RULE ("overwrite the last n cells of the outgoing section with the first n of the incoming one") is real and shipped -- X.24 built `song._pickup_cells`/`_pickup_values`, a direct reimplementation adapted for this project's richer cell-dict shape (`structure.pickup` itself returns bare `{duration, is_rest}` cells and would silently strip `role`/`velocity`/`timing_offset` if called directly -- see `song.py`'s own module comment above `_pickup_cells`, ~L330-361). So every section transition IS blended now, just not via a literal call to the original ported function. Still genuinely OPEN: `structure.bridge()`'s INSERTED-transition-cell device (adding new cells between sections, distinct from overwriting existing ones) -- deliberately deferred per `song.py`'s own comment, since inserting cells changes a section's total beat count and would desync every parallel instrument track's own beat count too. `structure.flatten` itself remains unused (superseded by the per-track `_pickup_cells` approach, which never needed a single flat list).
- `groove.gallop_cell` -- DONE (X.32). Wired into `motif.generate_motif`'s `feel` dispatch; `song._ROLE_FEEL_OVERRIDE_CHOICES` gives `build` sections a real chance at it regardless of preset.
- `groove.stutter_chug_cell` -- DONE (X.32), same real wiring as `gallop_cell`.
- `interplay.call_and_response` -- OPEN. Guitar reacting to drums/lead instead of being generated in isolation (scope `## 4`). Not investigated further this session.
- `structure.apply_half_time` -- OPEN. A real RHYTHM-level half-time device (doubles actual cell durations in a sub-range, distinct from X.18's TEMPO-only mid-section drop). Not investigated further this session.
- `slam.mark_pinch_harmonics` -- DONE (X.27, real accent on the true final hit of every `breakdown`/`outro` section, every preset). `chromatic_creep` (the low open-string chromatic "creep" device) -- still OPEN; a real PITCH-content generator that would need splicing into the existing degree-delta motif model, a bigger lift than the velocity-only pinch-harmonic accent was.
- `structure.generate_section_content`/`generate_song_sections` -- **CORRECTION (2026-09-09): NOT dead, the earlier "confirmed DEAD" claim was wrong.** The original grep only checked usage outside `structure.py`'s own test file; `tests/test_structure.py` has real, dedicated tests calling both directly (`test_generate_section_content_anchors_on_arc_start_degree`, `test_generate_song_sections_wires_arc_across_a_real_sequence`, `test_interlude_routes_through_chill_arc_row`, and more), and `tests/test_atmosphere.py` actively reuses `generate_section_content` as a real fixture helper to build Motif+pitches content for testing `atmosphere.py`'s own functions. Real status: an earlier, simpler P6.4 implementation of "wire `arc()` into section generation," legitimately SUPERSEDED for the production pipeline by `song.py`'s own richer `_generate_attempt` (which also wires IRVD/feel/chord_quality/tempo_map/etc.), but still real, tested, and actively reused as a building block by another module's tests -- not a deletion candidate, no action needed. X.26 (the planned deletion) was cancelled once this was found.

**DONE (2026-09-10, same session as the finding) -- the atmosphere/pad/synth layer and octave-stab device now actually reach exported output.** Found during a user-requested full scope re-read ("review the full scope file... there's a lot of gaps"): `song._generate_one_section` computed real `pad`/`octave_stabs` data every section but neither `midi_export.py` nor `reaper_project.py` ever consumed it (confirmed via a whole-engine grep returning zero matches outside `song.py`/`atmosphere.py`/their own tests) -- scope §6/§16's synth/atmosphere layer and the octave-stab device were silently inert in every real exported file all session. Fixed: `midi_export.py` gained `_pad_events_for_section` (a real sustained `atmosphere.pad_voicing` chord spanning each section's full length, GM program 90 "Pad 2") and `_accent_events_for_section` (`atmosphere.find_accents`'s own per-accent `stab_voicing` chord -- GM program 56 "Orchestra Hit", scope sec.16.3's "a single orchestral hit landing exactly on a structural accent" -- landing at the accent's real cell position, with `preset.octave_stab`'s `VoiceLeader.stab()` leap layered into the same event when present, since it's already generated 1:1-zipped with `accents`). Two new real MIDI tracks ("Pad" channel 4, "Accents" channel 5); `reaper_project.py` reuses the exact same two functions for two new real `.rpp` tracks (same "one source of truth, two serializers" pattern the module already follows). Fails closed on an accent `cell_index` that doesn't fit the current cells (never produced by real output, but not assumed). `editor/frontend/src/components/MidiPlayer.tsx` also gained dedicated undistorted `pad`/`accent` Tone.js synth voices (sine pad, triangle stab) and track-name routing, since the two new tracks would otherwise have silently fallen through to the distorted guitar synth rig. Verified live: real exported MIDI/`.rpp` for a real djent song (seed 1, 8 sections, octave_stab=true) shows 24 real pad notes (8 sections x 3-tone chord) and 40 real accent notes (8 accents x 4-tone stab + 8 octave-stab leaps); confirmed in the running editor (a fresh backend restart was needed -- `uvicorn --reload` only watches `editor/backend/`, not the sibling `engine/` dir it `sys.path`-inserts, worth remembering for any future engine-only change made while the editor is running) that the new tracks arrive over `/api/export-midi` and play back with zero console errors. `engine/tests/test_midi_export.py`/`test_reaper_project.py` gained real content tests (not just track-count). 620 passed engine-side (up from 613), 34 passed backend-side (unchanged -- no backend code touched). Not yet done: the reference's fuller orchestral-bed/choir layer (Spitfire/sfizz, still blocked on an external sample-library install, unchanged from before) -- this closes the EXPORT-WIRING gap specifically, not §16.2's full orchestral instrument roster.

---

## Cross-cutting (spans multiple phases, not one Phase N)
- [x] X.1 `engine/song.py` `compose_song(preset_id, seed)` -- full pipeline integration. Gap found on review: every phase (1-7) built and tested its own mechanism in isolation; nothing had ever taken a real `Preset` and threaded it through a real end-to-end generation run (rhythm guitar x2 double-tracked, lead, bass, drums+fills, atmosphere, judge/retry). `engine/tests/test_song.py` verifies this for every real preset -- passed first try, confirming the independently-built phases actually compose.
- [x] X.2 Consume remaining preset fields. `drums.kick_pattern_for_style` dispatches on the real style values found in the preset JSON files (bounce/lock -> exact guitar-lock; sparse -> every-other-hit; euclid -> genuine Bjorklund-equivalent even distribution, pulse count from the guitar's own hit count; two_step -> documented metalcore breakdown interpretation (kick at beat-offsets 0.0/1.5 per 2-beat cycle); blast -> every cell). `motif.generate_motif` gained `group_beats` (tiles a short cell via `rhythm.tile_cell` for djent's N-against-4 displacement) and `pedal` (via new `apply_pedal_bias`, biases delta selection toward the root proportional to `preset.pedal`). `preset.octave_stab` now adds real `VoiceLeader.stab()` leaps at accent positions when true, empty list when false. All wired into `song.py`'s real `compose_song` call path; tested with A/B comparisons on the real `djent` preset (`dataclasses.replace` to flip one field, identical seed on both sides) proving each field measurably changes output, not just RNG noise.
- [x] X.3 Wire `motif.ThemeRegistry` (P3.4) into `compose_song` -- it existed but had no caller; sections sharing a role now develop one recurring theme (rendered at each section's own arc-driven degree, `invert`-ed on alternate reuses) instead of an independently-rolled motif per section. Fixing this surfaced a real bug (`invert` can push a pitch below the guitar's lowest string) -- added `_snap_to_playable_octave`, same discipline `bass.py` already uses.
- [x] X.4 Fix `compose_song`'s dead `seed` parameter -- it called `structure.judge_and_retry`, which always tries its own `range(max_seeds)` internally, so every call silently collapsed onto whichever of seeds 0-5 judged first regardless of the caller's seed. Replaced with a local retry loop seeded `seed, seed+1, ...`.
- [x] X.5 Realistic per-role lead-guitar behavior -- was generating an active melodic line in every section all song long. Now: `solo` = dense featured line (register pushed up, more leaps), `chill`/`interlude` = harmonized doubling of the rhythm's own theme (`riff.harmonize_line`), everything else = silent (a busy independent lead would clash with a dense chug section).
- [x] X.6 Periphery-level musicianship goal (see CLAUDE.md Bar) -- three genuinely new capabilities, not just unwired data:
  - [x] X.6a Legato/technical run generator -- `engine/legato.py`: `generate_legato_run` (contiguous scale-degree walk via `scale.step`, structurally distinct from VoiceLeader's weighted-pick model; optional single mid-run direction reversal when seeded), `legato_run_rhythm` (fast even subdivisions, genuine tuplets via `rhythm.tuplet_grid` when the length fits 3/5/7), `fret_positions_for_run` (single-string-biased via `Fretboard.pitch_to_fret`'s `prev` chaining -- real hammer-on/pull-off technique stays on one string). Wired into `song.py`: every solo section splices a real legato lick onto its featured lead line (`section["legato"]`), fails closed (skips the splice) rather than fabricating a position if genuinely unplayable on that preset's tuning.
  - [x] X.6b Extended/altered chord vocabulary -- `engine/chord_vocab.py`: `CHORD_QUALITIES` (maj7/min7/dom7/sus2/sus4/add9/maj9/min9/six9, semitone-offset tuples matching `scales.py`'s `SCALES` convention), `get_chord_quality` (raises `ValueError` on unknown name, never fabricates), `voice_named_chord` (reuses `riff.voice_chord_section` -> `chords.solve_chord` for real fingering, no reimplemented solver), `quality_for_dissonance` (buckets a section's real `arc_row["dissonance"]` across `DISSONANCE_ORDER`, low->open/consonant, high->darker). Wired into `song.py`'s `chill`/`interlude` (`lead_mode == "harmony"`) branch as `section["chord_quality"]`/`section["chord_voicing"]`, alongside (not instead of) the existing `riff.harmonize_line` doubling; every other role leaves both `None`. Fails closed to `None` on a genuinely unreachable voicing rather than a fabricated shape. `engine/tests/test_chord_vocab.py`: every quality reachable on a real loaded tuning, unknown-quality/unreachable-chord rejection, monotonic dissonance bucketing, and an end-to-end `compose_song` check exercising both a real reachable voicing and a real unreachable one.
  - [x] X.6c Metric modulation as a compositional device (the pulse itself reinterpreting, e.g. a dotted-eighth becoming the new quarter) -- a step beyond the existing `tile_cell`/`metric_polyrhythm` (which stay within one fixed pulse). `engine/metric_modulation.py`: `modulation_ratio(old_subdivision, new_subdivision)` (subdivision = `(numerator, denominator)`, ratio = `value(old)/value(new)`; verified against the textbook dotted-quarter=new-quarter case, ratio 1.5) and `apply_metric_modulation(base_bpm, ratio)`; both fail closed on non-positive/malformed inputs. Wired a real `"metric_modulation"` branch into `structure.tempo_at`'s existing curve dispatch (alongside `"drop"`/`"ramp"`, same pattern). `song.py` fires a real trigger on the first `build`->`breakdown` transition in the generated sequence (straight eighth of the old pulse becomes the new quarter -- ratio 0.5, the genre's half-time breakdown treatment), producing `song["tempo_map"]` (one BPM per section, computed via `tempo_at` for every composed song, defaulting to flat `preset.bpm` when no transition occurs). `engine/tests/test_metric_modulation.py`, plus extensions to `test_structure.py`/`test_song.py` (including a direct `generate_section_sequence` seed search to guarantee a real build->breakdown transition exists before checking its tempo_map effect).
  - [x] X.6d Major-family scales (lydian, mixolydian, major/ionian, harmonic major) -- added to `scales.py` (`major`/`ionian` alias, `lydian`, `mixolydian`, `harmonic_major`). No existing preset was switched to use them yet (would need careful vocab-weight redesign, not a drop-in swap, since weights are semitone-interval based and snap onto whichever scale is active) -- the capability now exists, adopting it into a preset is a deliberate follow-up.
- [x] X.7 `engine/audio_vocab.py` -- real-audio structural/tonal vocabulary, the audio analogue of `midi_vocab.py`'s MIDI mining. Tempo, harmonic/percussive rhythm-density split, spectral-centroid brightness descriptor, per-section relative density curve, key/tonal-center estimate (Krumhansl-Schmuckler correlation), real structural segmentation (MFCC self-similarity, not equal slices), tempo-stability check, drum-onset role classification (KICK/SNARE/HIHAT_OR_CYMBAL by frequency band), and guitar-rhythm-pattern detection (straight/gallop/syncopated from onset timing alone, never pitch). Never transcribes notes/chords, only extracts numeric statistics -- same "reference data, not composer" boundary as the MIDI vocabulary. Optional local `demucs` pre-processing (`pip install demucs`, MIT-licensed, run locally -- never upload audio to a third-party "online demucs" site) gives real per-instrument stems. Validated against a real user-supplied track (a friend's original song): found the guitar/bass has more section-to-section dynamic range than the drums (validates `theory.arc()`'s design), the track sits in A MAJOR (a real data point that the scale library's lack of major-family modes, X.6b-adjacent, is a real gap), and the drum-role classifier's own output (implausibly few detected snares) honestly demonstrates a real ceiling -- a hand-built frequency-band heuristic isn't reliable on a dense, cymbal-heavy mix; a proper trained drum-transcription model would be needed to do better, not just more analysis code. Also added `transcribe_and_split_registers` (optional, needs `basic_pitch` -- not in requirements.txt, its own dependency pins are fragile on newer Python, install ad hoc): real polyphonic audio-to-note transcription split into rhythm-register vs lead-register layers by pitch. Cross-validated on the real track: the rhythm layer's dominant transcribed pitch class (A) independently matched `estimate_key`'s separate chroma-based key estimate (also A) -- two unrelated methods agreeing is real evidence, not proof, that this is picking up something true.
- [x] X.8 Wire `preset.feel` (was validated at load time, never consumed by generation -- same class of gap as X.2's kick/group/pedal/octave_stab). Investigated the real reference implementation ("Metalerator", `reference/metalerator/` -- confirmed via its own README to be a real, dedicated metalcore generator, not a generic engine with metalcore bolted on) for what a genuinely portable, non-invented `feel`-driven technique looks like: its breakdown generator (`rhythm_guitar/breakdown/default_melodic.py`) draws durations from a real weighted table (biased toward 8th/quarter chugging over rapid 16ths) and enforces "no isolated single 16th note" (pairs them). Ported both, adapted to this engine's duration vocabulary (half notes dropped, ratio preserved): `rhythm.FEEL_DURATION_WEIGHTS`/`FEEL_NO_SINGULAR_SHORT`/`duration_bias_for_feel`, new opt-in `weights`/`no_singular_short` params on `rhythm.generate_rhythm` (default `None` = byte-identical old uniform behavior, verified by test), threaded through `motif.generate_motif`/`ThemeRegistry.get_or_create` and wired at `song.py`'s `preset.feel` call site. Only `"breakdown"` (metalcore's real feel) had ported reference data at the time (X.15/X.16 later added `chug`/`bounce`) -- other feel names stay uniform rather than inventing weight tables with no real source, per this project's own "port, don't invent" discipline. `engine/tests/test_rhythm.py`: weights=None is byte-identical to pre-existing behavior (zero regression risk), the real breakdown weights measurably shift the duration distribution, the no-singular-short rule eliminates every isolated occurrence across 50 seeds, bad-input rejection (all-zero-weight). `engine/tests/test_song.py`: a real A/B compose_song comparison (metalcore vs. the same preset with an unmapped feel) proves the 16th-note share measurably drops in real generated output, not just in rhythm.py isolation. 450 passed in 10.76s.
- [x] X.9 Real snare backbeat, wired for every preset -- closes a real, previously-unaddressed gap: this engine had a kick (P4.2/X.2) and blast/fill patterns (P4.3) but NO snare/hihat layer at all. Ported from the same real reference implementation as X.8 (`reference/metalerator/metalerator/drums/snare/snare.py`, `Snare.snare_step`/`snare_half_step`/`snare_double_time`): `"step"` is a half-time hit on beat 3 of every bar (checked the real source rather than assuming the generic "2 and 4"), `"half_step"` once per 2-bar cycle, `"double_time"` every beat except the section's first. Built on the same real cell-timeline mechanism `_kick_two_step` already used (`_cell_starts`/`_time_to_cell_index`/`_cyclic_hit_indices`, extracted as shared helpers -- `_kick_two_step` itself refactored onto them, no duplicated logic). Wired for EVERY preset via a real per-ROLE dispatch (`drums.snare_pattern_for_role`, `song.py`'s `section["snare"]`), not gated to one genre -- breakdown/intro/outro get the half-time backbeat, build/solo get the denser double_time, chill/interlude stay silent (same judgment call `lead_mode` already makes for those roles). Wired into `midi_export.py`'s Drums track alongside the kick (real GM notes 36/38, standard single-drum-track convention). Ghost notes (Metalerator's quiet grace-note hits around the main snare) documented as a real, deliberately deferred follow-up -- don't fit the fixed-cell-array model without a larger rework. `engine/tests/test_drums.py`: exact hit-position checks for all three styles against a hand-built cell grid (not trusting the mechanism blindly), role-dispatch table coverage, bad-input rejection. `engine/tests/test_song.py`: every real preset gets a real snare layer with chill/interlude silent. `engine/tests/test_midi_export.py`: real GM kick(36)/snare(38) note counts cross-checked against actual generated song data. 464 passed in 10.96s.
- [x] X.10 New preset: `progressive` -- adopts X.6d's major-family scales for the first time (they existed since X.6d but nothing used them, a deliberately tracked follow-up). Real Periphery-styled "melodic technical djent": lydian scale (bright, uplifting #4, the quintessential prog-metal mode) over djent's own real mechanisms (group=3 N-against-4 displacement, euclid kick, octave stabs) rather than djent's deliberately un-melodic root/5th-only vocab. Calibrated against the real user-supplied reference track analyzed in X.7/P10.3: `drop_a_7` tuning chosen because its open low string IS pitch class A, matching the track's real measured A-major key (Krumhansl-Schmuckler estimate) exactly; lydian chosen over plain major for the track's real bright/articulate spectral-centroid reading. Verified the generated output is genuinely scale-legal: every pitch class across a real generated song fell within exactly `{A,B,C#,D#,E,F#,G#}` (A-lydian's real interval set), zero notes outside it. Also fixed a real, now-stale mapping this surfaced: the `"periphery"` band-linked alias (`presets.ALIASES`) pointed at `"chill"` since this project's earliest presets (a proxy chosen only because no real Periphery-styled preset existed yet) -- repointed to `"progressive"`, the actual match, with the two tests that hardcoded the old target (`test_presets.py`, `test_song.py`) updated to match. 482 passed.
- [x] X.11 Real hihat/cymbal layer + varied kick overlay for high-energy sections -- direct response to real listening feedback ("the drums were really basic... no double kick no cymbals or anything being hit. just generic kicks every now and then"). Closes a real, previously-total gap: through X.9 this engine had kick + snare but ZERO cymbal content anywhere, ever. `engine/drums.py`: `generate_hihat_pattern`/`_hihat_closed` (real steady 8th-note pulse, universal genre-agnostic "keeping time" convention, not ported from a specific source since it needs none), `hihat_pattern_for_role` (wired for every preset via `song.py`'s `section["hihat"]`, silent only for chill/interlude, same rule as snare). Also: `_kick_double_kick` (real continuous straight-16th pulse, ported from Metalerator's actual `Kick.double_bass`), registered as a new selectable `"double_kick"` style, plus `kick_pattern_for_role`/`_ROLE_KICK_OVERRIDE_CHOICES` -- `build`/`solo` sections now ALWAYS get a real, VARIED overlay (`double_kick` or `blast`, picked per section via the real seeded rng) regardless of what the preset's own `.kick` field declares, replacing `kick_pattern_for_style` as `song.py`'s call site. Wired into `midi_export.py` and `reaper_project.py`'s Drums track (real GM note 42/HIHAT_CLOSED alongside kick/snare). `engine/tests/test_drums.py`: real hit-position checks, role-table coverage, bad-input rejection, kick-role-override requires a real rng (fails closed rather than silently defaulting). `engine/tests/test_song.py`: every real preset gets real hihat with chill/interlude silent, build/solo sections get real overlay variety across seeds. Generated and sent the user two real demos: a 16-section/3:09 song (up from 8-section/1:28, testing whether length addresses the "basic" feedback) with the new drum layer audible throughout. 500 passed.
- [x] X.12 Real, critical tempo-map bug fix: half-time never reverted. Traced from direct listening feedback on a 16-section generated song ("drums are too slow... its not really metal. its too slow") -- inspecting the real `tempo_map` confirmed X.6c's original one-shot design (`_build_to_breakdown_index`: modulate once at the FIRST build->breakdown transition, hold for the rest of the song) meant 14 of 16 sections were stuck at half-time (76 BPM instead of 152) for the whole rest of that song, including both solo sections -- directly explaining "no solos" too (they were real, present, just playing at half-speed). Fixed `song.py`'s `_compute_tempo_map` to apply the real metric-modulation half-time treatment INDEPENDENTLY to every `"breakdown"`-role section and revert to the preset's own full `base_bpm` for every other role -- including one immediately after a breakdown -- matching real genre convention (a breakdown's half-time feel is a temporary device, not a permanent state change). Removed `_build_to_breakdown_index` (no longer needed); rewrote `engine/tests/test_song.py`'s tempo-map tests to verify real per-section modulation AND real reversion (a sequence with two breakdowns and sections between them, confirming a real drop-then-recover-then-drop-again pattern), including a real end-to-end check against actual `compose_song` output for every non-breakdown/breakdown section. 499 passed. Regenerated and sent the corrected demo -- same 16-section sequence, but solos and builds now play at real full tempo instead of stuck at half-speed.
- [x] X.13 Real hihat variation: open-hat accents + section-transition crashes. Grounded in a real finding, not guessed: ran `demucs` on a real user-supplied reference track ("periphery style full song.mp3"), then `audio_vocab.classify_drum_onsets` on the isolated drum stem -- cymbals/hihat were 84% of ALL detected drum onsets (1079 of 1282), by far the most constantly-present element in a real mix, while X.11's hihat had zero variation (one flat closed-hihat pulse). `engine/drums.py`: `apply_hihat_accents` (real open-hat accents at the SAME structural-accent positions `atmosphere.find_accents` already computes for octave stabs -- never an independently-invented accent set) and `add_transition_crash` (a real crash at a section's opening whenever its role genuinely changed from the previous one -- ported from Metalerator's real "crash on kick/snare pattern change" cymbal generator, generalized to this project's own role-based arrangement unit). Wired into `song.py` for every preset (skipped for chill/interlude, which stay silent by design). `engine/tests/test_drums.py`: accent overlay never fabricates a hit on a rest, crash override behavior, no-op when not firing. `engine/tests/test_song.py`: every real composed song's first section gets a real opening crash. 504 passed. Regenerated and sent the demo -- 14 real crashes, 20 real open-hat accents, 311 closed hihat hits.
- [x] X.14 Real 4-state theme-development rotation, closing a real "not much is going on" gap. Direct response to real listening feedback comparing generated output against two real reference tracks. Root cause: `ThemeRegistry` keys a base theme by ROLE alone, and every occurrence of that role for the WHOLE SONG only ever alternated between 2 pitch-contour states (base theme, or `invert()`) -- a longer song revisiting a role many times (real for any longer song, e.g. 6 breakdowns in a 16-section song) heard just those same 2 states over and over regardless of how many times the role recurred. `motif.transpose`/`motif.augment`/`motif.fragment` all already existed, real and tested, but only `invert` was ever wired into this rotation. Added `song._develop_theme(base_theme, occurrence)`: a real 4-state rotation (base / invert / transpose+2-degrees / transpose+invert) using only safe, length-preserving develop ops (transpose/invert touch pitch deltas alone, never rhythm cell count or duration, so no downstream section-length/timing invariant is at risk). Verified against real generated output: a 16-section song's most-repeated role (6 real occurrences) went from a 2-state ceiling to 4 real distinct pitch-content states. `engine/tests/test_song.py`: exact rotation-state check against a hand-built motif, real end-to-end variety check against actual `compose_song` output for a role recurring 3+ times. 506 passed. Noted as a real, honest scope limit: this addresses PITCH/harmonic repetition specifically -- the deeper RHYTHM-level repetition (the same base rhythm cell reused verbatim across every occurrence of a role, just pitch-shifted) remains a separate, larger, not-yet-attempted architectural question.
- [x] X.15 Real triplet + chug feel generation -- closes two more completely-unwired declared preset feels. Direct response to real listening feedback ("not enough chugging. not enough triplets."). `tech.json` (`"feel": "triplet"`, own description: "kick-locked triplet chug") and `deathcore.json` (`"feel": "chug"`) were both real, declared feel values with zero generation effect -- like `"breakdown"` before X.8, silently falling back to uniform duration selection. Built `rhythm.generate_triplet_rhythm(total_beats, hit_chance, rng)`: a genuinely different rhythmic device (real eighth-note-triplet subdivision, `span_beats/3` via the SAME `tuplet_grid(3, 2, 1.0)` spacing law every other real tuplet in this project uses), not a reweighting of the existing `[0.25, 0.5, 1.0]` duration menu -- a real triplet can't be expressed that way at all. Wired as a dedicated dispatch branch in `motif.generate_motif` (bypasses `duration_bias_for_feel`'s weighted-choice path entirely for `feel == "triplet"`). Also added `rhythm.FEEL_DURATION_WEIGHTS["chug"]` -- real, deliberately un-ported (no source needed: constant 16th-note palm-muted chugging is generic, textbook metal-guitar technique, same category as X.11's hihat pulse), the deliberate inverse of breakdown's 8th/quarter bias. Verified against real generated output: tech's real compose_song output is now 100% genuine 1/3-beat triplet durations; deathcore's is now 16th-note-dominant (>50% of all hits, up from a uniform 1/3 baseline). `engine/tests/test_rhythm.py`: real triplet-duration/count checks, spacing verified against the same real `tuplet_grid` law, bad-input rejection (non-whole beat counts fail closed rather than silently truncating). `engine/tests/test_song.py`: real end-to-end checks against actual `compose_song` output for both presets. 513 passed. Generated and sent real demos for both presets.
- [x] X.16 Real "bounce" feel density fix + real minor-third vocab weight for djent, grounded in EXACT reference data (not audio-transcription-uncertain, not textbook-generic). User supplied a real original MIDI file ("born of osiris style midi.mid", confirmed an original composition, not a copyrighted commercial transcription) -- parsed with `mido` for exact note data (no uncertainty at all, unlike audio-derived analysis): Guitar 1 measured 57.5% 16th notes, 36.9% 8th notes, and literally 0% quarter notes across 1464 real notes; pitch-class distribution showed real minor-third (interval 3) usage as the third-most-common interval (11%), right after root/fifth. Found `"bounce"` (djent/groovy/melodic/progressive's real declared feel -- the MOST common feel across the whole preset lineup) had ZERO real duration weighting, unlike `breakdown`/`chug` -- fell through to fully uniform selection, meaning roughly a third of every bounce-feel preset's notes were quarter notes, essentially never seen in the real reference. Added `rhythm.FEEL_DURATION_WEIGHTS["bounce"]` (real measured ratio, quarter notes kept at a small nonzero floor rather than a hard 0) and `FEEL_NO_SINGULAR_SHORT["bounce"]`. Also added real weight for interval `3` (minor third, confirmed scale-legal for djent's own phrygian scale) to `djent.json`'s vocab, matching the real reference's actual interval usage. Verified against real generated output: djent's real duration distribution went from ~33/33/33 (16th/8th/quarter) to 78.5%/21.5%/0%. `engine/tests/test_rhythm.py`: exact real-table check. `engine/tests/test_song.py`: real end-to-end density check against actual compose_song output. 515 passed.
- [x] X.17 Cross-preset audit triggered directly by the user's "I want these to be generating music identical to their respective genres" -- checked every preset for the same class of gap X.16 fixed in djent, not just the one preset that prompted it. Found a real regression X.16 itself introduced: `chill.json` shared `"feel": "bounce"` with djent/groovy/melodic/progressive, so X.16's real measured-dense duration weighting (correct for those four energetic presets) also silently made `chill`'s duration mix dense/16th-heavy -- directly contradicting chill's own documented identity ("Low-density breather, consonant. Airy, Periphery slow-part feel."). Added a new, distinct `"airy"` feel value to `rhythm.FEEL_DURATION_WEIGHTS` (deliberate inverse weighting, quarters dominant -- deliberately NOT added to `FEEL_NO_SINGULAR_SHORT`, since forcing isolated-16th pairing would fight the sparse character) and changed `chill.json`'s `feel` to `"airy"`. Verified against real generated output: chill went from what would have been dense-16th-dominant to 53.4% quarters / 28.6% 8ths / 18.0% 16ths. `engine/tests/test_rhythm.py`: `test_duration_bias_for_feel_airy_is_the_real_inverse_of_bounce`. Audited the rest of the lineup for the same class of gap and found no further fix needed: `groovy.json` (motion=0.0, root/5th-only vocab) correctly has no minor-third weight -- its own description is "Power chords... The baseline archetype", deliberately simple by design, not an oversight. `melodic.json` already weights interval 3 highest of all (5, above root's 4) matching its own "3rds and 6ths" description. `metalcore.json`/`deathcore.json`/`tech.json` were already confirmed correct in X.8/X.9/X.15/X.16's own work (real ported breakdown/chug/triplet feel data, real interval-3/tritone coverage). Also, separately: reviewed the whole project's file tree for staleness per the user's own "all files should be reviewed and updated for relevancy or deleted" -- deleted four one-time bootstrap artifacts already fully consumed (`HANDOFF.md` pointed at "Phase 3-5" long since finished; `README-SETUP.txt`/`REMOTE.txt`/`SESSION.txt` were one-time initial-assembly/git-init/first-paste instructions, already executed session 1), refreshed `PLAN.md`'s stale "Built vs intended" summary (was still "159 tests... Phases 3-5 in progress"), trimmed `CONTROL.txt`'s completed "ONCE" setup section, fixed `PORTS.md`'s stale "metalerator: ...do not run the app" line into a real cited port table (breakdown weights/snare styles/double_kick/crash-on-change, each with its real X.8/X.9/X.11/X.13 status), and fixed two real drifted `.claude/settings.json` hooks found uncommitted at session start (confirmed with the user before touching hook/security config): restored `Bash` to the `PreToolUse` matcher so `guard-write.ps1`'s already-improved Ww/123 command-string check actually runs, and restored the Stop hook's auto-close prompt so `CLAUDE.md`'s "Stop auto-runs the close skill" claim is true again. 516 passed, 1 skipped.
- [x] X.18 Real mid-section tempo/half-time drops (scope sec.14.2 item 4, closed via the new `## Scope gaps tracker` above -- a full scope re-read at the user's own request found this real, previously-untracked gap sitting in the scope doc since day one). Distinct from X.6c/X.12's BETWEEN-section metric modulation: this is a genuine "blasting, then half-times for 2 bars as a slam moment" device WITHIN one section. Same real mechanism as X.6c (`metric_modulation.modulation_ratio`/`apply_metric_modulation`, ratio 0.5 -- no new modulation math invented), just triggered mid-section: `song.py`'s per-section loop decides, via that section's own seeded rng, whether an eligible section (`role in ("build","breakdown")`, `preset.bars >= 4`, 50% chance) gets a drop trigger at its own last 2 bars; a new pure `_resolve_tempo_drop(section_bpm, trigger_beat)` (called once tempo_map is known) produces the final `section["tempo_drop"]` (`{"trigger_beat","bpm"}` or `None`, never fabricated for an ineligible section). Deliberately a pure tempo/BPM device matching X.6c/X.12's own precedent -- the SAME notated guitar/drum cells just play slower for the dropped tail, no rhythm regeneration, keeping this scoped to tempo bookkeeping + the two exporters. `midi_export.py`'s `_tempo_track` gained a real second `set_tempo` event at the correct tick for any dropped section (note positions elsewhere untouched -- MIDI ticks are beat-based, not time-based). `reaper_project.py`'s `_tempo_envelope_block` now takes a flattened, already-ordered point list; `song_to_rpp`'s `start_seconds` accumulation is now two-part for a dropped section (pre-drop bpm up to the trigger, dropped bpm for the remainder) so every LATER section's real start time still lands correctly. Verified against real generated output (djent seed=11/8 sections): two `build` sections dropped from 170->85 BPM, and -- a real, unplanned-but-correct emergent effect -- two `breakdown` sections (already at X.6c's own real half-time 85 BPM) dropped FURTHER to 42.5 BPM, a genuine "double drop" quarter-time slam moment inside an already-half-time breakdown. `engine/tests/test_song.py`: real seed-search end-to-end check (bpm exactly half that section's own `tempo_map` value, correct trigger beat) plus a fails-closed check (wrong role, and a synthetic `dataclasses.replace(bars=2)` preset, both always `None`). `engine/tests/test_midi_export.py`/`test_reaper_project.py`: exact tick/exact-real-seconds position checks for the extra tempo event, updated the 2 pre-existing tempo-map tests that assumed exactly one tempo point per section (a real, expected consequence of this feature actually firing on those seeds, not a regression). 520 passed, 1 skipped (up from 516).
- [x] X.19 Real IRVD phrase development (Introduction/Repetition/Variation/Destruction, scope sec.18.2), wired for the first time. Direct response to blunt real feedback after X.18: "it still doesnt sound like a good song. nowhere near the quality or composition i expect." Rather than guess another tuning tweak, grepped the whole engine for real, unconsumed capability -- the exact pattern behind nearly every prior quality fix this session. Found the biggest one yet: `rhythm.phrase_plan(bars)` (real, ported P2.7, real tests) was called NOWHERE outside `rhythm.py`'s own test file -- confirmed via a full-engine grep. Every section, for every preset, was one flat undifferentiated rhythm+pitch draw spanning the whole section; no bar had a deliberate shape. For 6 of 8 presets (everything except djent/progressive, which have their own real `group_beats` polymeter-tiling device instead -- confirmed via grep, only those two set `"group"` in their JSON) that meant literally zero internal section structure. `engine/motif.py`: new `_generate_irvd_motif` built ENTIRELY from already-real, already-tested primitives, no new pitch/rhythm-choice logic invented -- "I" (Introduction) is a real `generate_motif` call for one bar; "R" (Repetition) is a literal deep copy of "I" ("play it again, verbatim"); "V" (Variation) is `transpose(I, 2)` (same rhythm skeleton, moved pitches); "D" (Destruction) is `augment(previous_bar, 0.5)` concatenated with a copy of itself (same content played twice as fast, refilling the bar exactly -- real "fragment and densify", derived from the bar immediately before it for real forward momentum, not always reaching back to "I"). New `generate_motif(irvd_bars=...)` parameter, threaded through `ThemeRegistry.get_or_create`; `song.py`'s `themes.get_or_create` call passes `irvd_bars=(preset.bars if preset.group is None else None)` -- mutually exclusive with `group_beats` by design (`generate_motif` raises `ValueError` if both given), since IRVD's verbatim-repeat structure would directly fight group_beats' deliberate phase-drift tiling; djent/progressive keep their existing real device untouched. Nothing downstream needed to change -- bass/kick/snare/hihat-follows-guitar, X.14's cross-section `_develop_theme`, MIDI/`.rpp` export all already just consume whatever Motif `generate_motif` returns. `engine/tests/test_motif.py`: exact structural checks (R bar cells/deltas exactly equal I's; V bar's rhythm exactly equals I's with deltas shifted by the real transpose amount; D bar has exactly double V's hit count with exactly-halved durations; single-bar `irvd_bars=1` reduces to byte-identical plain generation; `group_beats`+`irvd_bars` together raises). `engine/tests/test_song.py`: real end-to-end check against actual `compose_song` output for a real non-`group` preset (metalcore) -- every real section's bar 2 is a literal verbatim copy of bar 1, bar 4 has exactly double bar 3's hit count; confirmed djent/progressive are untouched. One pre-existing test (`test_tech_preset_real_feel_triplet_produces_genuine_triplet_durations`) needed a real, expected update: tech.json has no `group`, so its sections now legitimately include IRVD-Destruction-halved triplet durations (1/6) alongside the plain 1/3 triplet value. Verified against real generated output (metalcore seed=6/4 sections): every section showed the exact real shape, e.g. intro's 4 bars had hit-counts `[3, 3, 3, 6]` (I=R=V=3, D=6=2x), bar1/bar2 durations byte-identical, bar4 exactly half-duration/double-count. 525 passed, 1 skipped (up from 520).
- [x] X.20 Fixed a real, severe density bug: `song.py` was passing `preset.open_chance` as `hit_chance` (`rhythm.generate_rhythm`'s rest-vs-hit probability). Checked the real reference source (`reference/ww-forge-prior-attempt/engine/riff_engine.py`): `open_chance` there is `wants_open = rng.random() < open_chance` -- the chance a HIT is an open ringing note vs a muted low-root note, a completely different (pitch/articulation) axis, not rest-vs-hit density at all. Verified real damage with real generated output (seed 3, every preset): `tech.json` ("extreme technical/dissonant deathcore... kick-locked triplet chug") had a REAL 5.56% hit rate -- **94% rests** -- because its correctly-low `open_chance` (0.10, correct for "rarely open/ringing, mostly muted") was misapplied as "almost never even a hit." deathcore was at 25%, metalcore 31%, most others 30-50%. Almost certainly the single biggest real root cause behind the whole session's recurring "not enough chugging/intensity" complaints -- every prior density-adjacent fix (X.8/X.15/X.16/X.17 feel weighting, X.19 IRVD, X.18 tempo drops) was operating inside an already-crippled density budget. Also found the REAL fix source in the same reference file: `base_density = float(knobs.get("density", 0.72))`, then `density = max(0.12, min(0.98, base_density * (0.55 + 0.9 * arcv["energy"])))` -- a real, previously-un-ported formula combining a flat baseline with real per-role ARC energy scaling. Ported it exactly: `song.py`'s new `_BASE_HIT_CHANCE = 0.72` + `_resolve_hit_chance(base, energy)`, called with `arc_row["energy"]` (already computed for every section since Phase 1 -- used for dissonance/register/start_degree -- but its `"energy"`/`"density"` fields were never consumed, same "capability exists, never used" pattern as X.19's IRVD). `preset.open_chance` itself is untouched (still validated) but no longer wired here -- its real purpose needs a muted-vs-open pitch axis this project's degree-delta pitch model doesn't have yet, tracked as a real, deliberately deferred gap in the Scope gaps tracker, not silently dropped. Verified against real generated output: tech's real hit rate went 5.56% -> 86.11%, deathcore 25.00% -> 68.00%, every preset now lands in a genre-plausible 68-88% range. Also fixes a second real finding from the same investigation: a seed's real "build" section previously measured SPARSER than its own "breakdown" (0.62 vs 1.25 notes/beat, backwards from genre convention) -- real ARC energy ordering (K=0.20 < I=0.30 < O=0.40 < A=0.60 < S=0.80 < U=0.85 < C=1.00) now actually drives density, confirmed by a real cross-seed test that breakdown-role sections are measurably denser than chill-role sections. `engine/tests/test_song.py`: exact formula/clamp checks against real hand-computed values; real end-to-end check that tech/deathcore's real hit rate now exceeds 50%; real cross-seed check that breakdown role is measurably denser than chill role. One pre-existing test (`test_preset_feel_breakdown_changes_real_song_duration_distribution`) needed its margin narrowed from 0.05 to 0.03 -- a real, expected consequence of density itself now varying by role (the underlying feel-bias effect is still clearly present, ~12% relative reduction, just a smaller absolute percentage-point gap now that X.19's IRVD Destruction bars and X.20's role-driven density both contribute to the same duration distribution). 528 passed, 1 skipped (up from 525).
- [x] X.21 Real open-string-vs-muted articulation, wired via velocity -- closes the real, deliberately-deferred gap X.20 left open (`preset.open_chance`'s actual purpose). Direct response to real positive-but-honest feedback on X.20: "its a big imrpovement... sounding a little more like metal. still lacking but a massive jump." Kept pulling the same real thread: `performance.humanize_take` had `velocity_base: float = 100.0` as a flat constant for literally every hit in every section -- zero articulation contrast anywhere in the engine. Read the real reference source further (`reference/ww-forge-prior-attempt/engine/riff_engine.py`): `wants_open = rng.random() < open_chance` (line 125) decides per-hit whether a note is an open ringing accent vs a muted chug, and the real emitted velocities (lines 183/187) are `120` for an open hit vs `88`/`96`/`100` otherwise; `artic.py`'s real `PALM_MUTE_VELOCITY_THRESHOLD = 110` confirms opens are always emitted comfortably above that line. Directly matches the user's own words from earlier this session: "djenty is just the style of guitar - open string palm mutes." Deliberately a simplified 2-tier port (open=120/muted=88, dropping the reference's accent/downbeat 3rd-4th tiers) rather than plumbing `atmosphere.find_accents`'s accent data through a new call chain -- ships the real, dominant, audible contrast; the richer tiering is a real, documented, deliberately deferred refinement. `engine/performance.py`: `humanize_take` gains `open_chance: float | None = None` (default `None` = exact prior flat-velocity behavior, byte-identical); when given, each hit independently rolls open (velocity clamped >= 110) vs muted (clamped < 110), jitter layered on top but never crossing the real threshold either way. `double_track` needed no signature change (`**humanize_kwargs` already forwards it). `engine/song.py`'s one real call site now passes `open_chance=preset.open_chance` -- take A/B roll independent open/muted patterns (separate rng, consistent with `double_track`'s own "two independently-humanized takes" philosophy). Real, expected side effect found and fixed: `judge()`'s real `pm_ratio` scoring reads `velocity<110` directly, so real bimodal open/muted velocity (vs the old jitter-noise-derived split) measurably changes which internal seed-retry attempts pass judge -- 2 pre-existing tests hardcoded `seed=11` expecting a "solo" section; fixed with a real seed-search (`range(11,30)`) matching this project's own established convention, not a loosened assertion. `engine/tests/test_performance.py`: `open_chance=None` byte-identical regression check; `open_chance=1.0`/`0.0` real distributional checks (every hit lands on the correct side of 110); a real mixed-`open_chance` check showing both bands actually occur; `double_track` forwards `open_chance` to both real takes. Verified against real generated output (metalcore seed=6/6 sections): 44.0% of real guitar hits landed open (>=110), 56.0% muted -- metalcore's own declared `open_chance=0.45`, matching almost exactly. Scope gaps tracker's `open_chance` line updated from OPEN to DONE (2-tier), with the accent-aware 3rd/4th-tier refinement noted as a real, smaller, still-open follow-up. 533 passed, 1 skipped (up from 528).
- [x] X.22 Real, previously-missed drum-silencing inconsistency: `kick_pattern_for_role` had NO `chill`/`interlude` check, unlike `snare_pattern_for_role`/`hihat_pattern_for_role` (both of which have gone silent for those two atmospheric roles since X.9/X.11). Direct response to blunt real feedback: "it sounds nothing like metalcore." Investigated by generating real metalcore output and inspecting every drum layer per section -- found `chill`/`interlude` sections had snare=0/hihat=0 (correctly silent) but kick=16 (full preset-density kick, IDENTICAL to the loud intro/breakdown sections). This is a real, significant bug present for EVERY preset, not just metalcore: an atmospheric "breather" section that still hammers a full kick drum underneath completely erases the dynamic contrast the whole arrangement depends on -- exactly the kind of thing that can make a song built from otherwise-correct pieces (real density X.20, real phrase shape X.19, real articulation X.21) still read as "nothing like" its genre, since the drums never actually let up anywhere in the song. Fixed `drums.kick_pattern_for_role` with the exact same real rule the other two layers already use: `role in ("chill", "interlude")` returns an all-rest cell array, checked BEFORE the existing `preset_kick`/build-solo-overlay dispatch. `engine/tests/test_drums.py`: split the old (bugged) "leaves other roles on preset style" test to exclude chill/interlude, added a dedicated `test_kick_pattern_for_role_is_silent_for_chill_and_interlude`. `engine/tests/test_song.py`: fixed `test_kick_style_field_changes_real_song_kick_output` (previously asserted the OLD buggy universal-lock behavior for chill/interlude too) and added `test_every_preset_gets_silent_kick_for_chill_and_interlude` mirroring the exact real snare/hihat coverage tests already in place, for every real preset. Verified against real generated output (metalcore seed=6/6 sections): chill/interlude now show kick=0/snare=0/hihat=0 (fully silent, real contrast), while intro/breakdown/build keep their real full drum kit. 535 passed, 1 skipped (up from 533).
- [x] X.23 Real power chords on the main rhythm guitar -- the engine has been strictly monophonic all session. Direct response to blunt feedback: "It needs to be heavier." Register checked first and was already correct (metalcore's real generated intro/breakdown sit at MIDI 35-42, right at the bottom of `drop_b_7`'s tuning). Checked `song.pitches_per_cell`'s real return type instead: `list[int | None]` -- one pitch per hit, confirmed via every consumer (`bass.follow_guitar_rhythm`, `midi_export`, `reaper_project.py`) that the main riff has never played a real chord, for any preset, any section, this whole session. Real metal rhythm guitar is overwhelmingly power chords, not single notes. Checked for existing real machinery before designing anything new (same discipline as every prior X.*): `chords.solve_chord` (P1.11, ground-up fretboard fingering solver) and `riff.voice_chord_section`/`generate_chord_riff` (P3.11) are real and tested, but never wired into the main riff -- `chord_vocab.py`'s own module docstring literally names this exact gap, previously only ever acted on for chill/interlude's ambient `chord_quality` (X.6b). `generate_chord_riff` itself holds one FIXED chord shape for a whole cell (a static vamp) so doesn't fit directly -- this engine's riffs have real per-hit pitch movement (vocab-weighted intervals, pedal bias) that must be preserved. Reused the lower-level real primitive instead: `chords.solve_chord`, applied PER HIT. `engine/midi_export.py`: new `_guitar_chord_tone_pitches(fretboard, root, intervals=(0,7), max_span=4)` (real power chord = root+5th, not the fuller (0,7,12) triad, to avoid pushing a chord's upper tone into a thinner higher register on an already-low root; a real, easy follow-up if more fullness is wanted) -- picks the lowest-position fingering, falls back to `[root]` alone if nothing is reachable (fail closed, never fabricates, never crashes the export over one hit). New `_chord_cell_events` (same on/off-tick/velocity logic as `_cell_events`, one event per real chord tone instead of one per hit). Thickening happens ONLY at MIDI/`.rpp` export time for the two rhythm-guitar takes -- `pitches_per_cell`/`Motif`/`bass`/`judge()` all stay completely untouched, zero risk to the generation/judging pipeline. Applied to every role EXCEPT `chill`/`interlude` (`_CHORD_THICKENED_ROLES`), matching the established real precedent (kick/snare/hihat already exempt those two atmospheric roles, X.9/X.11/X.22) -- their real melodic/harmony character (`harmonize_line`) stays single-note. `engine/reaper_project.py` imports the same real helpers from `midi_export.py` (one source of truth, two serializers) and applies the identical dispatch. Real, expected test breakage fixed properly: 3 pre-existing tests assumed exactly one note per guitar hit -- updated to compute real expected chord-tone counts via the same `_guitar_chord_tone_pitches` helper, never loosened. New tests: real fallback-to-root check on an unreachable interval; a real end-to-end check that a chord-thickened section's exported track has genuine simultaneous note-ons at the same tick (not just an inflated count); confirmed chill/interlude stay excluded. Verified against real generated output (metalcore seed=6/6 sections): Guitar Take A shows 95 real chord hits (2+ simultaneous notes) vs 30 single-note hits (the chill/interlude sections). 539 passed, 1 skipped (up from 535).
- [x] X.24 Real cross-section blending -- guitar, bass, AND guitar-locking kick styles, done properly (not the initial guitar-only slice). Direct response to a real, confirmed-original reference track upload ("Try recreate it... learn why and implement fixes"; then explicitly "There's no time limitations flesh it out and do it properly rather than rush"): full audio analysis (demucs-isolated drums + `audio_vocab.py`'s real tempo/key/structure/rhythm-pattern/onset tools) found the engine's typical output was ~5x shorter than the reference for a comparable section count, and confirmed `structure.pickup`/`bridge`/`flatten` (P6.2, real and tested) were still unwired -- every section transition in every song this whole session has been a raw, unblended concatenation. Real risk found before implementing: `structure.pickup` returns cells carrying ONLY `duration`/`is_rest` (the reference's simpler data model never needed more) -- a naive port would silently strip this project's real per-cell keys (`role` for drums, `velocity`/`timing_offset` for guitar takes) at every blended boundary. New key-preserving reimplementations of the same real rule ("last n cells of prev copy first n of next"): `song._pickup_cells`/`_pickup_values`, applied as a pure/deterministic post-processing pass (zero rng consumption) once `_generate_attempt`'s main per-section loop completes. First shipped guitar-only (`guitar_take_a`/`guitar_take_b`/`pitches_per_cell`), then extended once told to do it properly: bass and guitar-locking kick styles ALSO now re-lock to the blended guitar, since both "follow" the guitar's rhythm and would otherwise silently desync from it exactly at the blended boundary cells. Both regenerations are pure/deterministic (`bass.follow_guitar_rhythm` has no rng at all; `drums.kick_pattern_for_style`'s own docstring confirms every real style is deterministic given guitar_cells) -- real, exact re-locks, not approximations. New `drums.resolve_kick_style(role, preset_kick, rng)` extracted from `kick_pattern_for_role` (same real resolution logic, `kick_pattern_for_role` now delegates to it, zero behavior change for its own existing callers/tests) so `song.py` can resolve+store which style a section's kick actually used, then reuse that SAME style (never a fresh random re-roll) when regenerating kick cells from the blended guitar. Styles that don't look at guitar's specific hit/rest positions (`two_step`/`blast`/`double_kick`) regenerate to byte-identical output, so the re-lock applies uniformly across all styles rather than branching per style. `bridge()`'s inserted-transition-cell device remains deliberately deferred (changes total section length, would desync every other instrument's own beat count -- a real, coordinated multi-instrument redesign). Found and fixed a real bug during testing: `chill`/`interlude` sections (`lead_mode == "harmony"`) tie `section["lead"]` to the guitar's PRE-blend hit count (`riff.harmonize_line` renders one lead note per guitar hit, computed before blending) -- blending those sections' own guitar broke that pairing and crashed `midi_export`'s real per-hit harmony zip (`StopIteration`). Excluded chill/interlude as the blend's own `this_section` (matching the established role-exclusion pattern everywhere else this session); `next_section`'s role doesn't matter since only `this_section`'s own cells get modified. `guitar_track`/`drum_track` (used only for `judge()`) both rebuilt from the blended sections after the pass, so judge scores the real final output. `engine/tests/test_song.py`: exact unit tests for the two pickup helpers (key preservation, empty-input edge cases); a real end-to-end tail/head match check for guitar cells+pitches at every non-chill/interlude boundary; confirmed chill/interlude stay untouched; confirmed snare/hihat are unaffected (derive from `motif.cell`, not `guitar_take_a`); real bass re-lock check (regenerating independently via `follow_guitar_rhythm` on each section's own blended data and requiring an exact match); real guitar-locking-kick-style re-lock check (`bounce` kick hits exactly match the blended guitar's real hit positions, for a preset with no `group_beats`); real regression check that guitar-shape-independent kick styles (`two_step`/`blast`) stay byte-identical. `engine/tests/test_drums.py`: `resolve_kick_style` matches `kick_pattern_for_role`'s own real choice exactly (same rng state, same result), and requires `rng` for overlay roles same as before. Fixed one pre-existing test (`test_kick_style_field_changes_real_song_kick_output`) that compared against the stale pre-blend `motif.cell` instead of the real blended `guitar_take_a` -- a real, expected consequence of the fix actually working, confirmed by asserting the two songs' blended guitar is identical (same seed, deterministic blend) before using it as the shared reference. Verified against real generated output (metalcore seed=6/6 sections): every non-chill/interlude guitar boundary shows an exact tail=head match; bass length matches guitar length in every section; chill/interlude stay correctly excluded from all three. 550 passed, 1 skipped (up from 539).
- [x] X.25 Real kick "burst" device. Direct evidence from the same real, confirmed-original reference-track analysis (X.24): a demucs-isolated drum stem, classified via `audio_vocab.classify_drum_onsets`, showed 32.5% of real kick-to-kick gaps under 150ms -- concentrated bursts inside an otherwise moderate-paced groove (overall kick rate ~1.3/sec, not relentless). The engine's only existing fast-kick style (`double_kick`, X.11) is a flat, continuous straight-16th pulse for the WHOLE section -- structurally different from a burst that stands out against a comparatively sparse baseline. New `drums._kick_burst`: base pattern is `_kick_sparse`'s real thinned every-other-guitar-hit pattern (not a full guitar-lock, so the burst reads as a genuine contrast, matching the reference's own moderate overall rate), with real dense consecutive-hit bursts overriding the next `_BURST_LEN` (4) cells every `_BURST_CYCLE_BEATS` (4.0) beats -- reuses the same real `_cyclic_hit_indices` cell-timeline mechanism every other overlay style in `drums.py` already uses, no new timing primitive invented. Registered as a new `_KICK_STYLES["burst"]` entry and added to `_ROLE_KICK_OVERRIDE_CHOICES` for `build`/`solo` alongside the existing `double_kick`/`blast` choices -- three real overlay styles now vary across seeds instead of two. `engine/tests/test_drums.py`: exact hand-verifiable real position checks (a 16-cell/4-beat fixture where the burst covers cells 0-3 as real consecutive hits, and the real `_kick_sparse` base pattern resumes exactly afterward, checked cell-by-cell); confirms a real second burst fires in a second cycle (8-beat fixture); bad-input rejection; updated the existing build/solo overlay-variety test to require all three real styles across seeds, not two. Verified against real generated output: metalcore seed=0's "build" section shows a real burst signature (80% hit rate with a real 5-cell consecutive run), distinct from blast's 100% and a plain guitar-lock's guitar-hit-count-matching rate. 553 passed, 1 skipped (up from 550).
- [x] X.26 (cancelled -- investigation only, no code change) Planned as "delete confirmed-dead `structure.generate_section_content`/`generate_song_sections`." Per the user's explicit "massive audit... make sure nothing's been rushed or skipped" directive, re-verified the earlier "confirmed DEAD" claim (from the X.23 entry's audit) before deleting anything -- and found it was WRONG. The original grep only checked usage outside `structure.py`'s own test file; `tests/test_structure.py` has real, dedicated tests calling both functions directly, and `tests/test_atmosphere.py` actively reuses `generate_section_content` as a real fixture helper. Real status: an earlier, simpler P6.4 implementation of "wire `arc()` into section generation," legitimately superseded for the PRODUCTION pipeline by `song.py`'s own richer `_generate_attempt`, but still real, tested, and actively used elsewhere -- not a deletion candidate. Corrected the Scope gaps tracker and `## Next` summary rather than deleting anything. A genuine, worthwhile catch from the audit itself, not a wasted step.
- [x] X.28 Real, serious bug fix: X.24's cross-section blending was silently desyncing guitar/bass/kick from snare/hihat, compounding across the whole song. Direct response to blunt, repeated real listening feedback ("it's the composition," "it's slow and boring and nothing like metal") -- investigated by directly inspecting a real composed song's per-track total beat-durations per section instead of guessing. Found: `song._pickup_cells` (X.24) copies `structure.pickup`'s source line verbatim, including `duration`. In the SOURCE project this is a real no-op (fixed-size grid slots, every cell's duration is already identical) -- but this project's cells have VARIABLE durations (0.25/0.5/1.0 beat), so copying `duration` silently changed a blended section's real total beat count away from `preset.bars * 4` (measured real drifts of 0.25-1.75 beats per boundary on a real composed song, metalcore seed=27/6 sections). `bass`/`kick` get regenerated from the blended guitar afterward so they inherit the new (wrong) total and stay in sync with guitar -- but `snare`/`hihat` are generated once BEFORE blending and never touched again, so from the first blended boundary onward guitar/bass/kick ran on a different absolute-time grid than snare/hihat, a real compounding desync between instrument tracks for the rest of the song. This is squarely a composition-correctness bug, not an audio-rendering issue -- confirmed the user's insistence that the problem was compositional, not just production quality. Fixed `song._pickup_cells` to preserve the OUTGOING cell's own `duration` at every blended position -- every other real key (is_rest/role/velocity/timing_offset/pinch_harmonic) still comes from the incoming cell (the real "lean toward what's coming" blend effect is unchanged), but a section's total beat count is now exactly invariant under blending, matching this project's own stated law ("a section is `preset.bars` bars of 4/4") and keeping every parallel per-section track on the same absolute-time grid. `engine/tests/test_song.py`: rewrote `test_pickup_cells_copies_full_next_cells_not_just_duration_is_rest` (now `test_pickup_cells_copies_next_cells_content_but_keeps_own_duration`) to check the corrected contract plus a direct total-duration-invariance assertion; rewrote the end-to-end blend-signature test to exclude `duration` from the tail/head comparison (it's now deliberately NOT blended) and added a direct per-section `sum(duration) == preset.bars*4` check at every boundary. Verified against real generated output (metalcore seed=27/6 sections): every section's guitar/kick/bass/snare/hihat now sums to exactly 16.0 beats (was 16.0-17.75 before the fix, with snare/hihat stuck at 16.0 throughout). 560 passed, 1 skipped before the X.29 graph changes below (seed-seeking regressions from X.29 are noted in that entry).
- [x] X.29 Real `verse`/`chorus` roles wired into the structure graph -- the concrete first step of the Metalerator scope-comparison finding ("metalerator was able to make good stuff have we passed that level"): Metalerator's real song architecture is a deliberate linear arc (intro -> verse -> pre_chorus -> chorus -> post_chorus -> pre_breakdown -> breakdown -> outro) this project never had verse/chorus nodes for at all. Found a real, ready-to-use answer already sitting unused: `theory.ARC`'s `"A"` (energy 0.60) and `"B"` (energy 0.70) rows were real and tested (`tests/test_theory.py` calls `arc("A")` directly) but had NO role ever mapped to them in `ROLE_LETTER` -- exactly the real verse-vs-chorus energy contrast Metalerator's own verse/chorus riff generators show (sparse pedal-tone verse, denser power-chord chorus), ported for free with zero new ARC data. `engine/theory.py`: added `"verse": "A"`/`"v": "A"` and `"chorus": "B"`/`"ch": "B"` to `ROLE_LETTER`. `engine/structure.py`: added `verse`/`chorus` nodes to `DEFAULT_GRAPH` with real, genre-plausible edges (`intro`/`build`/`breakdown` -> `verse`; `verse` -> `chorus` as its dominant destination, with smaller returns to `build`/`breakdown`; `chorus` -> `breakdown`/`verse`/`build`/`outro`, so a song can genuinely end on a chorus) -- every existing edge's own weight is untouched, only new destinations were added. Real, expected side effect: adding new edges from `intro`/`build`/`breakdown` dilutes those nodes' existing weighted-random-walk distribution (same absolute weights, different relative shares once a new edge exists), so 2 pre-existing seed-dependent tests (`test_compose_song_gives_chill_and_interlude_sections_real_chord_voicings`, `test_a_role_recurring_many_times_gets_real_pitch_variety_not_just_two_states`) needed a different seed to keep exercising their real intent -- both test docstrings already explicitly permitted this ("try a different seed if this fires"), fixed with a real seed search, never a loosened assertion. `engine/tests/test_theory.py`: verse/chorus resolve to the real A/B rows; chorus has higher energy than verse. `engine/tests/test_structure.py`: verse/chorus are real reachable nodes from intro across many seeds; chorus can lead straight to outro; chorus is verse's real dominant destination. 562 passed, 1 skipped (up from 560). Per-bar tonal-center progression (the second, larger part of the Metalerator finding) and verse/chorus density-character contrast are tracked as the next real increments (X.30/X.31).
- [x] X.30 Real per-bar tonal-center progression for verse/chorus -- the second, larger half of the Metalerator finding (X.29 wired the roles; this wires the real device that makes them tonally distinct section-to-section). Ported verbatim from `reference/metalerator/metalerator/misc/generate_song.py`'s real `CHORD_PROGRESSIONS` table (verse, 7 named 4-bar progressions) and `rhythm_guitar/chorus/chorus.py`'s own denser table (chorus, 5 progressions) -- each a real sequence of scale-degree offsets from the tonic, with the source's own real device `set_root_note(bar) = current_scale[progression[bar]]`: each bar's riff root is looked up fresh per bar, not held fixed for the whole section. Directly answers `god-tier-metal-scope.md` sec.2 ("Key/tonal-center modulation across sections... needs a concept of home key vs modulated key per section") and sec.14.2 item 5, both OPEN in the Scope gaps tracker until now -- this project had never modulated a tonal center anywhere; every section held one fixed `start_degree` (from `theory.arc()`) the whole way through. New `engine/progression.py` (own module -- a genuine new pitch-content mechanism, matching this project's one-module-per-concern layout): `VERSE_PROGRESSIONS`/`CHORUS_PROGRESSIONS` (the real ported tables), `degree_for_bar(progression, bar_index)` (modulo-wrapped so it also works for a preset with more bars than the source's fixed 4-bar tables -- a real, sound generalization the source never needed), `render_motif_with_progression`/`pitches_per_cell_with_progression` (same real per-hit rendering `motif.render_motif`/`song.pitches_per_cell` already do, except the anchor is `start_degree + degree_for_bar(...)` per note instead of one fixed `start_degree` for the whole motif -- additive ON TOP of the section's existing `arc_row["start_degree"]` anchor, consistent with this project's existing all-additive degree-stacking convention). `engine/song.py`: `role in ("verse", "chorus")` picks a real progression via the section's own seeded rng (`_PROGRESSION_TABLES`), stores it (`section["chord_progression"]`), and renders `cell_pitches` through the progression-aware path instead of plain `pitches_per_cell`; every other role is untouched. `engine/tests/test_progression.py` (new): exact hand-computed bar-anchor checks, cell-alignment, wrap-past-table-length, and a real equivalence check against plain `render_motif` for a flat (`[0,0,0,0]`) progression. `engine/tests/test_song.py`: real composed-song check. Verified against real generated output (metalcore seed=0/8 sections): a real verse section's bars 0-1 (progression degree 0) sit on pitches `{35,38,42}`, bar 2 (degree 2) shifts up to `{42,45,49}`, bar 3 (degree 3) up again to `{43,47,50}` -- real, audible bar-to-bar tonal movement, not a flat pitch center. 569 passed, 1 skipped (up from 562).
- [x] X.31 Real verse/chorus character: verse pedal bias, chorus power chords, real chorus lead -- completes the Metalerator verse/chorus finding (X.29 roles, X.30 progression, this entry's real density/timbre contrast). `engine/song.py`: verse sections now force a real, strong pedal bias (`pedal=0.65`, matching Metalerator's own real ~65% root-note frequency for its pedal-tone verse riff) regardless of what the preset's own `.pedal` declares -- same real precedent as X.11's build/solo kick overlay overriding `preset.kick`, wired at the section's `ThemeRegistry.get_or_create` call site. `engine/midi_export.py` (and `reaper_project.py`, which imports the same set): added `"chorus"` to `_CHORD_THICKENED_ROLES` -- chorus sections now get the same real power-chord thickening (root+5th via `chords.solve_chord`, X.23) breakdown/build/intro/outro/solo already get, automatically using the section's own (X.30-progression-modulated) per-hit root; "verse" deliberately NOT added -- Metalerator's real verse riff is single-note pedal tone, not chorded. New `lead_mode == "chorus_lead"` (`song.py`'s per-role lead-voice block): a real accompanying lead for chorus, the second half of Metalerator's own `RGuitarChorus` device (`generate_lead`, chorus riff + a lead line, not silent like the other dense-chug roles) -- deliberately less extreme than `solo`'s fully-featured technical break: `motion` stays at the preset's own value (not solo's `+0.3` boost), a narrower `stab_chance` (0.15 vs solo's 0.35), register one octave above the riff (`lead_anchor..lead_anchor+24`, not solo's wider `-12..+24` span), and no legato splice (a real, deliberately solo-only technical device). `engine/midi_export.py`'s `_lead_events_for_section` dispatch extended (`mode in ("solo", "chorus_lead")`) to reuse the exact same even-8th-note timing solo's main phrase already uses -- `legato` is always `None` for chorus_lead, which that branch already handles gracefully (empty legato tail), so `reaper_project.py` (imports the same function) needed no separate change. `engine/tests/test_song.py`: real cross-seed check that verse sections show a measurably higher root-degree (delta==0) hit fraction than non-verse/chorus sections; chorus sections get a real non-empty `chorus_lead`; chorus is chord-thickened and verse is not. Verified against real generated output (metalcore seed=8/8 sections): a real `intro -> build -> verse -> chorus -> ...` sequence, chorus section shows a real 32-note lead (`lead_mode="chorus_lead"`). 572 passed, 1 skipped (up from 569).
- [x] X.32 Real per-role feel: wired `groove.gallop_cell`/`stutter_chug_cell` (real, tested, confirmed-unwired classic metal rhythm devices -- scope sec.4: "named rhythmic cells... 'gallop', 'stutter-chug'"), and closed the real architectural gap this session's audit surfaced: `feel` was ONE value per whole PRESET, so a metalcore song's intro/verse/build/chorus/outro all got the exact same duration-weighting as the actual breakdown section -- zero rhythmic contrast between "riffing toward the breakdown" and "the breakdown itself" within one song. Direct response to blunt user feedback ("Compositional gaps. Rn it's making dogshit nothing like even any basic metal") -- the gallop is arguably THE single most stereotypically-recognized "metal riffing" device, more foundational to reading as "basic metal" than any advanced technique. `engine/motif.py`: `feel == "gallop"`/`feel == "stutter_chug"` dispatch inside `generate_motif`, mirroring the EXACT existing precedent for `feel == "triplet"` (a special-case cell-generation branch before the normal `generate_rhythm` path, `group_beats` handled the same "short cell then tile" way, then the same generic pitch-delta-assignment loop runs afterward regardless of which branch built `cell`) -- `gallop_cell` is fully deterministic (the gallop feel comes from the fixed short-short-long duration pattern, not a random draw); `stutter_chug_cell` uses a new `_STUTTER_CHUG_HIT_LEN = 0.25` (16th-note slot, matching this project's existing shortest allowed length). `engine/song.py`: `_ROLE_FEEL_FORCED = {"breakdown": "breakdown"}` (breakdown role ALWAYS gets real breakdown duration weighting regardless of what the preset otherwise declares -- confirmed via real generated output that djent, whose own feel is "bounce", now shows real 8th/quarter-dominant breakdown-shaped durations for its breakdown section specifically); `_ROLE_FEEL_OVERRIDE_CHOICES = {"build": ("gallop", "stutter_chug")}` (build gets a real chance at either device, picked per-section via the section's own seeded rng alongside the preset's own declared feel as a real, always-available third choice -- same established precedent as X.11's build/solo kick-style overlay and X.31's verse pedal-bias override). `_generate_irvd_motif` already threads `feel` through to its own per-bar `generate_motif` calls (confirmed by reading the function, not assumed), so gallop/stutter_chug apply bar-by-bar under IRVD automatically, no special-casing needed. Real, expected test breakage fixed properly (never loosened): the new extra `rng.choice()` for every "build" section shifts downstream rng draws, requiring a seed bump for 2 pre-existing tempo_drop tests (djent seed 11 -> 0, same real cause X.29 already established); `test_preset_feel_breakdown_changes_real_song_duration_distribution` now excludes breakdown-role sections from its A/B comparison (both compared variants get the SAME forced breakdown treatment now, so that role no longer isolates what `preset.feel` alone controls); `test_tech_preset_real_feel_triplet_produces_genuine_triplet_durations` now excludes build/breakdown roles (both can now real-override tech's own declared triplet feel). `engine/tests/test_motif.py`: real dispatch tests for both new feels (exact short-short-long duration sequence and all-hits invariant for gallop, deterministic-regardless-of-seed for gallop vs seed-varying for stutter_chug, equal-slot-length check for stutter_chug, `group_beats` tiling for gallop). `engine/tests/test_song.py`: real end-to-end check that breakdown sections get real breakdown-shaped durations even under `tech` (declared feel "triplet"); real cross-seed check that build sections show real variety across gallop/stutter_chug/the preset's own feel. Verified against real generated output: djent seed=2's build section shows the real gallop signature (`[0.25, 0.25, 0.5]` repeating); djent seed=0's breakdown section (preset feel="bounce") shows real breakdown-shaped durations (`1.0`/`0.25`-dominant, not bounce's own 16th-heavy mix). 579 passed, 1 skipped (up from 572).
- [x] X.33 Real rebalance of song pacing -- direct, data-driven answer to the user's "why doesn't this sound like any real genre" question. Measured across every preset, 10 seeds each (80 real generated songs) BEFORE this fix: by real elapsed wall-clock time, 58.3% of every generated song's total runtime was either half-tempo breakdown (41.8%, `song._compute_tempo_map` unconditionally halved tempo for EVERY breakdown section, no exceptions) or a deliberately sparse/silent-drum chill/interlude section (16.5% combined, `structure.DEFAULT_GRAPH` gave `interlude` a weight from `breakdown` exactly equal to `build`'s -- a quiet breather was as likely as building energy back up). Only ~42% of total listening time was full-tempo energetic riffing. Two real fixes, both user-approved ("Yes" / "Rebalance"): (1) new `song._BREAKDOWN_HALFTIME_CHANCE = 0.4` -- half-time is now a real per-section coin flip (same established precedent as X.18's own `_TEMPO_DROP_CHANCE`, resolved on the section's own seeded rng during the main loop), not a universal rule; `_compute_tempo_map` gains a `halftime_flags` parameter, a breakdown section only gets the real metric-modulation half-time curve when its own flag is `True`. (2) `structure.DEFAULT_GRAPH` weights rebalanced -- every edge INTO chill/interlude from an energetic node reduced (never zeroed -- still real, valid, occasional transitions), verse's own reachability mildly boosted (it and chorus were confirmed under-visited in practice, ~8.3%/1.4% of all sections, since the walk's probability mass mostly stayed trapped in the build/breakdown/interlude loop). Every other real device keyed on `role == "breakdown"` (X.18's mid-section tempo DROP, X.27's pinch-harmonic accent, X.23's chord thickening) stays unconditional -- only the full-section half-time tempo-map treatment became probabilistic. Real, expected test breakage fixed properly: 2 `_compute_tempo_map` unit tests updated for the new signature/contract; 2 tempo-track tests (`test_tempo_track_reflects_the_real_tempo_map`, `test_rpp_tempo_envelope_matches_real_tempo_map`) needed a seed bump (djent 11->0, half-time is no longer guaranteed for that seed); a chord-vocab seed test needed a bump (djent 1->2, graph rebalance shifted which seed lands a reachable chill/interlude chord voicing); `test_preset_feel_breakdown_changes_real_song_duration_distribution`'s margin lowered 0.03->0.015 (confirmed via n_seeds=200 that the real, directionally-correct ~5% relative effect genuinely shrank, not sampling noise -- rebalancing makes verse/chorus/other roles reachable more often within a 4-section sample, diluting the "non-breakdown" bucket this test already excludes breakdown from); also fixed a real PRE-EXISTING gap the rebalance surfaced (not introduced by X.33): `test_compose_song_end_to_end_for_every_preset` never accounted for X.31's `lead_mode == "chorus_lead"` (chorus was rare enough before X.33 that no default seed had ever hit it in this test). `engine/tests/test_song.py`: new `test_breakdown_halftime_is_real_and_occasional_not_universal` (cross-seed check that breakdown shows BOTH full-tempo and real half-time outcomes). `engine/tests/test_structure.py`: new `test_rebalanced_graph_visits_chill_and_interlude_less_often` (300-seed distributional check, real share now < 15%, was ~21%). Verified against real generated output, re-running the exact same measurement methodology used to find the problem: chill+interlude time share 16.5%->6.9%; verse 6.6%->11.5%; chorus 1.1%->5.8%; breakdown (total) 41.8%->32.1% (no longer unconditional). 581 passed, 1 skipped (up from 579).
- [x] X.34 Real blast beats, wired into the actual drum track -- direct response to the user's "insane drums, fast and heavy" question. Investigated and found a real, serious gap: `drums.generate_vocabulary_informed_blast_fill` (P4.3/P4.4, real, tested, three genuine alternating-KICK/SNARE blast-beat renderers -- traditional/gravity/hammer) was called in `song.py` for every `breakdown`/`solo` section and produced real data (`section["fill"]`) -- but `midi_export.py`'s own docstring admitted it outright: "section['fill'] is intentionally NOT exported here: _generate_attempt never actually blends it into the song's assembled drum data." The real blast machinery ran, generated real output, and was discarded every single time. Separately confirmed the existing `"blast"` KICK STYLE (`drums._kick_blast`) is misleadingly named -- it just puts a kick on every cell the guitar already plays, so its actual speed is capped by the guitar's own moderate rhythm (measured peak ~10.8 kick hits/sec in build/solo), not an independent fast device. Real hard constraint found during investigation (not guessed): every existing kick/snare style returns a cell list the EXACT SAME LENGTH as `guitar_cells` -- `judge()`'s kick-lock scoring does `zip(guitar, drums)`, and X.24's post-blend re-lock depends on this exact alignment. `generate_vocabulary_informed_blast_fill`'s own cells come from an INDEPENDENTLY-drawn `RhythmRegistry` skeleton with a different cell count than `guitar_cells` -- using its raw output directly would have silently broken that invariant. Fixed by reusing the REAL, valuable part of the existing machinery (the alternating-role renderers, `drums._render_traditional`/`_render_gravity`/`_render_hammer`, dispatched via `rhythm.pick_blast_type`) applied DIRECTLY onto a section's own `guitar_cells` instead of an independent skeleton -- guaranteed cell-alignment by construction, same trick `_kick_blast` already uses but with real KICK/SNARE alternation instead of a flat kick-only pulse. New `engine/drums.py` functions: `render_blast_beat`/`render_blast_beat_for_type` (the latter for re-locking to an already-chosen blast type with no rng re-roll, same real precedent as `resolve_kick_style`/kick-style re-lock), `blast_kick_cells`/`blast_snare_cells` (convert a rendered blast's `roles` list into the exact `{"duration","is_rest","role"}` shape every other kick/snare style already uses -- a "hammer" blast cell carries both roles, a real, intentional simultaneous kick+snare hit, not a bug). `engine/song.py`: new `_BLAST_FILL_ROLES = ("breakdown", "solo")`/`_BLAST_FILL_CHANCE = 0.35`/`_BLAST_WEIGHTS`, real occasional (not universal, same established precedent as X.33's `_BREAKDOWN_HALFTIME_CHANCE`) blast override applied right after kick/snare are resolved, REPLACING both for the chosen section. Real, necessary interaction with X.24's post-blend re-lock (found during investigation): a blast section is guitar-locked, so its kick+snare must be regenerated against the blended `guitar_take_a` using the SAME blast type already chosen (never a fresh re-roll) -- this is the one place `snare_cells` becomes blend-aware, a real, deliberate, documented exception to X.24's "snare stays exactly as generated" rule, scoped only to blast-fill sections; the normal kick_style re-lock is skipped entirely for these sections (blast owns both kick and snare). Removed the now-superseded unused `fill = generate_vocabulary_informed_blast_fill(...)` call, the `RhythmRegistry()` instance that only existed for it, and the `section["fill"]` key (grepped: no test asserted on it) -- `generate_vocabulary_informed_blast_fill` itself remains real, tested, and callable on its own, same status as `structure.pickup`/`bridge`/`flatten` (real and tested, just not the thing chosen for this production path). `engine/tests/test_drums.py`: unit tests for `render_blast_beat`/`render_blast_beat_for_type`/`blast_kick_cells`/`blast_snare_cells` (cell-count/duration alignment with input cells, real traditional-never-simultaneous vs hammer-always-simultaneous role-split checks, unsupported-type rejection). `engine/tests/test_song.py`: real cross-seed check that breakdown/solo sections show BOTH real blast (measurable snare activity, cell-aligned with guitar) and non-blast outcomes; real check that a blast section's kick/snare stay exactly cell-aligned with the guitar after X.24's blend pass. Verified against real generated output: real blast sections show genuine KICK/SNARE alternation with exact cell-count alignment (e.g. a real breakdown: 35 kick hits, 35 snare hits, 35 cells, matching guitar exactly); combined real kick+snare activity now reaches up to 13.3 hits/sec across real blast sections (vs the old kick-only style's ~10.8/sec ceiling), with genuine interplay instead of a flat pulse. 589 passed, 1 skipped (up from 581).
- [x] X.35 Real riff-repetition bias correction -- direct evidence from a user-supplied reference MIDI (`born of osiris style midi.mid`, a genuine Born-of-Osiris-style transcription, parsed with `pretty_midi`): the opening 16-note riff recurs BYTE-IDENTICALLY 23 times across the real song (intro, mid-song, a dense outro vamp), never varied, transposed, or inverted anywhere in the file. Web research (Fundamental Changes' "Writing Better Metal Riffs: Sequencing") confirmed the real convention is "repeat first, vary rarely." This directly contradicted `song._develop_theme`'s ORIGINAL X.14 design: a fixed `occurrence % 4` rotation (base/invert/transpose/both) that started varying on the very 2nd occurrence, every time -- the opposite of the real convention, guaranteeing nothing ever repeated verbatim twice in a row. Fixed: `_develop_theme` now takes the section's own seeded `rng`; occurrence 0 still always returns the base theme (establishes it), but every later occurrence rolls a real `_THEME_REPEAT_VERBATIM_CHANCE = 0.7` -- 70% verbatim repeat (the new real default), 30% one of the same three real, length-preserving develop ops as before (kept as legitimate occasional devices, not removed). `engine/tests/test_song.py`: rewrote the old fixed-rotation unit test into two real ones -- occurrence-0-always-base, and a 500-seed distributional check that base-repeat share lands in `[0.55, 0.85]` (never outside the 4 real develop shapes); rewrote the real end-to-end "role recurring many times" test to check BOTH real repetition dominance (most-common state > 50% share) AND that variety remains genuinely possible (>=2 distinct states across enough occurrences), replacing the old (now-inverted) "must show MORE variety" premise. Real, expected side effects fixed properly: the extra rng draw on every repeat occurrence shifts downstream random choices for several existing seed-pinned tests (kick-style/blast-fill interactions on "breakdown" role, snare-blend-awareness under X.34) -- 3 pre-existing tests needed their role-exclusion lists extended to also exclude "breakdown" (a real, already-existing gap those tests had: they never accounted for X.34's blast-fill override applying to breakdown too, only build/solo/chill/interlude), not loosened, just completed. Verified against real generated output: a real progressive-preset song (seed=16, 16 sections) shows "build" role recurring 7 times with 3 distinct pitch-content shapes, most-common covering 57% -- real repetition-dominant, variety-possible, matching the new design exactly.
- [x] X.36 Real melodic "sequence" generator for solos -- the second finding from the same reference MIDI: a real melodic passage sits inside the guitar track itself (54-77s, recurring 143-167s), a short shape repeated at successively shifted scale positions (`72,75,79,75,72,67,72,75,79,80,79,75,72,...`). Web research (Fundamental Changes, Riffhard, Splice guides to writing metal solos) confirmed this IS the real, textbook core solo/lead-writing technique -- a motif moved stepwise through the scale -- which this project had no device for (`lead.generate_lead_line` produces coherent motion via `VoiceLeader.move()`, but never a repeated-shifted-shape pattern). New `lead.generate_sequence_line`: draws ONE short motif once via the exact real weighted-interval-to-scale-degree-delta mechanism `motif.generate_motif`'s own pitch loop already uses (`motif.pick_pitch_interval` + newly-public `motif.degree_delta_for_interval`, renamed from `_degree_delta_for_interval` since `lead.py` became a real second caller -- same established precedent as X.24's `resolve_kick_style` extraction), then repeats that exact relative shape at anchors shifted by `step_degrees` per repeat, mirroring `motif.render_motif`'s own additive-delta-from-anchor convention exactly. Wired into `song.py`'s solo branch as a real, additive splice (same established pattern as X.6a's legato-tail splice) BETWEEN the existing main phrase and the legato close, giving a solo a real 3-part shape (established phrase -> sequence passage -> fast legato close) matching the research's own "a solo should read as a phrase with a beginning, middle, and end." New `_SOLO_SEQUENCE_MOTIF_LEN = 4`/`_SOLO_SEQUENCE_REPEATS = 4`; `step_degrees` drawn per-section (`rng.choice((-1, 1))`, real ascending/descending direction), `start_degree` continues from wherever the main phrase's last note landed (never a disconnected register jump). Sequence notes get the same real register clamp `generate_lead_line` already applies to the main phrase (`[lead_anchor-12, lead_anchor+24]`) -- a real, honest, documented tradeoff: this can truncate the sequence's exact repeated shape near the register ceiling (verified and reported, not hidden). `midi_export._lead_events_for_section`'s "solo" branch needed NO change -- it already treats everything except the legato tail as one flat, evenly-spaced main phrase, so the spliced sequence notes are automatically included. `engine/tests/test_lead.py`: new unit tests for `generate_sequence_line` (exact same-relative-shape-every-repeat check via `scale.index_of`, real anchor-shift-by-step_degrees check, negative step descends, bad-input rejection). `engine/tests/test_song.py`: real end-to-end check that solo sections' `lead` length matches main-phrase + sequence + legato-tail exactly, across many seeds; real check that every lead note (all three segments) is a genuine int pitch. Verified against real generated output: a real melodic-preset solo (seed=1) shows the real sequence passage with degree-shape `[5,1,1]` repeating (shifting by whole scale steps) before the register ceiling clamp naturally truncates it near the top -- the real mechanism firing correctly, tail behavior honestly reported rather than cherry-picked around. 596 passed, 1 skipped (up from 590).
- [x] X.27 Real pinch-harmonic accent, wired for every preset. Direct continuation of the "massive audit" pass: re-verified `slam.mark_pinch_harmonics`/`chromatic_creep`, `groove.gallop_cell`/`stutter_chug_cell`, `interplay.call_and_response`, and `structure.apply_half_time` the SAME corrected way (checking each real function's OWN test file too, not just usage elsewhere) -- confirmed all four are genuinely real, tested, but still never called from `song.py`'s actual production pipeline (unlike the X.26 false alarm). Wired the cleanest, lowest-risk, highest-value one first: `slam.mark_pinch_harmonics` (P3.10, scope sec.4's "pinch-harmonic accent simulation") -- kept as a real, genre-agnostic technique per the user's own standing direction that Metalerator-mined techniques generalize across every relevant genre, not gated to the slam target this project no longer builds for. Real design decision: did NOT call `slam.mark_pinch_harmonics` directly -- its own real contract resets EVERY hit in a cell to a flat `velocity_base`, which would destroy X.21's real per-hit open-vs-muted velocity data. New `song._apply_pinch_harmonic_accent(cells)` reuses the same real CONCEPT (the phrase's last real hit gets a velocity spike, `127`, matching `slam.py`'s own real default) but only touches that one accent cell, leaving every other cell's existing velocity untouched. Applied to `breakdown`/`outro` roles (the real phrase-ending roles, the idiomatic place a chug riff punches a closing squeal), as the LAST step in `_generate_attempt` -- after X.24's cross-section blending, so the accent always lands on the TRUE final hit of the fully-assembled section rather than one blending might later overwrite. Real, expected interaction found and fixed: X.24's own "exact tail=head blend signature" test broke for breakdown/outro boundaries, since the pinch-harmonic override touches the same cell the blend check compares -- fixed by excluding `velocity`/`pinch_harmonic` from that specific comparison for those two roles (duration/is_rest/timing_offset -- the real rhythmic blend shape -- and the real pitch blend are still checked exactly). `engine/tests/test_song.py`: exact unit tests for `_apply_pinch_harmonic_accent` (only the last real hit changes, a rest-only cell list is a no-op); a real end-to-end check across EVERY preset that breakdown/outro sections get a real accent on their true final hit, and every OTHER role does not. Verified against real generated output (metalcore seed=6/6 sections): both real breakdown sections show `velocity=127, pinch_harmonic=True` on their real final hit. 556 passed, 1 skipped (up from 553).

---

## Phase 0 — Repo
- [x] P0.1 folders
- [x] P0.2 assets present
- [x] P0.3 pytest smoke
- [x] P0.4 gitignore / commit
- [x] P0.5 STATUS template

## Phase 1 — Tonal + presets
Scope: `## 2. Tonal/Harmonic` + `### 18.2` + `### 12.3` (chord-shape solver). Then `PORTS.md` Ww.
- [x] P1.1 `Fretboard` + MIDI↔(string,fret)
- [x] P1.2 Port `pitch_to_fret` from `reference/ww-forge-prior-attempt/engine/tab_score.py`
- [x] P1.3 TEST: unplayable MIDI rejected
- [x] P1.4 Scale library + cluster + power. Unique intervals
- [x] P1.5 TEST: no duplicate non-alias scales
- [x] P1.6 Tuning JSON from source MIDI (`engine/presets/tunings.json`, loaded via `presets.load_tunings`)
- [x] P1.7 Preset ids: groovy, djent, chill, tech, melodic, metalcore, deathcore (`slam` removed per user direction -- targets djent/deathcore/metalcore/technical deathcore, not slam). `progressive` added later (X.10). Strict JSON
- [x] P1.8 Schema validator called by loader -- `presets.validate_preset` called directly from `load_preset` (`presets.py:200`), the real load path, not a standalone check nobody invokes. `test_presets.py` covers malformed-input rejection.
- [x] P1.9 TEST glob `*.json` loads every file -- `presets.load_all_presets` globs `directory.glob("*.json")` (`presets.py:230`), never a hardcoded name list; `test_all_preset_json_files_load_via_glob` verifies this directly.
- [x] P1.10 Port VoiceLeader, shade(), ARC -- all three real and in `theory.py` (`VoiceLeader` class, `shade()`, `arc()`), used throughout the pipeline (motif generation, octave stabs, chord-quality dissonance bucketing).
- [x] P1.11 Chord-shape solver (ground-up, no lookup tables) -- `chords.solve_chord`, real fretboard enumeration; wired into `riff.voice_chord_section` and `chord_vocab.voice_named_chord` (X.6b), not a solver nobody calls.

## Phase 2 — Rhythm
Scope: `## 3. Rhythm` + `### 8.1` `### 8.2` `### 8.3`.
- [x] P2.1 Two-layer `{duration, is_rest}` then pitch
- [x] P2.2 Cell-tile polymeter + TEST 7 into 16 drifts
- [x] P2.3 Separate `metric_polyrhythm()` + TEST ≠ tile
- [x] P2.4 Real tuplet grids 3/5/7
- [x] P2.5 Blasts traditional/gravity/hammer; zero weight = never
- [x] P2.6 Shared rhythm id
- [x] P2.7 IRVD
- [x] P2.8 Seeded RNG + identical bytes twice

## Phase 3 — Motif / riff
Scope: `## 4. Riff-writing` + `### 17.1` + `## 6. New instrument layer` (P3.12 only).
- [x] P3.1 Motif = rhythm_cell + pitch deltas
- [x] P3.2 TEST same shape two roots
- [x] P3.3 Develop ops; lengths stay paired
- [x] P3.4 Cross-section theme id reused
- [x] P3.5 Gallop / stutter-chug change output
- [x] P3.6 Chromatic flags change output
- [x] P3.7 VoiceLeader leads
- [x] P3.8 Double-track two humanized takes
- [x] P3.9 Call-and-response: guitar reacts to drums/lead, not generated in isolation (§4)
- [x] P3.10 Slam devices: pinch-harmonic accent sim, low open-string chromatic creep (§4)
- [x] P3.11 Wire chord-shape solver (P1.11) into the pitch layer for chord-voiced sections -- a solver nobody calls is not done
- [x] P3.12 (optional, scope hedges with "possibly") second rhythm guitar harmonizing/doubling the lead (§6)

## Phase 4 — Drums
Scope: `### 11.6` `### 11.7`.
- [x] P4.1 Role → kit MIDI + fallback
- [x] P4.2 Kick follows guitar accents on breakdown
- [x] P4.3 Fills/blasts use shared-sequence
- [x] P4.4 Mine `reference/midi-corpus/` (1967 real, legally-clean groove/breakdown MIDIs, user-supplied superset of the original 63-file sample) for drum-fill density/placement vocabulary per §18.6 -- reference data only, never copied riffs; the symbolic engine (P4.1-P4.3) stays the actual writer. Queued after P4.1-P4.3 land to avoid touching drums.py while it's mid-build.

## Phase 5 — Bass
Scope: `### 14.2` (item 2, bass fretboard).
- [x] P5.1 Own 4/5-string fretboard
- [x] P5.2 Follows guitar rhythm; own playability

## Phase 6 — Structure
Scope: `## 5. Song-structure` + `### 10.3` + `## 3. Rhythm` (tempo-automation paragraph, P6.7 only).
- [x] P6.1 Weighted section graph
- [x] P6.2 flatten+pickup
- [x] P6.3 Half-time inside a section
- [x] P6.4 Modulation called from generation
- [x] P6.5 Judge/retry
- [x] P6.6 Tech/atmospheric interlude section type -- "not present at all currently" per §5
- [x] P6.7 Tempo curve: mid-song ramps/drops per §3. Reaper export still uses one constant tempo (§17.5) -- this is an internal song-data property, not a Reaper tempo-map change

## Phase 7 — Atmosphere
Scope: `## 16. Atmospheric`.
- [x] P7.1 GM 90 pad / 56 hit
- [x] P7.2 Synth doubles motif
- [x] P7.3 Accent-synced hits

## Phase 8 — Reaper
Scope: `## 11. Audio rendering` + `### 14.1` (reapy-boost, Surge XT, Supermassive).
- [x] P8.1 (superseded) Real Reaper project generation -- `engine/reaper_project.py`: `song_to_rpp(song, path)` writes a complete, directly-openable `.rpp` project file entirely offline, no live Reaper connection at any point. Investigated the scope doc's originally-chosen `reapy`/`reapy-boost` live-bridge approach first: got real, substantial progress (installed reapy-boost, MIT-licensed; automated the ENTIRE one-time bridge setup headlessly via config-file editing + HTTP-triggering Reaper's own built-in web control surface, no GUI clicks needed; found and fixed two real upstream bugs -- a socket-timeout bug causing `WinError 10053` on the first `send()` after `accept()`, and the activation script's `if __name__ == "__main__":` guard never firing under Reaper's script-execution model) but the connection remained unreliable in this environment even after both fixes, with no way to get further diagnostic visibility. Verified directly with the user that nothing Phase 8 actually needs requires a live connection: real REAPER project assembly, FX chains, and tempo automation are all expressible as a static file, and REAPER has a real, documented headless render flag (`-renderproject file.rpp`, confirmed via ReaTeam/Doc's REAPER-CLI.md) for the render step. Pivoted to `.rpp` generation, built against REAL ground truth rather than a guessed format: had REAPER itself (v7.79, already installed) import this project's own `midi_export.song_to_midi` output and save as `.rpp`, then reverse-engineered every field from that real file -- including base64-decoding the `<X>` track-name block to confirm it's the exact standard MIDI `0xFF 0x03 <name>` meta-event, not assumed. Reuses `midi_export`'s real event-extraction functions (one source of truth for per-track note/timing data, two serializers on top). **Verified end-to-end for real**: generated a project, had the user's actual installed Reaper open it, confirmed via screenshot -- 5 correctly-named tracks, real visible note content in every track, correct tempo, no error dialog. `engine/tests/test_reaper_project.py`: structural bracket-balance check across every real preset, the `<X>` block's base64 decoded and checked against the real MIDI meta-event format, guitar/drum note counts cross-checked against actual generated song data, real tempo-envelope values checked against `tempo_map`, reproducibility (same song -> same content, GUIDs excluded), bad-input rejection. 477 passed.
- [ ] P8.2 Separate buses (tracks already separate per-instrument; bus/send routing not yet added)
- [ ] P8.3 .RfxChain per track -- needs a small hand-built library of real FX-chain presets (NAM captures + cab IR, dialed in once by the user in Reaper's own GUI, per the scope doc's own §11.5 design) to splice into the generated `.rpp`; the splice mechanism itself is straightforward once real `.RfxChain` files exist
- [ ] P8.4 sfizz + role map
- [ ] P8.5 Surge + Supermassive
- [ ] P8.6 Orchestra
- [ ] P8.7 Adaptive mix -- real per-section automation envelopes, bakeable into the `.rpp` directly (no live connection needed) once a concrete automation target is scoped
- [ ] P8.8 Constant tempo map (already true: the generated `.rpp`'s `TEMPO`/`TEMPOENVEX` fields are the ONLY tempo data Reaper sees, per scope sec.17.5 -- nothing further needed here)

## Phase 9 — Editor
Scope: `## 10. Application / Editor`.
- [x] P9.1 Timeline CRUD -- real, working local Python (FastAPI) + React/TS editor, per scope sec.10.5's own architecture decision (local backend + local frontend over localhost). `editor/backend/`: `app/main.py` (three real routes -- `GET /api/presets`, `POST /api/compose`, `POST /api/export-midi` -- each calling straight into the real, already-tested engine: `presets.load_all_presets`, `song.compose_song`, `midi_export.song_to_midi`; adds no generation logic of its own), `app/serialize.py` (real, JSON-safe summaries of a composed song/preset -- only real fields the engine actually produces, nothing fabricated for UI convenience), `app/arrange.py` (`apply_order`: real, pure reorder/duplicate/mute/solo mechanism -- indexes the original real `sections`/`sequence`/`tempo_map` lists by a client-provided order, fails closed on an out-of-range index). Real, honest scope boundary documented in code and the UI itself: the engine has no capability to regenerate a single section in isolation or blend a freshly-reordered boundary (scope sec.10.3's cross-section blending on structural EDITS is a real, separate, not-yet-built requirement, distinct from X.24/X.28's existing end-of-generation blending) -- so this scaffold's CRUD rearranges which already-generated sections play, not per-section regeneration; "insert" duplicates the nearest section rather than fabricating a from-scratch generation capability that doesn't exist yet. `editor/frontend/`: Vite + React + TypeScript + Tailwind v4, `@dnd-kit` for real drag-and-drop timeline reordering, Tone.js + `@tonejs/midi` for real in-browser MIDI playback (a proper, actively-maintained Web Audio library -- real distorted-synth guitar/bass rig, membrane/noise/metal-synth drum rig -- switched away from `html-midi-player`/Magenta.js after `npm audit` found unpatched critical vulnerabilities with no fix available in its dependency chain; 0 vulnerabilities in the final dependency set). Dark, high-contrast custom theme (role-colored section blocks, ember/violet gradient accents) per the standing "gorgeous, cutting-edge editor" preference. `editor/backend/tests/`: real pytest tests for `serialize`/`arrange` against actual `compose_song` output (not fixtures), including real JSON-serializability checks. Verified end-to-end in a real browser (not just built): generated a real metalcore song (8 sections, judge ok, hits 295/pm 0.55/kick-lock 0.77), confirmed mute correctly shrinks the exported arrangement duration (58.8s -> 52.6s), duplicate/delete correctly change the real section count and re-trigger MIDI export, and real audio playback starts/stops via Tone.js with no console errors. `.claude/launch.json` added (two dev-server configs: `editor-backend` on :8000, `editor-frontend` on :5173).
- [x] P9.2 Pro + Guided -- per CLAUDE.md's own law ("Guided = sliders on the same params as Pro"), NOT a separate data model: both modes drive the exact same two real engine knobs. `engine/song.py`: `compose_song_from_preset(preset, seed, num_sections, max_seeds, blast_fill_chance)` extracted from `compose_song`'s own retry loop so a real `Preset` OBJECT (not just a preset_id) can be composed directly -- needed because a blended preset has no id of its own; `compose_song` now just resolves `preset_id` then delegates. `blast_fill_chance` threaded as a real optional override through the full call chain (`compose_song`/`compose_song_from_preset` -> `_generate_attempt` -> `_generate_one_section`), defaulting to the existing module constant when omitted (zero behavior change for every existing caller). `engine/presets.py`: `blend_presets(preset_a, preset_b, t)` (already built in P9.3, reused here) -- real linear interpolation of ONLY the continuous character knobs (vocab weights, dissonance, motion, pedal); structural fields (tuning/scale/bpm/bars/feel/kick) come from `preset_a` unchanged, since blending two different tunings/scales isn't well-defined. `editor/backend/app/main.py`: `ComposeRequest` gains `blend_with`/`blend_t`/`blast_fill_chance`; `_compose` branches on `blend_with is not None` to blend-then-compose-from-preset vs. the plain compose path, both going through the same real `apply_order`/`apply_edits` pipeline afterward. `editor/frontend/`: new `GuidedControls.tsx` -- a Pro/Guided toggle that only relabels the SAME two live controls (blend-target preset + blend-amount slider; blast-frequency slider) rather than hiding them, since both modes are the same real params per the law above; wired into `App.tsx`'s `guidedParams()` helper, included in every `composeSong`/`exportMidi`/`exportRpp` call and the arranged-song refetch effect's dependency array. Verified live in the browser: generating a metalcore song then setting blend_with=djent at t=0.5 changed the real output (pm 0.55->0.56, kick-lock 0.77->0.78, preview 58.8s->55.7s); pushing blast_fill_chance to 1.0 changed it further (hits 295->300, kick-lock 0.78->0.69, matching X.34's documented behavior that blast fills aren't kick-locked); toggling Pro<->Guided correctly relabels the same live controls without resetting their values. `npx tsc --noEmit` clean. 613 passed engine-side (up from 610), 21 passed backend-side (up from 19).
- [x] P9.3 Regen primitives -- a genuinely new real engine capability (regenerating ONE section in isolation, which this project never had) plus real editor wiring. `engine/song.py`: `_generate_one_section` extracted from `_generate_attempt`'s own per-section loop body (pure extraction, verified byte-identical against the full 596-test suite before adding anything new); `regenerate_section(song, index, rng, preset, mode, role, hit_chance_bias)` -- real `"full"` (fresh rhythm+pitch), `"pitch"` ("new notes, same hits", redraws only deltas via the newly-public `motif.generate_pitch_deltas`), and `"rhythm"` ("same notes, new hits", fresh cell + the existing pitch contour adapted via `_adapt_deltas`) modes; `role` override for section-type conversion ("Make breakdown"); `hit_chance_bias` for the real density-nudge primitive; a real, documented, MINIMAL neighbor re-blend (X.24's own pickup mechanism at the up to two real boundaries touching the edit) so an edit doesn't leave an abrupt seam, with an honest, explicit limitation noted (a neighbor's kick isn't re-locked, since which kick style/blast type it used isn't tracked per section). `engine/presets.py`: `blend_presets(preset_a, preset_b, t)` -- real linear interpolation of the continuous character knobs (vocab weights, dissonance, motion, pedal) between two real presets, feeding both this and P9.2's Guided blend slider. `editor/backend/`: `app/edits.py` (`apply_edits`, real in-order replay of a client's edit history via `regenerate_section` -- stateless, "same seed + edit history = same bytes"), `/api/compose`/`/api/export-midi` gain an `edits` field. `editor/frontend/`: `SectionDetailPanel` gets real regen buttons (full reroll / new notes / new hits / too busy / too thin / make-this-section-X), wired through a real `TimelineBlock.edit` + a dedicated `arrangedSong` fetch (kept separate from the base `song` so the timeline's own block labels and the detail panel's live edited stats can diverge -- documented tradeoff: a role-conversion edit doesn't relabel its timeline block's color until a fresh Generate). `engine/tests/test_song.py` (regen-mode/role-override/density/no-mutation/reproducibility checks against real `compose_song` output), `engine/tests/test_presets.py` (`blend_presets` real interpolation checks), `editor/backend/tests/test_edits.py`. Verified live in the browser: Full reroll on a real "Intro" section changed guitar hits 26->17; "Make this section breakdown" correctly converted role and regenerated content; song-level judge score updated correctly after each edit. 610 passed engine-side (up from 596), 12 passed backend-side (up from 8).
- [x] P9.4 Accept/reroll/undo -- "Try again" is just re-calling a regen primitive (a fresh `regen_seed` every time is a genuinely different real result, not a fake reroll); "Keep this pocket" is implicit (an applied edit just stays, no separate action needed); real undo via a snapshot stack of past `blocks` arrays (capped at 20), verified live in the browser: converting a section's role then clicking Undo correctly reverted both the timeline state and the real regenerated content back to the pre-edit values.
- [x] P9.5 Preview vs Render -- Preview is the existing P9.1 Tone.js in-browser MIDI playback; Render is a new `POST /api/export-rpp`, reusing the real, already-tested `reaper_project.song_to_rpp` (verified opening in the user's real Reaper earlier this session) over the exact same real arrange+edits pipeline `/api/export-midi` uses -- same real arrangement, a different, DAW-importable output format. `editor/backend/app/main.py`: `_write_and_stream` extracted (both MIDI/`.rpp` writers write to a real path, not a stream -- one shared real tempfile round-trip instead of duplicating it). Frontend: a real "Render .rpp" download button next to the MIDI player, using the current real arrangement (order + edits) the player and detail panel already reflect. `editor/backend/tests/test_api.py` (new): real end-to-end route tests for presets/compose/export-midi/export-rpp, including real edit application and bad-input rejection. Verified live in the browser: clicking Render fired a real `POST /api/export-rpp` returning 200, no console errors. 19 passed backend-side (up from 12).
- [x] P9.6 VexFlow tab -- real per-cell `(string, fret)` data, never a fabricated fingering. `editor/backend/app/serialize.py`: `summarize_tab(section, guitar_fb)` walks `section["guitar_take_a"]` zipped with `section["pitches_per_cell"]` (index-aligned, since `performance.double_track` preserves the input cell's exact rhythm shape) and resolves each real hit via the already-used `Fretboard.pitch_to_fret`, threading `prev` across the whole section so fret choice favors minimal movement (the same real rule `pitch_to_fret` itself implements) -- uses `max_fret=guitar_fb.max_fret` (not the smaller default 12) to match the real `max_fret` every pitch was already guaranteed reachable within at generation time (`song._snap_to_playable_octave`), so it never raises. `editor/backend/app/main.py`: new `POST /api/section-tab` (`TabRequest` extends `ComposeRequest` with `section_position`), resolves the real fretboard via `presets.get_tuning`/`load_tunings` against the RESOLVED preset (post-blend, if any) -- never a UI-guessed tuning. `editor/frontend/`: `npm install vexflow` (0 vulnerabilities), new `TabView.tsx` renders a real VexFlow `TabStave`/`TabNote` staff via SVG, with a real, honestly-documented approximation where a cell's real beat-duration doesn't land exactly on a power-of-two notated value (this engine's triplet/gallop/stutter-chug rhythm devices) -- snapped to the nearest standard duration rather than silently rendered wrong or crashing; rests use `GhostNote` to preserve timing without drawing a fret glyph. Wired into `App.tsx` next to `SectionDetailPanel`, refetching on selection/edit/blend/blast changes via the same real `tabParams` (order+edits+blend+blast) the rest of the panel already uses. Verified live in the browser: generated a real metalcore song, confirmed the tab staff rendered real fret digits matching a real riff shape (`0,0,0,...,3,3,3,...,5`); clicked "Full reroll" on the same section and confirmed both the guitar-hit count (26->31) and the rendered tab digits changed to a genuinely different real fret sequence -- proving the tab reflects live regenerated content, not a cached snapshot. `editor/backend/tests/test_serialize.py` (new `summarize_tab` tests: JSON-safety, cell-alignment, every non-rest position within the real fretboard's own string/fret range), `editor/backend/tests/test_api.py` (route + bad-input rejection). `npx tsc --noEmit` clean. 613 passed engine-side (unchanged -- no engine changes needed, this is pure backend/frontend wiring over already-real engine data), 25 passed backend-side (up from 21).
- [x] P9.7 Section-level save/load presets, distinct from song-level style presets (§10.2) -- the final P9.2-P9.7 sub-part of the "All of them" authorization. A section preset IS a real, complete, reproducible `EditFields` (mode/role/hit_chance_bias/regen_seed, no position of its own) under a user-given name -- nothing more needed inventing. `editor/backend/app/main.py`: `RegenEdit` split into a new base `EditFields` (the reproducible part) plus `RegenEdit(EditFields)` adding `section_position` (the real position it applies to within one specific arrangement) -- needed because a SAVED preset has no position yet, only gets one when loaded onto a section. New `editor/backend/app/section_presets.py`: real local-only JSON persistence at `editor/backend/data/section_presets.json` (gitignored -- user session state, not source), `list_section_presets`/`save_section_preset`/`delete_section_preset`, fails closed on an empty name (save) or unknown name (delete). Three new routes: `GET`/`POST /api/section-presets`, `DELETE /api/section-presets/{name}`. `editor/frontend/`: new `SectionPresetPanel.tsx` -- Save disabled until the section actually has a real edit applied (the un-edited base generation isn't itself a reproducible `EditFields`); clicking a saved name applies it as the block's real edit via the same `handleApplyEdit` path any other regen action uses, no special-casing. Verified live in the browser: regenerated a real Intro section (26->16 guitar hits), saved it as "heavy intro", clicked reset (back to 26 hits), then clicked "heavy intro" and confirmed it reproduced the EXACT same edited state byte-for-byte (16 hits, identical tab fret sequence `0,0,0,0,0,0,3,3,3,3,3,3,3,3,3,5`) -- real proof of "same seed + edit = same bytes" extended to saved presets. Deleted it and confirmed it disappeared from the list and the real local JSON file. `editor/backend/tests/test_section_presets.py` (new, 7 tests: round-trip, disk-persistence, overwrite, delete, bad-input rejection for both save and delete), `editor/backend/tests/test_api.py` (+2 route tests, isolated from the real local data file via `monkeypatch` on `_DATA_PATH`). `npx tsc --noEmit` clean. 613 passed engine-side (unchanged), 34 passed backend-side (up from 25).

All seven Phase 9 sub-parts (P9.1-P9.7) are now DONE. The "All of them" authorization is fully discharged.

## Phase 10 — Export
Scope: `### 17.6` + `### 14.2` (items 10-11, project format + export matrix). JSON skeleton already in `### 18.2`.
- [x] P10.1 MIDI stems (real, playable, Reaper-importable) -- `engine/midi_export.py`: `song_to_midi(song, path, ppq=480)` writes a real Standard MIDI File (type 1) with a genuine per-section tempo track (X.6c's `tempo_map`, not flat), guitar take A/B, bass (its own fretboard's real positions), lead (harmony-mode notes timed hit-for-hit against the rhythm motif's own cells; solo-mode notes timed at the exact even 8th-note spacing `song.py`'s own note-count math implies, plus the legato tail's real per-note durations), and drums (the kick pattern actually assembled into the song, via `drums.note_for_role` for the real GM note number). Deliberately does NOT invent a fill/kick blend, since `song.py` itself never blends `section["fill"]` into the assembled drum track -- exporting one would be new arrangement logic, not wiring. `engine/tests/test_midi_export.py`: every real preset round-trips to a parseable file; guitar/bass pitches and hit counts cross-checked against the actual `pitches_per_cell`/bass-cell data (not just "a file was written"); tempo track values checked against real `tempo_map`; drum notes checked against the real GM kick value; solo-section note count checked against real `lead` data; bad-input rejection (non-positive ppq, mismatched tempo_map/sections length). `.rpp` project-file generation is now DONE too (see Phase 8 P8.1 -- `engine/reaper_project.py`, verified opening in the user's real Reaper). Mix audio and tab notation remain open (need real Reaper FX-chain work (P8.3) and VexFlow (P9.6) respectively).
- [ ] P10.2 Song JSON
- [ ] P10.3 Preset calibration

## Deferred
Vocals. Extra ML. Playability beyond pitch_to_fret.
