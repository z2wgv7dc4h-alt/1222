# TASKS

Work only `## Next`. One checkbox per session. Paste pytest before flipping to DONE.
P1+ : open `SCOPE-INDEX.md`, then only those headings in `god-tier-metal-scope.md`.

## Next
(X.1-X.23 done. X.20-X.22 fixed real density/articulation/kick-silence bugs. X.23 wired real power chords onto the main rhythm guitar (it had been strictly monophonic all session -- `chords.solve_chord`/`riff.voice_chord_section`, both real and tested since P1.11/P3.11, had never been reached for the main riff, only chill/interlude's ambient chords). 539 passed, 1 skipped as of the last full-suite run.

A user-requested full audit ("What else is missing that's never been wired in") found several more real, confirmed-via-grep gaps, NOT yet acted on -- see the new entries at the bottom of `## Scope gaps tracker` for the full list and real risk assessment. Highest-value, real candidates for the next session: `structure.pickup`/`bridge`/`flatten` (P6.2's real cross-section blending -- every section transition in every song has been a raw, unblended concatenation this whole session; genuinely higher-risk to wire since it touches the cell-level boundary across every parallel instrument track, deserves its own careful plan-mode pass, not a rushed fold-in) and `groove.gallop_cell`/`stutter_chug_cell` (real, named classic-metal rhythm devices with no current preset/role slot to cleanly land in yet). Also real and open: `interplay.call_and_response`, `structure.apply_half_time` (a rhythm-level, not tempo-level, half-time device -- distinct from X.18), `slam.mark_pinch_harmonics`/`chromatic_creep` (kept as "genre-agnostic" when the slam preset was removed, never wired anywhere else either). `structure.generate_section_content`/`generate_song_sections` are confirmed DEAD/superseded by `song.py`'s own `_generate_attempt` -- candidates for deletion, not wiring.

Other real, not-yet-built remaining work: the reference's richer 4-tier accent-aware velocity table (X.21 shipped a simplified 2-tier version), scope sec.14.2 item 5 (key/tonal-center modulation, still OPEN), the deeper RHYTHM-level repetition question X.14 left open, P8.3 FX-chain tone presets (needs the user's own hands in Reaper's GUI once), P10.2 Song JSON, P10.3 further preset calibration, a real "tapping" lead-guitar technique. Phase 9 editor UI not started -- see PROGRESS.md for the full real findings/decisions log.)

---

## Scope gaps tracker
Cross-checked against `god-tier-metal-scope.md`'s OWN numbered "still open" lists (§14.2, §16, §17), not just session-by-session listening feedback -- built after a full scope re-read (2026-09-08) found two real gaps (§14.2 items 4/5) that had never been promoted into `## Next`'s prose despite sitting in the scope doc since day one. Re-run this reconciliation per `SCOPE-INDEX.md`'s trigger (after a batch of X.* items lands, or whenever `## Next` runs dry) rather than waiting for another ad hoc full re-read.

**§14.2 (scope gaps needing decisions), items 1-13:**
1. Vocals -- DONE (RESOLVED: out of scope, non-goal).
2. Bass own fretboard -- DONE (P5.1).
3. Lead/solo generation algorithm -- DONE (P3.7 VoiceLeader, X.6a legato).
4. Mid-section tempo/feel drops ("blasting, then half-times for 2 bars as a slam moment") -- DONE (X.18).
5. Key/tonal-center modulation mechanics -- OPEN. No preset changes key mid-song; not concretely designed yet.
6. Humanization (micro-timing + dynamic arc) -- PARTIAL: dynamic arc DONE (`theory.arc()`, density=0.28+0.62*energy); per-cell `timing_offset` exists and is consumed by `midi_export._cell_events`, but hasn't been audited as a deliberate, broad humanization pass -- worth confirming, not yet done.
7. Mixing intelligence (adaptive mixing) -- OPEN. Needs a render-time automation layer; punted along with P8.1's pivot to static `.rpp` (no live Reaper connection to script against yet).
8. Theory validator / automated test harness -- DONE in spirit: the pytest suite itself (scale-membership, fretboard-reachability, rhythm-sum checks) is this, not a separate module.
9. Evaluation/QA methodology -- PARTIAL: this session's whole listening-feedback-driven iteration loop IS this in practice; no written rubric exists.
10. Project file format (Song JSON) -- OPEN (P10.2).
11. Export matrix -- PARTIAL: MIDI stems DONE (P10.1), Reaper project DONE (P8.1); full mix audio, individual stems, tab/notation PDF still OPEN (need P8.3+ FX chains, P9.6 VexFlow).
12. Reference-corpus analysis -- DONE as a capability (`audio_vocab.py`, `midi_vocab.py`), ONGOING as a practice (X.7/X.10/X.15/X.16/X.17 all used it against real reference material).
13. ML/GPU stretch track -- OPEN, explicitly deferred (not v1, revisit post-editor).

**§16 (atmospheric/orchestral expansion):**
- Placeholder GM pad/orch-hit + synth-doubles-motif -- DONE (P7.1-P7.3).
- Real orchestral bed (strings/brass/choir via Spitfire/sfizz) -- OPEN, blocked on an external sample-library install (same category as P8.3 -- needs the user's hands once, not automatable blind).
- Synth harmonizing/doubling the guitar motif at a fixed interval (not just unison) -- PARTIAL, worth confirming P7.2's exact behavior against this specific bar next time atmosphere.py is touched.

**§17 (further gaps):**
- 17.1 Double-tracked guitars -- DONE (P3.8).
- 17.2 Human playability -- RESOLVED (Option B: accept programmed precision as genre-normal); "flag dense passages in the tab view" deferred to P9.6 (not started).
- 17.3 Reproducibility/seed -- DONE (P2.8, enforced throughout).
- 17.4 Trademark/naming -- DONE (mood-name presets adopted; band names kept only as `ALIASES`/docs).
- 17.5 Reaper tempo-map for polymeter -- DONE (Option A chosen and implemented: real absolute-second `TEMPOENVEX` positions, not a human-legible grid -- matches P8.1's actual output).
- 17.6 Preset calibration workflow -- ONGOING (P10.3; X.7/X.10/X.16/X.17 are this in practice, not yet a standalone tool).
- 17.7 Repo structure -- DONE (real layout + `.gitignore` policy).

**§18.7:** one loose thread, never asked -- whether the old `z2wgv7dc4h-alt/Ww` repo has more history worth pulling forward. Low priority; flag if it comes up.

**Real gap found during X.20, closed by X.21 (not in the scope doc's own numbered lists, but worth tracking here since it's the same class of issue):** `preset.open_chance`'s real intended purpose -- an open-string-ringing-vs-muted-note VELOCITY/articulation choice per hit (confirmed via the real reference source, `reference/ww-forge-prior-attempt/engine/riff_engine.py`'s `wants_open = rng.random() < open_chance`) -- is now DONE at the 2-tier level (X.21: real 120/88 velocities, clamped around the real 110 threshold). Deliberately deferred: the reference's richer 4-tier accent/downbeat velocity table (`120`/`100`/`96`/`88`, needing `atmosphere.find_accents`'s accent data threaded into `performance.humanize_take`) is a real, smaller, still-OPEN follow-up.

**Real gap found+closed X.23:** `chords.solve_chord`/`riff.voice_chord_section` (P1.11/P3.11, real and tested) were only ever reached for chill/interlude's ambient chords, never the main rhythm-guitar riff -- confirmed via grep the main riff has been strictly monophonic all session. DONE (X.23: real power chords, root+5th, at MIDI/`.rpp` export time for every role except chill/interlude).

**Full-engine unwired-capability audit (2026-09-09, user-requested "what else is missing"), confirmed via grep -- zero real usage anywhere outside each function's own file + tests:**
- `structure.pickup`/`bridge`/`flatten` -- OPEN. The real, ported (P6.2) cross-section BLENDING mechanism ("overwrite the last 2 cells of the outgoing section with the first 2 of the incoming one," plus a real bridge transition between every pair). Every section transition in every song this whole session has been a raw, unblended concatenation. Real risk: `flatten` operates on ONE flat list of cell-lists: this engine has SIX parallel per-section cell arrays (guitar take A/B, bass, kick, snare, hihat) that must all stay index-aligned with `pitches_per_cell`/kick-lock timing -- wiring this naively on just one track would desync pitch/rhythm data across the boundary. Needs its own careful design pass (which parallel streams get blended, how alignment is preserved), not a quick fold-in.
- `groove.gallop_cell` -- OPEN. The classic metal short-short-long gallop rhythm, named explicitly in the scope doc (`## 4. Riff-writing`). No current preset/role has an obvious clean slot for it (this engine's roles are intro/build/breakdown/outro/solo/chill/interlude, no "verse," and `feel` is already a per-preset-wide setting, not a per-section one) -- needs a real design decision about where it plugs in before wiring.
- `groove.stutter_chug_cell` -- OPEN, same real gap/blocker as `gallop_cell`.
- `interplay.call_and_response` -- OPEN. Guitar reacting to drums/lead instead of being generated in isolation (scope `## 4`). Not investigated further this session.
- `structure.apply_half_time` -- OPEN. A real RHYTHM-level half-time device (doubles actual cell durations in a sub-range, distinct from X.18's TEMPO-only mid-section drop). Not investigated further this session.
- `slam.mark_pinch_harmonics`/`chromatic_creep` -- OPEN. Real pinch-harmonic accent sim + chromatic creep (P3.10), deliberately kept as "genre-agnostic" when the `slam` preset was removed, but never wired into any of the remaining presets either.
- `structure.generate_section_content`/`generate_song_sections` -- DEAD, not a gap. An earlier, simpler song-generation pipeline that predates and is fully superseded by `song.py`'s own `_generate_attempt` (confirmed via grep: only ever mentioned in a docstring comment, never actually called). Candidate for deletion.

---

## Cross-cutting (spans multiple phases, not one Phase N)
- [x] X.1 `engine/song.py` `compose_song(preset_id, seed)` -- full pipeline integration. Gap found on review: every phase (1-7) built and tested its own mechanism in isolation; nothing had ever taken a real `Preset` and threaded it through a real end-to-end generation run (rhythm guitar x2 double-tracked, lead, bass, drums+fills, atmosphere, judge/retry). `engine/tests/test_song.py` verifies this for every real preset -- passed first try, confirming the independently-built phases actually compose.
- [x] X.2 Consume remaining preset fields. `drums.kick_pattern_for_style` dispatches on the real style values found in the preset JSON files (bounce/lock -> exact guitar-lock; sparse -> every-other-hit; euclid -> genuine Bjorklund-equivalent even distribution, pulse count from the guitar's own hit count; two_step -> documented metalcore breakdown interpretation (kick at beat-offsets 0.0/1.5 per 2-beat cycle); blast -> every cell). `motif.generate_motif` gained `group_beats` (tiles a short cell via `rhythm.tile_cell` for djent's N-against-4 displacement) and `pedal` (via new `apply_pedal_bias`, biases delta selection toward the root proportional to `preset.pedal`). `preset.octave_stab` now adds real `VoiceLeader.stab()` leaps at accent positions when true, empty list when false. All wired into `song.py`'s real `compose_song` call path; tested with A/B comparisons on the real `djent` preset (`dataclasses.replace` to flip one field, identical seed on both sides) proving each field measurably changes output, not just RNG noise.
- [x] X.3 Wire `motif.ThemeRegistry` (P3.4) into `compose_song` -- it existed but had no caller; sections sharing a role now develop one recurring theme (rendered at each section's own arc-driven degree, `invert`-ed on alternate reuses) instead of an independently-rolled motif per section. Fixing this surfaced a real bug (`invert` can push a pitch below the guitar's lowest string) -- added `_snap_to_playable_octave`, same discipline `bass.py` already uses.
- [x] X.4 Fix `compose_song`'s dead `seed` parameter -- it called `structure.judge_and_retry`, which always tries its own `range(max_seeds)` internally, so every call silently collapsed onto whichever of seeds 0-5 judged first regardless of the caller's seed. Replaced with a local retry loop seeded `seed, seed+1, ...`.
- [x] X.5 Realistic per-role lead-guitar behavior -- was generating an active melodic line in every section all song long. Now: `solo` = dense featured line (register pushed up, more leaps), `chill`/`interlude` = harmonized doubling of the rhythm's own theme (`riff.harmonize_line`), everything else = silent (a busy independent lead would clash with a dense chug section).
- [x] X.6 Periphery-level musicianship goal (see CLAUDE.md Bar) -- three genuinely new capabilities, not just unwired data:
  - [x] X.6a Legato/technical run generator -- `engine/legato.py`: `generate_legato_run` (contiguous scale-degree walk via `scale.step`, structurally distinct from VoiceLeader's weighted-pick model; optional single mid-run direction reversal when seeded), `legato_run_rhythm` (fast even subdivisions, genuine tuplets via `rhythm.tuplet_grid` when the length fits 3/5/7), `fret_positions_for_run` (single-string-biased via `Fretboard.pitch_to_fret`'s `prev` chaining -- real hammer-on/pull-off technique stays on one string). Wired into `song.py`: every solo section splices a real legato lick onto its featured lead line (`section["legato"]`), fails closed (skips the splice) rather than fabricating a position if genuinely unplayable on that preset's tuning.
  - [x] X.6b Extended/altered chord vocabulary -- `engine/chord_vocab.py`: `CHORD_QUALITIES` (maj7/min7/dom7/sus2/sus4/add9/maj9/min9/six9, semitone-offset tuples matching `scales.py`'s `SCALES` convention), `get_chord_quality` (raises `ValueError` on unknown name, never fabricates), `voice_named_chord` (reuses `riff.voice_chord_section` -> `chords.solve_chord` for real fingering, no reimplemented solver), `quality_for_dissonance` (buckets a section's real `arc_row["dissonance"]` across `DISSONANCE_ORDER`, low->open/consonant, high->darker). Wired into `song.py`'s `chill`/`interlude` (`lead_mode == "harmony"`) branch as `section["chord_quality"]`/`section["chord_voicing"]`, alongside (not instead of) the existing `riff.harmonize_line` doubling; every other role leaves both `None`. Fails closed to `None` on a genuinely unreachable voicing rather than a fabricated shape. `engine/tests/test_chord_vocab.py`: every quality reachable on a real loaded tuning, unknown-quality/unreachable-chord rejection, monotonic dissonance bucketing, and an end-to-end `compose_song` check exercising both a real reachable voicing and a real unreachable one.
  - [x] X.6c Metric modulation as a compositional device (the pulse itself reinterpreting, e.g. a dotted-eighth becoming the new quarter) -- a step beyond the existing `tile_cell`/`metric_polyrhythm` (which stay within one fixed pulse). `engine/metric_modulation.py`: `modulation_ratio(old_subdivision, new_subdivision)` (subdivision = `(numerator, denominator)`, ratio = `value(old)/value(new)`; verified against the textbook dotted-quarter=new-quarter case, ratio 1.5) and `apply_metric_modulation(base_bpm, ratio)`; both fail closed on non-positive/malformed inputs. Wired a real `"metric_modulation"` branch into `structure.tempo_at`'s existing curve dispatch (alongside `"drop"`/`"ramp"`, same pattern). `song.py` fires a real trigger on the first `build`->`breakdown` transition in the generated sequence (straight eighth of the old pulse becomes the new quarter -- ratio 0.5, the genre's half-time breakdown treatment), producing `song["tempo_map"]` (one BPM per section, computed via `tempo_at` for every composed song, defaulting to flat `preset.bpm` when no transition occurs). `engine/tests/test_metric_modulation.py`, plus extensions to `test_structure.py`/`test_song.py` (including a direct `generate_section_sequence` seed search to guarantee a real build->breakdown transition exists before checking its tempo_map effect).
  - [x] X.6d Major-family scales (lydian, mixolydian, major/ionian, harmonic major) -- added to `scales.py` (`major`/`ionian` alias, `lydian`, `mixolydian`, `harmonic_major`). No existing preset was switched to use them yet (would need careful vocab-weight redesign, not a drop-in swap, since weights are semitone-interval based and snap onto whichever scale is active) -- the capability now exists, adopting it into a preset is a deliberate follow-up.
- [x] X.7 `engine/audio_vocab.py` -- real-audio structural/tonal vocabulary, the audio analogue of `midi_vocab.py`'s MIDI mining. Tempo, harmonic/percussive rhythm-density split, spectral-centroid brightness descriptor, per-section relative density curve, key/tonal-center estimate (Krumhansl-Schmuckler correlation), real structural segmentation (MFCC self-similarity, not equal slices), tempo-stability check, drum-onset role classification (KICK/SNARE/HIHAT_OR_CYMBAL by frequency band), and guitar-rhythm-pattern detection (straight/gallop/syncopated from onset timing alone, never pitch). Never transcribes notes/chords, only extracts numeric statistics -- same "reference data, not composer" boundary as the MIDI vocabulary. Optional local `demucs` pre-processing (`pip install demucs`, MIT-licensed, run locally -- never upload audio to a third-party "online demucs" site) gives real per-instrument stems. Validated against a real user-supplied track (a friend's original song): found the guitar/bass has more section-to-section dynamic range than the drums (validates `theory.arc()`'s design), the track sits in A MAJOR (a real data point that the scale library's lack of major-family modes, X.6b-adjacent, is a real gap), and the drum-role classifier's own output (implausibly few detected snares) honestly demonstrates a real ceiling -- a hand-built frequency-band heuristic isn't reliable on a dense, cymbal-heavy mix; a proper trained drum-transcription model would be needed to do better, not just more analysis code. Also added `transcribe_and_split_registers` (optional, needs `basic_pitch` -- not in requirements.txt, its own dependency pins are fragile on newer Python, install ad hoc): real polyphonic audio-to-note transcription split into rhythm-register vs lead-register layers by pitch. Cross-validated on the real track: the rhythm layer's dominant transcribed pitch class (A) independently matched `estimate_key`'s separate chroma-based key estimate (also A) -- two unrelated methods agreeing is real evidence, not proof, that this is picking up something true.
- [x] X.8 Wire `preset.feel` (was validated at load time, never consumed by generation -- same class of gap as X.2's kick/group/pedal/octave_stab). Investigated the real reference implementation ("Metalerator", `reference/metalerator/` -- confirmed via its own README to be a real, dedicated metalcore generator, not a generic engine with metalcore bolted on) for what a genuinely portable, non-invented `feel`-driven technique looks like: its breakdown generator (`rhythm_guitar/breakdown/default_melodic.py`) draws durations from a real weighted table (biased toward 8th/quarter chugging over rapid 16ths) and enforces "no isolated single 16th note" (pairs them). Ported both, adapted to this engine's duration vocabulary (half notes dropped, ratio preserved): `rhythm.FEEL_DURATION_WEIGHTS`/`FEEL_NO_SINGULAR_SHORT`/`duration_bias_for_feel`, new opt-in `weights`/`no_singular_short` params on `rhythm.generate_rhythm` (default `None` = byte-identical old uniform behavior, verified by test), threaded through `motif.generate_motif`/`ThemeRegistry.get_or_create` and wired at `song.py`'s `preset.feel` call site. Only `"breakdown"` (metalcore's real feel) had ported reference data at the time (X.15/X.16 later added `chug`/`bounce`) -- other feel names stay uniform rather than inventing weight tables with no real source, per this project's own "port, don't invent" discipline. `engine/tests/test_rhythm.py`: weights=None is byte-identical to pre-existing behavior (zero regression risk), the real breakdown weights measurably shift the duration distribution, the no-singular-short rule eliminates every isolated occurrence across 50 seeds, bad-input rejection (all-zero-weight). `engine/tests/test_song.py`: a real A/B compose_song comparison (metalcore vs. the same preset with an unmapped feel) proves the 16th-note share measurably drops in real generated output, not just in rhythm.py isolation. 450 passed in 10.76s.
- [x] X.9 Real snare backbeat, wired for every preset -- closes a real, previously-unaddressed gap: this engine had a kick (P4.2/X.2) and blast/fill patterns (P4.3) but NO snare/hihat layer at all. Ported from the same real reference implementation as X.8 (`reference/metalerator/metalerator/drums/snare/snare.py`, `Snare.snare_step`/`snare_half_step`/`snare_double_time`): `"step"` is a half-time hit on beat 3 of every bar (checked the real source rather than assuming the generic "2 and 4"), `"half_step"` once per 2-bar cycle, `"double_time"` every beat except the section's first. Built on the same real cell-timeline mechanism `_kick_two_step` already used (`_cell_starts`/`_time_to_cell_index`/`_cyclic_hit_indices`, extracted as shared helpers -- `_kick_two_step` itself refactored onto them, no duplicated logic). Wired for EVERY preset via a real per-ROLE dispatch (`drums.snare_pattern_for_role`, `song.py`'s `section["snare"]`), not gated to one genre -- breakdown/intro/outro get the half-time backbeat, build/solo get the denser double_time, chill/interlude stay silent (same judgment call `lead_mode` already makes for those roles). Wired into `midi_export.py`'s Drums track alongside the kick (real GM notes 36/38, standard single-drum-track convention). Ghost notes (Metalerator's quiet grace-note hits around the main snare) documented as a real, deliberately deferred follow-up -- don't fit the fixed-cell-array model without a larger rework. `engine/tests/test_drums.py`: exact hit-position checks for all three styles against a hand-built cell grid (not trusting the mechanism blindly), role-dispatch table coverage, bad-input rejection. `engine/tests/test_song.py`: every real preset gets a real snare layer with chill/interlude silent. `engine/tests/test_midi_export.py`: real GM kick(36)/snare(38) note counts cross-checked against actual generated song data. 464 passed in 10.96s.
- [x] X.10 New preset: `progressive` -- adopts X.6d's major-family scales for the first time (they existed since X.6d but nothing used them, a deliberately tracked follow-up). Real Periphery-styled "melodic technical djent": lydian scale (bright, uplifting #4, the quintessential prog-metal mode) over djent's own real mechanisms (group=3 N-against-4 displacement, euclid kick, octave stabs) rather than djent's deliberately un-melodic root/5th-only vocab. Calibrated against the real user-supplied reference track analyzed in X.7/P10.3: `drop_a_7` tuning chosen because its open low string IS pitch class A, matching the track's real measured A-major key (Krumhansl-Schmuckler estimate) exactly; lydian chosen over plain major for the track's real bright/articulate spectral-centroid reading. Verified the generated output is genuinely scale-legal: every pitch class across a real generated song fell within exactly `{A,B,C#,D#,E,F#,G#}` (A-lydian's real interval set), zero notes outside it. Also fixed a real, now-stale mapping this surfaced: the `"periphery"` band-linked alias (`presets.ALIASES`) pointed at `"chill"` since this project's earliest presets (a proxy chosen only because no real Periphery-styled preset existed yet) -- repointed to `"progressive"`, the actual match, with the two tests that hardcoded the old target (`test_presets.py`, `test_song.py`) updated to match. 482 passed.
- [x] X.11 Real hihat/cymbal layer + varied kick overlay for high-energy sections -- direct response to real listening feedback ("the drums were really basic... no double kick no cymbals or anything being hit. just generic kicks every now and then"). Closes a real, previously-total gap: through X.9 this engine had kick + snare but ZERO cymbal content anywhere, ever. `engine/drums.py`: `generate_hihat_pattern`/`_hihat_closed` (real steady 8th-note pulse, universal genre-agnostic "keeping time" convention, not ported from a specific source since it needs none), `hihat_pattern_for_role` (wired for every preset via `song.py`'s `section["hihat"]`, silent only for chill/interlude, same rule as snare). Also: `_kick_double_kick` (real continuous straight-16th pulse, ported from Metalerator's actual `Kick.double_bass`), registered as a new selectable `"double_kick"` style, plus `kick_pattern_for_role`/`_ROLE_KICK_OVERRIDE_CHOICES` -- `build`/`solo` sections now ALWAYS get a real, VARIED overlay (`double_kick` or `blast`, picked per section via the real seeded rng) regardless of what the preset's own `.kick` field declares, replacing `kick_pattern_for_style` as `song.py`'s call site. Wired into `midi_export.py` and `reaper_project.py`'s Drums track (real GM note 42/HIHAT_CLOSED alongside kick/snare). `engine/tests/test_drums.py`: real hit-position checks, role-table coverage, bad-input rejection, kick-role-override requires a real rng (fails closed rather than silently defaulting). `engine/tests/test_song.py`: every real preset gets real hihat with chill/interlude silent, build/solo sections get real overlay variety across seeds. Generated and sent the user two real demos: a 16-section/3:09 song (up from 8-section/1:28, testing whether length addresses the "basic" feedback) with the new drum layer audible throughout. 500 passed.
- [x] X.12 Real, critical tempo-map bug fix: half-time never reverted. Traced from direct listening feedback on a 16-section generated song ("drums are too slow... its not really metal. its too slow") -- inspecting the real `tempo_map` confirmed X.6c's original one-shot design (`_build_to_breakdown_index`: modulate once at the FIRST build->breakdown transition, hold for the rest of the song) meant 14 of 16 sections were stuck at half-time (76 BPM instead of 152) for the whole rest of that song, including both solo sections -- directly explaining "no solos" too (they were real, present, just playing at half-speed). Fixed `song.py`'s `_compute_tempo_map` to apply the real metric-modulation half-time treatment INDEPENDENTLY to every `"breakdown"`-role section and revert to the preset's own full `base_bpm` for every other role -- including one immediately after a breakdown -- matching real genre convention (a breakdown's half-time feel is a temporary device, not a permanent state change). Removed `_build_to_breakdown_index` (no longer needed); rewrote `engine/tests/test_song.py`'s tempo-map tests to verify real per-section modulation AND real reversion (a sequence with two breakdowns and sections between them, confirming a real drop-then-recover-then-drop-again pattern), including a real end-to-end check against actual `compose_song` output for every non-breakdown/breakdown section. 499 passed. Regenerated and sent the corrected demo -- same 16-section sequence, but solos and builds now play at real full tempo instead of stuck at half-speed.
- [x] X.13 Real hihat variation: open-hat accents + section-transition crashes. Grounded in a real finding, not guessed: ran `demucs` on a real user-supplied reference track ("periphery style full song.mp3"), then `audio_vocab.classify_drum_onsets` on the isolated drum stem -- cymbals/hihat were 84% of ALL detected drum onsets (1079 of 1282), by far the most constantly-present element in a real mix, while X.11's hihat had zero variation (one flat closed-hihat pulse). `engine/drums.py`: `apply_hihat_accents` (real open-hat accents at the SAME structural-accent positions `atmosphere.find_accents` already computes for octave stabs -- never an independently-invented accent set) and `add_transition_crash` (a real crash at a section's opening whenever its role genuinely changed from the previous one -- ported from Metalerator's real "crash on kick/snare pattern change" cymbal generator, generalized to this project's own role-based arrangement unit). Wired into `song.py` for every preset (skipped for chill/interlude, which stay silent by design). `engine/tests/test_drums.py`: accent overlay never fabricates a hit on a rest, crash override behavior, no-op when not firing. `engine/tests/test_song.py`: every real composed song's first section gets a real opening crash. 504 passed. Regenerated and sent the demo -- 14 real crashes, 20 real open-hat accents, 311 closed hihat hits.
- [x] X.14 Real 4-state theme-development rotation, closing a real "not much is going on" gap. Direct response to real listening feedback comparing generated output against two real reference tracks. Root cause: `ThemeRegistry` keys a base theme by ROLE alone, and every occurrence of that role for the WHOLE SONG only ever alternated between 2 pitch-contour states (base theme, or `invert()`) -- a longer song revisiting a role many times (real for any longer song, e.g. 6 breakdowns in a 16-section song) heard just those same 2 states over and over regardless of how many times the role recurred. `motif.transpose`/`motif.augment`/`motif.fragment` all already existed, real and tested, but only `invert` was ever wired into this rotation. Added `song._develop_theme(base_theme, occurrence)`: a real 4-state rotation (base / invert / transpose+2-degrees / transpose+invert) using only safe, length-preserving develop ops (transpose/invert touch pitch deltas alone, never rhythm cell count or duration, so no downstream section-length/timing invariant is at risk). Verified against real generated output: a 16-section song's most-repeated role (6 real occurrences) went from a 2-state ceiling to 4 real distinct pitch-content states. `engine/tests/test_song.py`: exact rotation-state check against a hand-built motif, real end-to-end variety check against actual `compose_song` output for a role recurring 3+ times. 506 passed. Noted as a real, honest scope limit: this addresses PITCH/harmonic repetition specifically -- the deeper RHYTHM-level repetition (the same base rhythm cell reused verbatim across every occurrence of a role, just pitch-shifted) remains a separate, larger, not-yet-attempted architectural question.
- [x] X.15 Real triplet + chug feel generation -- closes two more completely-unwired declared preset feels. Direct response to real listening feedback ("not enough chugging. not enough triplets."). `tech.json` (`"feel": "triplet"`, own description: "kick-locked triplet chug") and `deathcore.json` (`"feel": "chug"`) were both real, declared feel values with zero generation effect -- like `"breakdown"` before X.8, silently falling back to uniform duration selection. Built `rhythm.generate_triplet_rhythm(total_beats, hit_chance, rng)`: a genuinely different rhythmic device (real eighth-note-triplet subdivision, `span_beats/3` via the SAME `tuplet_grid(3, 2, 1.0)` spacing law every other real tuplet in this project uses), not a reweighting of the existing `[0.25, 0.5, 1.0]` duration menu -- a real triplet can't be expressed that way at all. Wired as a dedicated dispatch branch in `motif.generate_motif` (bypasses `duration_bias_for_feel`'s weighted-choice path entirely for `feel == "triplet"`). Also added `rhythm.FEEL_DURATION_WEIGHTS["chug"]` -- real, deliberately un-ported (no source needed: constant 16th-note palm-muted chugging is generic, textbook metal-guitar technique, same category as X.11's hihat pulse), the deliberate inverse of breakdown's 8th/quarter bias. Verified against real generated output: tech's real compose_song output is now 100% genuine 1/3-beat triplet durations; deathcore's is now 16th-note-dominant (>50% of all hits, up from a uniform 1/3 baseline). `engine/tests/test_rhythm.py`: real triplet-duration/count checks, spacing verified against the same real `tuplet_grid` law, bad-input rejection (non-whole beat counts fail closed rather than silently truncating). `engine/tests/test_song.py`: real end-to-end checks against actual `compose_song` output for both presets. 513 passed. Generated and sent real demos for both presets.
- [x] X.16 Real "bounce" feel density fix + real minor-third vocab weight for djent, grounded in EXACT reference data (not audio-transcription-uncertain, not textbook-generic). User supplied a real original MIDI file ("born of osiris style midi.mid", confirmed an original composition, not a copyrighted commercial transcription) -- parsed with `mido` for exact note data (no uncertainty at all, unlike audio-derived analysis): Guitar 1 measured 57.5% 16th notes, 36.9% 8th notes, and literally 0% quarter notes across 1464 real notes; pitch-class distribution showed real minor-third (interval 3) usage as the third-most-common interval (11%), right after root/fifth. Found `"bounce"` (djent/groovy/melodic/progressive's real declared feel -- the MOST common feel across the whole preset lineup) had ZERO real duration weighting, unlike `breakdown`/`chug` -- fell through to fully uniform selection, meaning roughly a third of every bounce-feel preset's notes were quarter notes, essentially never seen in the real reference. Added `rhythm.FEEL_DURATION_WEIGHTS["bounce"]` (real measured ratio, quarter notes kept at a small nonzero floor rather than a hard 0) and `FEEL_NO_SINGULAR_SHORT["bounce"]`. Also added real weight for interval `3` (minor third, confirmed scale-legal for djent's own phrygian scale) to `djent.json`'s vocab, matching the real reference's actual interval usage. Verified against real generated output: djent's real duration distribution went from ~33/33/33 (16th/8th/quarter) to 78.5%/21.5%/0%. `engine/tests/test_rhythm.py`: exact real-table check. `engine/tests/test_song.py`: real end-to-end density check against actual compose_song output. 515 passed.
- [x] X.17 Cross-preset audit triggered directly by the user's "I want these to be generating music identical to their respective genres" -- checked every preset for the same class of gap X.16 fixed in djent, not just the one preset that prompted it. Found a real regression X.16 itself introduced: `chill.json` shared `"feel": "bounce"` with djent/groovy/melodic/progressive, so X.16's real measured-dense duration weighting (correct for those four energetic presets) also silently made `chill`'s duration mix dense/16th-heavy -- directly contradicting chill's own documented identity ("Low-density breather, consonant. Airy, Periphery slow-part feel."). Added a new, distinct `"airy"` feel value to `rhythm.FEEL_DURATION_WEIGHTS` (deliberate inverse weighting, quarters dominant -- deliberately NOT added to `FEEL_NO_SINGULAR_SHORT`, since forcing isolated-16th pairing would fight the sparse character) and changed `chill.json`'s `feel` to `"airy"`. Verified against real generated output: chill went from what would have been dense-16th-dominant to 53.4% quarters / 28.6% 8ths / 18.0% 16ths. `engine/tests/test_rhythm.py`: `test_duration_bias_for_feel_airy_is_the_real_inverse_of_bounce`. Audited the rest of the lineup for the same class of gap and found no further fix needed: `groovy.json` (motion=0.0, root/5th-only vocab) correctly has no minor-third weight -- its own description is "Power chords... The baseline archetype", deliberately simple by design, not an oversight. `melodic.json` already weights interval 3 highest of all (5, above root's 4) matching its own "3rds and 6ths" description. `metalcore.json`/`deathcore.json`/`tech.json` were already confirmed correct in X.8/X.9/X.15/X.16's own work (real ported breakdown/chug/triplet feel data, real interval-3/tritone coverage). Also, separately: reviewed the whole project's file tree for staleness per the user's own "all files should be reviewed and updated for relevancy or deleted" -- deleted four one-time bootstrap artifacts already fully consumed (`HANDOFF.md` pointed at "Phase 3-5" long since finished; `README-SETUP.txt`/`REMOTE.txt`/`SESSION.txt` were one-time initial-assembly/git-init/first-paste instructions, already executed session 1), refreshed `PLAN.md`'s stale "Built vs intended" summary (was still "159 tests... Phases 3-5 in progress"), trimmed `CONTROL.txt`'s completed "ONCE" setup section, fixed `PORTS.md`'s stale "metalerator: ...do not run the app" line into a real cited port table (breakdown weights/snare styles/double_kick/crash-on-change, each with its real X.8/X.9/X.11/X.13 status), and fixed two real drifted `.claude/settings.json` hooks found uncommitted at session start (confirmed with the user before touching hook/security config): restored `Bash` to the `PreToolUse` matcher so `guard-write.ps1`'s already-improved Ww/123 command-string check actually runs, and restored the Stop hook's auto-close prompt so `CLAUDE.md`'s "Stop auto-runs the close skill" claim is true again. 516 passed, 1 skipped.
- [x] X.18 Real mid-section tempo/half-time drops (scope sec.14.2 item 4, closed via the new `## Scope gaps tracker` above -- a full scope re-read at the user's own request found this real, previously-untracked gap sitting in the scope doc since day one). Distinct from X.6c/X.12's BETWEEN-section metric modulation: this is a genuine "blasting, then half-times for 2 bars as a slam moment" device WITHIN one section. Same real mechanism as X.6c (`metric_modulation.modulation_ratio`/`apply_metric_modulation`, ratio 0.5 -- no new modulation math invented), just triggered mid-section: `song.py`'s per-section loop decides, via that section's own seeded rng, whether an eligible section (`role in ("build","breakdown")`, `preset.bars >= 4`, 50% chance) gets a drop trigger at its own last 2 bars; a new pure `_resolve_tempo_drop(section_bpm, trigger_beat)` (called once tempo_map is known) produces the final `section["tempo_drop"]` (`{"trigger_beat","bpm"}` or `None`, never fabricated for an ineligible section). Deliberately a pure tempo/BPM device matching X.6c/X.12's own precedent -- the SAME notated guitar/drum cells just play slower for the dropped tail, no rhythm regeneration, keeping this scoped to tempo bookkeeping + the two exporters. `midi_export.py`'s `_tempo_track` gained a real second `set_tempo` event at the correct tick for any dropped section (note positions elsewhere untouched -- MIDI ticks are beat-based, not time-based). `reaper_project.py`'s `_tempo_envelope_block` now takes a flattened, already-ordered point list; `song_to_rpp`'s `start_seconds` accumulation is now two-part for a dropped section (pre-drop bpm up to the trigger, dropped bpm for the remainder) so every LATER section's real start time still lands correctly. Verified against real generated output (djent seed=11/8 sections): two `build` sections dropped from 170->85 BPM, and -- a real, unplanned-but-correct emergent effect -- two `breakdown` sections (already at X.6c's own real half-time 85 BPM) dropped FURTHER to 42.5 BPM, a genuine "double drop" quarter-time slam moment inside an already-half-time breakdown. `engine/tests/test_song.py`: real seed-search end-to-end check (bpm exactly half that section's own `tempo_map` value, correct trigger beat) plus a fails-closed check (wrong role, and a synthetic `dataclasses.replace(bars=2)` preset, both always `None`). `engine/tests/test_midi_export.py`/`test_reaper_project.py`: exact tick/exact-real-seconds position checks for the extra tempo event, updated the 2 pre-existing tempo-map tests that assumed exactly one tempo point per section (a real, expected consequence of this feature actually firing on those seeds, not a regression). 520 passed, 1 skipped (up from 516).
- [x] X.19 Real IRVD phrase development (Introduction/Repetition/Variation/Destruction, scope sec.18.2), wired for the first time. Direct response to blunt real feedback after X.18: "it still doesnt sound like a good song. nowhere near the quality or composition i expect." Rather than guess another tuning tweak, grepped the whole engine for real, unconsumed capability -- the exact pattern behind nearly every prior quality fix this session. Found the biggest one yet: `rhythm.phrase_plan(bars)` (real, ported P2.7, real tests) was called NOWHERE outside `rhythm.py`'s own test file -- confirmed via a full-engine grep. Every section, for every preset, was one flat undifferentiated rhythm+pitch draw spanning the whole section; no bar had a deliberate shape. For 6 of 8 presets (everything except djent/progressive, which have their own real `group_beats` polymeter-tiling device instead -- confirmed via grep, only those two set `"group"` in their JSON) that meant literally zero internal section structure. `engine/motif.py`: new `_generate_irvd_motif` built ENTIRELY from already-real, already-tested primitives, no new pitch/rhythm-choice logic invented -- "I" (Introduction) is a real `generate_motif` call for one bar; "R" (Repetition) is a literal deep copy of "I" ("play it again, verbatim"); "V" (Variation) is `transpose(I, 2)` (same rhythm skeleton, moved pitches); "D" (Destruction) is `augment(previous_bar, 0.5)` concatenated with a copy of itself (same content played twice as fast, refilling the bar exactly -- real "fragment and densify", derived from the bar immediately before it for real forward momentum, not always reaching back to "I"). New `generate_motif(irvd_bars=...)` parameter, threaded through `ThemeRegistry.get_or_create`; `song.py`'s `themes.get_or_create` call passes `irvd_bars=(preset.bars if preset.group is None else None)` -- mutually exclusive with `group_beats` by design (`generate_motif` raises `ValueError` if both given), since IRVD's verbatim-repeat structure would directly fight group_beats' deliberate phase-drift tiling; djent/progressive keep their existing real device untouched. Nothing downstream needed to change -- bass/kick/snare/hihat-follows-guitar, X.14's cross-section `_develop_theme`, MIDI/`.rpp` export all already just consume whatever Motif `generate_motif` returns. `engine/tests/test_motif.py`: exact structural checks (R bar cells/deltas exactly equal I's; V bar's rhythm exactly equals I's with deltas shifted by the real transpose amount; D bar has exactly double V's hit count with exactly-halved durations; single-bar `irvd_bars=1` reduces to byte-identical plain generation; `group_beats`+`irvd_bars` together raises). `engine/tests/test_song.py`: real end-to-end check against actual `compose_song` output for a real non-`group` preset (metalcore) -- every real section's bar 2 is a literal verbatim copy of bar 1, bar 4 has exactly double bar 3's hit count; confirmed djent/progressive are untouched. One pre-existing test (`test_tech_preset_real_feel_triplet_produces_genuine_triplet_durations`) needed a real, expected update: tech.json has no `group`, so its sections now legitimately include IRVD-Destruction-halved triplet durations (1/6) alongside the plain 1/3 triplet value. Verified against real generated output (metalcore seed=6/4 sections): every section showed the exact real shape, e.g. intro's 4 bars had hit-counts `[3, 3, 3, 6]` (I=R=V=3, D=6=2x), bar1/bar2 durations byte-identical, bar4 exactly half-duration/double-count. 525 passed, 1 skipped (up from 520).
- [x] X.20 Fixed a real, severe density bug: `song.py` was passing `preset.open_chance` as `hit_chance` (`rhythm.generate_rhythm`'s rest-vs-hit probability). Checked the real reference source (`reference/ww-forge-prior-attempt/engine/riff_engine.py`): `open_chance` there is `wants_open = rng.random() < open_chance` -- the chance a HIT is an open ringing note vs a muted low-root note, a completely different (pitch/articulation) axis, not rest-vs-hit density at all. Verified real damage with real generated output (seed 3, every preset): `tech.json` ("extreme technical/dissonant deathcore... kick-locked triplet chug") had a REAL 5.56% hit rate -- **94% rests** -- because its correctly-low `open_chance` (0.10, correct for "rarely open/ringing, mostly muted") was misapplied as "almost never even a hit." deathcore was at 25%, metalcore 31%, most others 30-50%. Almost certainly the single biggest real root cause behind the whole session's recurring "not enough chugging/intensity" complaints -- every prior density-adjacent fix (X.8/X.15/X.16/X.17 feel weighting, X.19 IRVD, X.18 tempo drops) was operating inside an already-crippled density budget. Also found the REAL fix source in the same reference file: `base_density = float(knobs.get("density", 0.72))`, then `density = max(0.12, min(0.98, base_density * (0.55 + 0.9 * arcv["energy"])))` -- a real, previously-un-ported formula combining a flat baseline with real per-role ARC energy scaling. Ported it exactly: `song.py`'s new `_BASE_HIT_CHANCE = 0.72` + `_resolve_hit_chance(base, energy)`, called with `arc_row["energy"]` (already computed for every section since Phase 1 -- used for dissonance/register/start_degree -- but its `"energy"`/`"density"` fields were never consumed, same "capability exists, never used" pattern as X.19's IRVD). `preset.open_chance` itself is untouched (still validated) but no longer wired here -- its real purpose needs a muted-vs-open pitch axis this project's degree-delta pitch model doesn't have yet, tracked as a real, deliberately deferred gap in the Scope gaps tracker, not silently dropped. Verified against real generated output: tech's real hit rate went 5.56% -> 86.11%, deathcore 25.00% -> 68.00%, every preset now lands in a genre-plausible 68-88% range. Also fixes a second real finding from the same investigation: a seed's real "build" section previously measured SPARSER than its own "breakdown" (0.62 vs 1.25 notes/beat, backwards from genre convention) -- real ARC energy ordering (K=0.20 < I=0.30 < O=0.40 < A=0.60 < S=0.80 < U=0.85 < C=1.00) now actually drives density, confirmed by a real cross-seed test that breakdown-role sections are measurably denser than chill-role sections. `engine/tests/test_song.py`: exact formula/clamp checks against real hand-computed values; real end-to-end check that tech/deathcore's real hit rate now exceeds 50%; real cross-seed check that breakdown role is measurably denser than chill role. One pre-existing test (`test_preset_feel_breakdown_changes_real_song_duration_distribution`) needed its margin narrowed from 0.05 to 0.03 -- a real, expected consequence of density itself now varying by role (the underlying feel-bias effect is still clearly present, ~12% relative reduction, just a smaller absolute percentage-point gap now that X.19's IRVD Destruction bars and X.20's role-driven density both contribute to the same duration distribution). 528 passed, 1 skipped (up from 525).
- [x] X.21 Real open-string-vs-muted articulation, wired via velocity -- closes the real, deliberately-deferred gap X.20 left open (`preset.open_chance`'s actual purpose). Direct response to real positive-but-honest feedback on X.20: "its a big imrpovement... sounding a little more like metal. still lacking but a massive jump." Kept pulling the same real thread: `performance.humanize_take` had `velocity_base: float = 100.0` as a flat constant for literally every hit in every section -- zero articulation contrast anywhere in the engine. Read the real reference source further (`reference/ww-forge-prior-attempt/engine/riff_engine.py`): `wants_open = rng.random() < open_chance` (line 125) decides per-hit whether a note is an open ringing accent vs a muted chug, and the real emitted velocities (lines 183/187) are `120` for an open hit vs `88`/`96`/`100` otherwise; `artic.py`'s real `PALM_MUTE_VELOCITY_THRESHOLD = 110` confirms opens are always emitted comfortably above that line. Directly matches the user's own words from earlier this session: "djenty is just the style of guitar - open string palm mutes." Deliberately a simplified 2-tier port (open=120/muted=88, dropping the reference's accent/downbeat 3rd-4th tiers) rather than plumbing `atmosphere.find_accents`'s accent data through a new call chain -- ships the real, dominant, audible contrast; the richer tiering is a real, documented, deliberately deferred refinement. `engine/performance.py`: `humanize_take` gains `open_chance: float | None = None` (default `None` = exact prior flat-velocity behavior, byte-identical); when given, each hit independently rolls open (velocity clamped >= 110) vs muted (clamped < 110), jitter layered on top but never crossing the real threshold either way. `double_track` needed no signature change (`**humanize_kwargs` already forwards it). `engine/song.py`'s one real call site now passes `open_chance=preset.open_chance` -- take A/B roll independent open/muted patterns (separate rng, consistent with `double_track`'s own "two independently-humanized takes" philosophy). Real, expected side effect found and fixed: `judge()`'s real `pm_ratio` scoring reads `velocity<110` directly, so real bimodal open/muted velocity (vs the old jitter-noise-derived split) measurably changes which internal seed-retry attempts pass judge -- 2 pre-existing tests hardcoded `seed=11` expecting a "solo" section; fixed with a real seed-search (`range(11,30)`) matching this project's own established convention, not a loosened assertion. `engine/tests/test_performance.py`: `open_chance=None` byte-identical regression check; `open_chance=1.0`/`0.0` real distributional checks (every hit lands on the correct side of 110); a real mixed-`open_chance` check showing both bands actually occur; `double_track` forwards `open_chance` to both real takes. Verified against real generated output (metalcore seed=6/6 sections): 44.0% of real guitar hits landed open (>=110), 56.0% muted -- metalcore's own declared `open_chance=0.45`, matching almost exactly. Scope gaps tracker's `open_chance` line updated from OPEN to DONE (2-tier), with the accent-aware 3rd/4th-tier refinement noted as a real, smaller, still-open follow-up. 533 passed, 1 skipped (up from 528).
- [x] X.22 Real, previously-missed drum-silencing inconsistency: `kick_pattern_for_role` had NO `chill`/`interlude` check, unlike `snare_pattern_for_role`/`hihat_pattern_for_role` (both of which have gone silent for those two atmospheric roles since X.9/X.11). Direct response to blunt real feedback: "it sounds nothing like metalcore." Investigated by generating real metalcore output and inspecting every drum layer per section -- found `chill`/`interlude` sections had snare=0/hihat=0 (correctly silent) but kick=16 (full preset-density kick, IDENTICAL to the loud intro/breakdown sections). This is a real, significant bug present for EVERY preset, not just metalcore: an atmospheric "breather" section that still hammers a full kick drum underneath completely erases the dynamic contrast the whole arrangement depends on -- exactly the kind of thing that can make a song built from otherwise-correct pieces (real density X.20, real phrase shape X.19, real articulation X.21) still read as "nothing like" its genre, since the drums never actually let up anywhere in the song. Fixed `drums.kick_pattern_for_role` with the exact same real rule the other two layers already use: `role in ("chill", "interlude")` returns an all-rest cell array, checked BEFORE the existing `preset_kick`/build-solo-overlay dispatch. `engine/tests/test_drums.py`: split the old (bugged) "leaves other roles on preset style" test to exclude chill/interlude, added a dedicated `test_kick_pattern_for_role_is_silent_for_chill_and_interlude`. `engine/tests/test_song.py`: fixed `test_kick_style_field_changes_real_song_kick_output` (previously asserted the OLD buggy universal-lock behavior for chill/interlude too) and added `test_every_preset_gets_silent_kick_for_chill_and_interlude` mirroring the exact real snare/hihat coverage tests already in place, for every real preset. Verified against real generated output (metalcore seed=6/6 sections): chill/interlude now show kick=0/snare=0/hihat=0 (fully silent, real contrast), while intro/breakdown/build keep their real full drum kit. 535 passed, 1 skipped (up from 533).
- [x] X.23 Real power chords on the main rhythm guitar -- the engine has been strictly monophonic all session. Direct response to blunt feedback: "It needs to be heavier." Register checked first and was already correct (metalcore's real generated intro/breakdown sit at MIDI 35-42, right at the bottom of `drop_b_7`'s tuning). Checked `song.pitches_per_cell`'s real return type instead: `list[int | None]` -- one pitch per hit, confirmed via every consumer (`bass.follow_guitar_rhythm`, `midi_export`, `reaper_project.py`) that the main riff has never played a real chord, for any preset, any section, this whole session. Real metal rhythm guitar is overwhelmingly power chords, not single notes. Checked for existing real machinery before designing anything new (same discipline as every prior X.*): `chords.solve_chord` (P1.11, ground-up fretboard fingering solver) and `riff.voice_chord_section`/`generate_chord_riff` (P3.11) are real and tested, but never wired into the main riff -- `chord_vocab.py`'s own module docstring literally names this exact gap, previously only ever acted on for chill/interlude's ambient `chord_quality` (X.6b). `generate_chord_riff` itself holds one FIXED chord shape for a whole cell (a static vamp) so doesn't fit directly -- this engine's riffs have real per-hit pitch movement (vocab-weighted intervals, pedal bias) that must be preserved. Reused the lower-level real primitive instead: `chords.solve_chord`, applied PER HIT. `engine/midi_export.py`: new `_guitar_chord_tone_pitches(fretboard, root, intervals=(0,7), max_span=4)` (real power chord = root+5th, not the fuller (0,7,12) triad, to avoid pushing a chord's upper tone into a thinner higher register on an already-low root; a real, easy follow-up if more fullness is wanted) -- picks the lowest-position fingering, falls back to `[root]` alone if nothing is reachable (fail closed, never fabricates, never crashes the export over one hit). New `_chord_cell_events` (same on/off-tick/velocity logic as `_cell_events`, one event per real chord tone instead of one per hit). Thickening happens ONLY at MIDI/`.rpp` export time for the two rhythm-guitar takes -- `pitches_per_cell`/`Motif`/`bass`/`judge()` all stay completely untouched, zero risk to the generation/judging pipeline. Applied to every role EXCEPT `chill`/`interlude` (`_CHORD_THICKENED_ROLES`), matching the established real precedent (kick/snare/hihat already exempt those two atmospheric roles, X.9/X.11/X.22) -- their real melodic/harmony character (`harmonize_line`) stays single-note. `engine/reaper_project.py` imports the same real helpers from `midi_export.py` (one source of truth, two serializers) and applies the identical dispatch. Real, expected test breakage fixed properly: 3 pre-existing tests assumed exactly one note per guitar hit -- updated to compute real expected chord-tone counts via the same `_guitar_chord_tone_pitches` helper, never loosened. New tests: real fallback-to-root check on an unreachable interval; a real end-to-end check that a chord-thickened section's exported track has genuine simultaneous note-ons at the same tick (not just an inflated count); confirmed chill/interlude stay excluded. Verified against real generated output (metalcore seed=6/6 sections): Guitar Take A shows 95 real chord hits (2+ simultaneous notes) vs 30 single-note hits (the chill/interlude sections). 539 passed, 1 skipped (up from 535).
- [x] X.24 Real cross-section blending -- guitar, bass, AND guitar-locking kick styles, done properly (not the initial guitar-only slice). Direct response to a real, confirmed-original reference track upload ("Try recreate it... learn why and implement fixes"; then explicitly "There's no time limitations flesh it out and do it properly rather than rush"): full audio analysis (demucs-isolated drums + `audio_vocab.py`'s real tempo/key/structure/rhythm-pattern/onset tools) found the engine's typical output was ~5x shorter than the reference for a comparable section count, and confirmed `structure.pickup`/`bridge`/`flatten` (P6.2, real and tested) were still unwired -- every section transition in every song this whole session has been a raw, unblended concatenation. Real risk found before implementing: `structure.pickup` returns cells carrying ONLY `duration`/`is_rest` (the reference's simpler data model never needed more) -- a naive port would silently strip this project's real per-cell keys (`role` for drums, `velocity`/`timing_offset` for guitar takes) at every blended boundary. New key-preserving reimplementations of the same real rule ("last n cells of prev copy first n of next"): `song._pickup_cells`/`_pickup_values`, applied as a pure/deterministic post-processing pass (zero rng consumption) once `_generate_attempt`'s main per-section loop completes. First shipped guitar-only (`guitar_take_a`/`guitar_take_b`/`pitches_per_cell`), then extended once told to do it properly: bass and guitar-locking kick styles ALSO now re-lock to the blended guitar, since both "follow" the guitar's rhythm and would otherwise silently desync from it exactly at the blended boundary cells. Both regenerations are pure/deterministic (`bass.follow_guitar_rhythm` has no rng at all; `drums.kick_pattern_for_style`'s own docstring confirms every real style is deterministic given guitar_cells) -- real, exact re-locks, not approximations. New `drums.resolve_kick_style(role, preset_kick, rng)` extracted from `kick_pattern_for_role` (same real resolution logic, `kick_pattern_for_role` now delegates to it, zero behavior change for its own existing callers/tests) so `song.py` can resolve+store which style a section's kick actually used, then reuse that SAME style (never a fresh random re-roll) when regenerating kick cells from the blended guitar. Styles that don't look at guitar's specific hit/rest positions (`two_step`/`blast`/`double_kick`) regenerate to byte-identical output, so the re-lock applies uniformly across all styles rather than branching per style. `bridge()`'s inserted-transition-cell device remains deliberately deferred (changes total section length, would desync every other instrument's own beat count -- a real, coordinated multi-instrument redesign). Found and fixed a real bug during testing: `chill`/`interlude` sections (`lead_mode == "harmony"`) tie `section["lead"]` to the guitar's PRE-blend hit count (`riff.harmonize_line` renders one lead note per guitar hit, computed before blending) -- blending those sections' own guitar broke that pairing and crashed `midi_export`'s real per-hit harmony zip (`StopIteration`). Excluded chill/interlude as the blend's own `this_section` (matching the established role-exclusion pattern everywhere else this session); `next_section`'s role doesn't matter since only `this_section`'s own cells get modified. `guitar_track`/`drum_track` (used only for `judge()`) both rebuilt from the blended sections after the pass, so judge scores the real final output. `engine/tests/test_song.py`: exact unit tests for the two pickup helpers (key preservation, empty-input edge cases); a real end-to-end tail/head match check for guitar cells+pitches at every non-chill/interlude boundary; confirmed chill/interlude stay untouched; confirmed snare/hihat are unaffected (derive from `motif.cell`, not `guitar_take_a`); real bass re-lock check (regenerating independently via `follow_guitar_rhythm` on each section's own blended data and requiring an exact match); real guitar-locking-kick-style re-lock check (`bounce` kick hits exactly match the blended guitar's real hit positions, for a preset with no `group_beats`); real regression check that guitar-shape-independent kick styles (`two_step`/`blast`) stay byte-identical. `engine/tests/test_drums.py`: `resolve_kick_style` matches `kick_pattern_for_role`'s own real choice exactly (same rng state, same result), and requires `rng` for overlay roles same as before. Fixed one pre-existing test (`test_kick_style_field_changes_real_song_kick_output`) that compared against the stale pre-blend `motif.cell` instead of the real blended `guitar_take_a` -- a real, expected consequence of the fix actually working, confirmed by asserting the two songs' blended guitar is identical (same seed, deterministic blend) before using it as the shared reference. Verified against real generated output (metalcore seed=6/6 sections): every non-chill/interlude guitar boundary shows an exact tail=head match; bass length matches guitar length in every section; chill/interlude stay correctly excluded from all three. 550 passed, 1 skipped (up from 539).
- [x] X.25 Real kick "burst" device. Direct evidence from the same real, confirmed-original reference-track analysis (X.24): a demucs-isolated drum stem, classified via `audio_vocab.classify_drum_onsets`, showed 32.5% of real kick-to-kick gaps under 150ms -- concentrated bursts inside an otherwise moderate-paced groove (overall kick rate ~1.3/sec, not relentless). The engine's only existing fast-kick style (`double_kick`, X.11) is a flat, continuous straight-16th pulse for the WHOLE section -- structurally different from a burst that stands out against a comparatively sparse baseline. New `drums._kick_burst`: base pattern is `_kick_sparse`'s real thinned every-other-guitar-hit pattern (not a full guitar-lock, so the burst reads as a genuine contrast, matching the reference's own moderate overall rate), with real dense consecutive-hit bursts overriding the next `_BURST_LEN` (4) cells every `_BURST_CYCLE_BEATS` (4.0) beats -- reuses the same real `_cyclic_hit_indices` cell-timeline mechanism every other overlay style in `drums.py` already uses, no new timing primitive invented. Registered as a new `_KICK_STYLES["burst"]` entry and added to `_ROLE_KICK_OVERRIDE_CHOICES` for `build`/`solo` alongside the existing `double_kick`/`blast` choices -- three real overlay styles now vary across seeds instead of two. `engine/tests/test_drums.py`: exact hand-verifiable real position checks (a 16-cell/4-beat fixture where the burst covers cells 0-3 as real consecutive hits, and the real `_kick_sparse` base pattern resumes exactly afterward, checked cell-by-cell); confirms a real second burst fires in a second cycle (8-beat fixture); bad-input rejection; updated the existing build/solo overlay-variety test to require all three real styles across seeds, not two. Verified against real generated output: metalcore seed=0's "build" section shows a real burst signature (80% hit rate with a real 5-cell consecutive run), distinct from blast's 100% and a plain guitar-lock's guitar-hit-count-matching rate. 553 passed, 1 skipped (up from 550).

---

## Phase 0 — Repo
- [x] P0.1 folders
- [x] P0.2 assets present
- [x] P0.3 pytest smoke
- [x] P0.4 gitignore / commit
- [x] P0.5 STATUS template

## Phase 1 — Tonal + presets
Scope: `## 2. Tonal/Harmonic` + `### 18.2` + `### 12.3` (chord-shape solver). Then `PORTS.md` Ww.
- [x] P1.1 `Fretboard` + MIDI↔(string,fret)
- [x] P1.2 Port `pitch_to_fret` from `reference/ww-forge-prior-attempt/engine/tab_score.py`
- [x] P1.3 TEST: unplayable MIDI rejected
- [x] P1.4 Scale library + cluster + power. Unique intervals
- [x] P1.5 TEST: no duplicate non-alias scales
- [x] P1.6 Tuning JSON from source MIDI (`engine/presets/tunings.json`, loaded via `presets.load_tunings`)
- [x] P1.7 Preset ids: groovy, djent, chill, tech, melodic, metalcore, deathcore (`slam` removed per user direction -- targets djent/deathcore/metalcore/technical deathcore, not slam). `progressive` added later (X.10). Strict JSON
- [x] P1.8 Schema validator called by loader -- `presets.validate_preset` called directly from `load_preset` (`presets.py:200`), the real load path, not a standalone check nobody invokes. `test_presets.py` covers malformed-input rejection.
- [x] P1.9 TEST glob `*.json` loads every file -- `presets.load_all_presets` globs `directory.glob("*.json")` (`presets.py:230`), never a hardcoded name list; `test_all_preset_json_files_load_via_glob` verifies this directly.
- [x] P1.10 Port VoiceLeader, shade(), ARC -- all three real and in `theory.py` (`VoiceLeader` class, `shade()`, `arc()`), used throughout the pipeline (motif generation, octave stabs, chord-quality dissonance bucketing).
- [x] P1.11 Chord-shape solver (ground-up, no lookup tables) -- `chords.solve_chord`, real fretboard enumeration; wired into `riff.voice_chord_section` and `chord_vocab.voice_named_chord` (X.6b), not a solver nobody calls.

## Phase 2 — Rhythm
Scope: `## 3. Rhythm` + `### 8.1` `### 8.2` `### 8.3`.
- [x] P2.1 Two-layer `{duration, is_rest}` then pitch
- [x] P2.2 Cell-tile polymeter + TEST 7 into 16 drifts
- [x] P2.3 Separate `metric_polyrhythm()` + TEST ≠ tile
- [x] P2.4 Real tuplet grids 3/5/7
- [x] P2.5 Blasts traditional/gravity/hammer; zero weight = never
- [x] P2.6 Shared rhythm id
- [x] P2.7 IRVD
- [x] P2.8 Seeded RNG + identical bytes twice

## Phase 3 — Motif / riff
Scope: `## 4. Riff-writing` + `### 17.1` + `## 6. New instrument layer` (P3.12 only).
- [x] P3.1 Motif = rhythm_cell + pitch deltas
- [x] P3.2 TEST same shape two roots
- [x] P3.3 Develop ops; lengths stay paired
- [x] P3.4 Cross-section theme id reused
- [x] P3.5 Gallop / stutter-chug change output
- [x] P3.6 Chromatic flags change output
- [x] P3.7 VoiceLeader leads
- [x] P3.8 Double-track two humanized takes
- [x] P3.9 Call-and-response: guitar reacts to drums/lead, not generated in isolation (§4)
- [x] P3.10 Slam devices: pinch-harmonic accent sim, low open-string chromatic creep (§4)
- [x] P3.11 Wire chord-shape solver (P1.11) into the pitch layer for chord-voiced sections -- a solver nobody calls is not done
- [x] P3.12 (optional, scope hedges with "possibly") second rhythm guitar harmonizing/doubling the lead (§6)

## Phase 4 — Drums
Scope: `### 11.6` `### 11.7`.
- [x] P4.1 Role → kit MIDI + fallback
- [x] P4.2 Kick follows guitar accents on breakdown
- [x] P4.3 Fills/blasts use shared-sequence
- [x] P4.4 Mine `reference/midi-corpus/` (1967 real, legally-clean groove/breakdown MIDIs, user-supplied superset of the original 63-file sample) for drum-fill density/placement vocabulary per §18.6 -- reference data only, never copied riffs; the symbolic engine (P4.1-P4.3) stays the actual writer. Queued after P4.1-P4.3 land to avoid touching drums.py while it's mid-build.

## Phase 5 — Bass
Scope: `### 14.2` (item 2, bass fretboard).
- [x] P5.1 Own 4/5-string fretboard
- [x] P5.2 Follows guitar rhythm; own playability

## Phase 6 — Structure
Scope: `## 5. Song-structure` + `### 10.3` + `## 3. Rhythm` (tempo-automation paragraph, P6.7 only).
- [x] P6.1 Weighted section graph
- [x] P6.2 flatten+pickup
- [x] P6.3 Half-time inside a section
- [x] P6.4 Modulation called from generation
- [x] P6.5 Judge/retry
- [x] P6.6 Tech/atmospheric interlude section type -- "not present at all currently" per §5
- [x] P6.7 Tempo curve: mid-song ramps/drops per §3. Reaper export still uses one constant tempo (§17.5) -- this is an internal song-data property, not a Reaper tempo-map change

## Phase 7 — Atmosphere
Scope: `## 16. Atmospheric`.
- [x] P7.1 GM 90 pad / 56 hit
- [x] P7.2 Synth doubles motif
- [x] P7.3 Accent-synced hits

## Phase 8 — Reaper
Scope: `## 11. Audio rendering` + `### 14.1` (reapy-boost, Surge XT, Supermassive).
- [x] P8.1 (superseded) Real Reaper project generation -- `engine/reaper_project.py`: `song_to_rpp(song, path)` writes a complete, directly-openable `.rpp` project file entirely offline, no live Reaper connection at any point. Investigated the scope doc's originally-chosen `reapy`/`reapy-boost` live-bridge approach first: got real, substantial progress (installed reapy-boost, MIT-licensed; automated the ENTIRE one-time bridge setup headlessly via config-file editing + HTTP-triggering Reaper's own built-in web control surface, no GUI clicks needed; found and fixed two real upstream bugs -- a socket-timeout bug causing `WinError 10053` on the first `send()` after `accept()`, and the activation script's `if __name__ == "__main__":` guard never firing under Reaper's script-execution model) but the connection remained unreliable in this environment even after both fixes, with no way to get further diagnostic visibility. Verified directly with the user that nothing Phase 8 actually needs requires a live connection: real REAPER project assembly, FX chains, and tempo automation are all expressible as a static file, and REAPER has a real, documented headless render flag (`-renderproject file.rpp`, confirmed via ReaTeam/Doc's REAPER-CLI.md) for the render step. Pivoted to `.rpp` generation, built against REAL ground truth rather than a guessed format: had REAPER itself (v7.79, already installed) import this project's own `midi_export.song_to_midi` output and save as `.rpp`, then reverse-engineered every field from that real file -- including base64-decoding the `<X>` track-name block to confirm it's the exact standard MIDI `0xFF 0x03 <name>` meta-event, not assumed. Reuses `midi_export`'s real event-extraction functions (one source of truth for per-track note/timing data, two serializers on top). **Verified end-to-end for real**: generated a project, had the user's actual installed Reaper open it, confirmed via screenshot -- 5 correctly-named tracks, real visible note content in every track, correct tempo, no error dialog. `engine/tests/test_reaper_project.py`: structural bracket-balance check across every real preset, the `<X>` block's base64 decoded and checked against the real MIDI meta-event format, guitar/drum note counts cross-checked against actual generated song data, real tempo-envelope values checked against `tempo_map`, reproducibility (same song -> same content, GUIDs excluded), bad-input rejection. 477 passed.
- [ ] P8.2 Separate buses (tracks already separate per-instrument; bus/send routing not yet added)
- [ ] P8.3 .RfxChain per track -- needs a small hand-built library of real FX-chain presets (NAM captures + cab IR, dialed in once by the user in Reaper's own GUI, per the scope doc's own §11.5 design) to splice into the generated `.rpp`; the splice mechanism itself is straightforward once real `.RfxChain` files exist
- [ ] P8.4 sfizz + role map
- [ ] P8.5 Surge + Supermassive
- [ ] P8.6 Orchestra
- [ ] P8.7 Adaptive mix -- real per-section automation envelopes, bakeable into the `.rpp` directly (no live connection needed) once a concrete automation target is scoped
- [ ] P8.8 Constant tempo map (already true: the generated `.rpp`'s `TEMPO`/`TEMPOENVEX` fields are the ONLY tempo data Reaper sees, per scope sec.17.5 -- nothing further needed here)

## Phase 9 — Editor
Scope: `## 10. Application / Editor`.
- [ ] P9.1 Timeline CRUD
- [ ] P9.2 Pro + Guided
- [ ] P9.3 Regen primitives
- [ ] P9.4 Accept/reroll/undo
- [ ] P9.5 Preview vs Render
- [ ] P9.6 VexFlow tab
- [ ] P9.7 Section-level save/load presets, distinct from song-level style presets (§10.2)

## Phase 10 — Export
Scope: `### 17.6` + `### 14.2` (items 10-11, project format + export matrix). JSON skeleton already in `### 18.2`.
- [x] P10.1 MIDI stems (real, playable, Reaper-importable) -- `engine/midi_export.py`: `song_to_midi(song, path, ppq=480)` writes a real Standard MIDI File (type 1) with a genuine per-section tempo track (X.6c's `tempo_map`, not flat), guitar take A/B, bass (its own fretboard's real positions), lead (harmony-mode notes timed hit-for-hit against the rhythm motif's own cells; solo-mode notes timed at the exact even 8th-note spacing `song.py`'s own note-count math implies, plus the legato tail's real per-note durations), and drums (the kick pattern actually assembled into the song, via `drums.note_for_role` for the real GM note number). Deliberately does NOT invent a fill/kick blend, since `song.py` itself never blends `section["fill"]` into the assembled drum track -- exporting one would be new arrangement logic, not wiring. `engine/tests/test_midi_export.py`: every real preset round-trips to a parseable file; guitar/bass pitches and hit counts cross-checked against the actual `pitches_per_cell`/bass-cell data (not just "a file was written"); tempo track values checked against real `tempo_map`; drum notes checked against the real GM kick value; solo-section note count checked against real `lead` data; bad-input rejection (non-positive ppq, mismatched tempo_map/sections length). `.rpp` project-file generation is now DONE too (see Phase 8 P8.1 -- `engine/reaper_project.py`, verified opening in the user's real Reaper). Mix audio and tab notation remain open (need real Reaper FX-chain work (P8.3) and VexFlow (P9.6) respectively).
- [ ] P10.2 Song JSON
- [ ] P10.3 Preset calibration

## Deferred
Vocals. Extra ML. Playability beyond pitch_to_fret.
