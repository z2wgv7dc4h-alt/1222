# CURRENT

task: X.1 (song.py integration) + follow-on fixes from user Q&A
phase: cross-cutting
status: DONE
last_pytest: 314 passed in 2.46s
note: Phase 7 merged, then engine/song.py::compose_song(preset_id, seed) built to close the biggest gap found on review -- no code had ever threaded a real Preset through one end-to-end run. While answering the user's questions about it, found and fixed THREE more real bugs the integration layer itself introduced: (1) ThemeRegistry (P3.4) was built but never wired into song generation -- every section got an independently-rolled motif instead of a developed recurring theme, which scope calls "the single highest-leverage change for making songs sound composed"; wired it in, keyed by section role, with invert() on alternate reuses. (2) Wiring ThemeRegistry surfaced a real bug: motif.invert() can push a rendered pitch below the guitar's lowest open string -- added _snap_to_playable_octave (same discipline bass.py already uses) rather than let it fabricate or crash. (3) compose_song's own `seed` parameter was DEAD -- it called structure.judge_and_retry, which always tries its own range(max_seeds) internally regardless of caller seed, so every call for a preset silently collapsed onto whichever of seeds 0-5 judged first. Replaced with a local retry loop seeded seed+attempt, confirmed different seeds now produce genuinely different song structures. Also added realistic per-role lead-guitar behavior (solo=dense featured lead, chill/interlude=harmonized doubling of the rhythm theme via riff.harmonize_line, everything else=silent -- a busy independent lead doesn't belong over a breakdown) after the user correctly pointed out a generic lead-every-section wasn't realistic. 314 passed in 2.46s.
updated: 2026-09-08
