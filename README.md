# God Tier Metal

**The lab is the active work.** Everything else is paused.

- **Start:** double-click `tools/boo-lab/START.bat`. Operator guide: `tools/boo-lab/USER.md`.
- **Keeper law:** only `source in {human, guess-accepted}` **and** `heard=true` is gold,
  written to `tools/boo-lab/data/sections.jsonl`. Machines may draft; they never label.
- **Do not write `../Ww`.** Do not import `engine/riff_model.py`.
- **Engine is paused.** Its shape is in `PLAN.md`; state in `docs/CURRENT.md`.

Read `CONTROL.txt` for the session loop, `TASKS.md` for the one active box, and
`SCOPE-INDEX.md` (points at `docs/archive/god-tier-metal-scope.md`; not a backlog).
