# CURRENT

task: preset lineup change + deeper audio_vocab
phase: cross-cutting
status: DONE
last_pytest: 333 passed in 13.45s
note: Two things landed together. (1) User direction: this project targets djent/deathcore/metalcore/technical-deathcore, not slam -- removed the slam PRESET (slam.json + id, NOT the slam.py technique module, which is unrelated pinch-harmonic/chromatic-creep code reusable across genres) and added real metalcore.json + deathcore.json presets (newly designed, not ported -- documented as such in PORTS.md, since the original style_packs.py source only had 6 presets and neither of these was among them). Preset lineup is now: djent, tech, metalcore, deathcore, melodic, chill, groovy (7 ids). Updated CLAUDE.md's stated identity line accordingly. (2) Extended audio_vocab.py with estimate_key (Krumhansl-Schmuckler correlation), structural_segments (real MFCC self-similarity boundaries, not equal slices), energy_curve, tempo_stability, classify_drum_onsets (KICK/SNARE/HIHAT_OR_CYMBAL by frequency band), and guitar_rhythm_pattern (straight/gallop/syncopated from onset timing, never pitch). Found and fixed a real bug in the first gallop-detection draft: bucketing each IOI against the GLOBAL median fails for a genuine short-short-long cycle, since the short interval IS the median (majority value) -- redesigned around CONSECUTIVE IOI ratios instead, verified against synthetic straight and gallop click tracks. Ran everything against the real user-supplied track: found it sits in A major (confirms scale library's missing major-family modes, X.6d, as a real not theoretical gap), and found the drum-role classifier's own real limitation on this track (implausibly few detected snares -- a hand-built frequency-band heuristic isn't reliable on a dense, cymbal-heavy mix; disclosed honestly rather than presented as ground truth).
updated: 2026-09-08
