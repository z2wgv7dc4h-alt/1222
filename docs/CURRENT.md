# CURRENT

task: X.25
phase: cross-cutting
status: DONE
last_pytest: 553 passed, 1 skipped in 11.96s
note: Real kick "burst" device. Direct evidence from the same real reference-track analysis (X.24): a demucs-isolated drum stem showed 32.5% of real kick-to-kick gaps under 150ms -- concentrated bursts inside an otherwise moderate groove, not a flat continuous pulse (that's already double_kick, a genuinely different device). New drums._kick_burst: base is _kick_sparse's real thinned pattern (not a full guitar-lock, so bursts read as real contrast against a moderate baseline matching the reference's own overall rate), with real dense consecutive-hit bursts overriding cells every _BURST_CYCLE_BEATS via the same real _cyclic_hit_indices mechanism every other overlay style already uses. Registered as a new "burst" kick style and a third real build/solo overlay choice alongside double_kick/blast. Verified against real generated output: metalcore seed=0's build section shows an 80% hit rate with a real 5-cell consecutive run, distinct from blast (100%) and guitar-lock. 553 passed (up from 550). Next: X.26 (delete confirmed-dead structure.generate_section_content/generate_song_sections), then the comprehensive audit explicitly requested by the user ("massive audit... make sure nothing's been rushed or skipped") covering the scope doc's own remaining items, a real comparison against Metalerator's quality bar, and any further research that could help.
updated: 2026-09-09
