# CURRENT

task: X.21
phase: cross-cutting
status: DONE
last_pytest: 533 passed, 1 skipped in 12.58s
note: Real open-string-vs-muted articulation, wired via velocity -- closes the gap X.20 left open (preset.open_chance's actual purpose). Direct response to real feedback: "its a big imrpovement... sounding a little more like metal. still lacking but a massive jump." performance.humanize_take had velocity_base=100.0 as a flat constant for every hit -- zero articulation contrast anywhere. Read the reference source further: wants_open = rng.random() < open_chance decides per-hit whether a note is open/ringing vs muted, real emitted velocities are 120 (open) vs 88/96/100 (otherwise), artic.py's real PALM_MUTE_VELOCITY_THRESHOLD=110 confirms opens sit comfortably above that line. Shipped a deliberately simplified 2-tier port (open=120/muted=88) rather than the reference's full 4-tier accent table, to avoid plumbing find_accents data through a new call chain this session. humanize_take gains open_chance=None (byte-identical default) parameter; song.py's one call site now passes open_chance=preset.open_chance. Real side effect found and fixed: judge()'s pm_ratio reads velocity<110 directly, so real bimodal velocity changes which seed-retries pass judge -- 2 pre-existing tests hardcoded seed=11 expecting a solo section, fixed with a real seed-search matching this project's convention. Verified: metalcore seed=6 real guitar hits landed 44.0% open vs its own declared open_chance=0.45 -- matches almost exactly. 533 passed (up from 528). Next: get real listening feedback on X.19+X.20+X.21 together -- then the reference's richer accent-aware velocity tiers, sec.14.2 item 5 (key modulation), P10.2 Song JSON, Phase 9 editor.
updated: 2026-09-09
