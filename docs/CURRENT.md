# CURRENT

task: X.23
phase: cross-cutting
status: DONE
last_pytest: 539 passed, 1 skipped in 11.62s
note: Real power chords on the main rhythm guitar -- the engine has been strictly monophonic all session. Direct response to blunt feedback: "It needs to be heavier." Register was already correct (metalcore sits at MIDI 35-42, bottom of drop_b_7). Checked song.pitches_per_cell instead: one pitch per hit, confirmed via every consumer this has been monophonic for every preset, every section, all session. Real machinery already existed (chords.solve_chord P1.11, riff.voice_chord_section P3.11, both real/tested) but was only ever wired into chill/interlude's ambient chord_quality (X.6b) -- chord_vocab.py's own docstring literally names this exact gap. generate_chord_riff itself doesn't fit (one fixed shape per whole cell, a static vamp) since this engine's riffs have real per-hit pitch movement to preserve -- reused solve_chord per-hit instead. New midi_export._guitar_chord_tone_pitches (real power chord = root+5th, lowest-position fingering, falls back to bare root if unreachable) and _chord_cell_events, applied at MIDI/.rpp export time only (generation/judging pipeline untouched) for every role except chill/interlude. reaper_project.py reuses the same real helpers. 3 pre-existing tests fixed to compute real expected chord-tone counts (never loosened); new tests for fallback behavior and real simultaneous-note proof. Verified: metalcore seed=6 Guitar Take A shows 95 real chord hits vs 30 single-note (chill/interlude). 539 passed (up from 535).

Also: user asked "what else is missing that's never been wired in" -- ran a full audit (grep every module's __all__ against song.py usage, verified each candidate individually rather than trusting raw grep). Confirmed real, still-open gaps: structure.pickup/bridge/flatten (P6.2's cross-section blending, real but genuinely higher-risk to wire -- touches 6 parallel per-section cell arrays that must stay index-aligned), groove.gallop_cell/stutter_chug_cell (real named metal rhythm devices, no clean preset/role slot yet), interplay.call_and_response, structure.apply_half_time (rhythm-level half-time, distinct from X.18's tempo-level one), slam.mark_pinch_harmonics/chromatic_creep. Confirmed DEAD (not a gap): structure.generate_section_content/generate_song_sections, superseded by song.py's own _generate_attempt -- candidate for deletion. Full list in TASKS.md's Scope gaps tracker.

Next: get real listening feedback on X.19-X.23 combined, then prioritize the cross-section blending fix (likely highest remaining impact, needs its own plan-mode pass) vs. gallop/stutter-chug wiring vs. sec.14.2 item 5 (key modulation).
updated: 2026-09-09
