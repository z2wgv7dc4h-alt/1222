# God Tier Metal

Local engine: djent / deathcore / metalcore / tech. Bar: IA, BoO, VoM. Python writes notes. React edits. Reaper renders. No cloud writer. No slam pack.

## Law

- Grid is the writer. Audio is paint after the score.
- Note = scale-legal `(string, fret)`. Seeded. Same seed, same bytes.
- Guided = same params as Pro. No vocals.
- Do not edit `../Ww`. Do not extend `123/`.
- Do not bulk-read scope. Open one heading if the ticket names it.
- One ticket. Listed files only. Stop at acceptance.
- Do not import `riff_model.py`. Do not train. Do not scrape Songsterr.
- Labyrinth listen path = fragment bank, one riff tiled. GM preview is not NAM.
- Lab for FLACs/tabs: `tools/boo-lab`. Do not invent a second parser if `riff_bank.py` already extracts.

## Build

```
cd engine && python -m pytest -q
```

## Session

1. Read `docs/CURRENT.md` then `TASKS.md` ## Now. That is the only box.
2. Read `docs/DECISIONS.md` if the ticket touches writer philosophy.
3. Implement that box. Pytest. Stop.
4. Do not `/next` a backlog. Do not append X.* devices because you found a gap.
5. Auto off when idle. No push unless asked.

Ports: `PORTS.md`. Deep scope: `SCOPE-INDEX.md` then one heading.
