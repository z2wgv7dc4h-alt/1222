# CURRENT

task: X.22
phase: cross-cutting
status: DONE
last_pytest: 535 passed, 1 skipped in 11.44s
note: Fixed a real, previously-missed drum-silencing inconsistency. Direct response to blunt feedback: "it sounds nothing like metalcore." Generated real metalcore output and inspected every drum layer per section directly: chill/interlude sections had snare=0/hihat=0 (correctly silent since X.9/X.11) but kick=16 -- the EXACT same full-density kick as the loud intro/breakdown sections. drums.kick_pattern_for_role had no chill/interlude check at all, unlike snare_pattern_for_role/hihat_pattern_for_role which both already go silent there. This is a real bug present for EVERY preset, not just metalcore -- a "quiet breather" section that still hammers a full kick drum completely erases the dynamic contrast the whole arrangement depends on, which could easily explain "sounds nothing like X" even with density (X.20), phrase structure (X.19), and articulation (X.21) all independently correct: the drums simply never let up anywhere in the song. Fixed with the exact same real rule the other two layers use: role in (chill, interlude) returns an all-rest kick pattern, checked before the existing dispatch. Verified against real generated output (metalcore seed=6/6 sections): chill/interlude now show kick=0/snare=0/hihat=0 (fully silent), intro/breakdown/build keep their real full kit. 535 passed (up from 533; 2 pre-existing tests had asserted the old buggy universal-kick-lock behavior for chill/interlude, fixed to match the corrected behavior; added a new test mirroring the existing snare/hihat silence coverage, extended to kick, across every preset). Next: get real listening feedback on this -- it's a real, structural arrangement fix, distinct in kind from the density/phrase/articulation fixes so far.
updated: 2026-09-09
